---
phase: 37-cpu-database-foundation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/registry/categories.ts
  - src/lib/registry/infrastructure-converters.ts
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/de.json
  - src/messages/it.json
autonomous: true
requirements:
  - CPUDB-01
  - CPUDB-02
  - CPUDB-03
  - CPUDB-04

must_haves:
  truths:
    - "The infrastructure category in categories.ts includes a 'cpu' subcategory visible in the registry"
    - "infrastructure-converters.ts has placeholder registry entries for cpu-comparison-calculator and server-refresh-calculator under subcategory 'cpu'"
    - "All 4 locale files (en, fr, de, it) have translation keys for cpu-comparison-calculator and server-refresh-calculator with no MISSING_MESSAGE errors"
    - "All 4 locale files have the 'cpu' subcategory label translated in categories.infrastructure.subcategories"
    - "npm run build completes without translation missing-key errors"
  artifacts:
    - path: "src/lib/registry/categories.ts"
      provides: "Infrastructure category with cpu subcategory added to subcategories array"
      contains: "id: \"cpu\""
    - path: "src/lib/registry/infrastructure-converters.ts"
      provides: "Registry entries for cpu-comparison-calculator and server-refresh-calculator"
      exports: ["infrastructureConverters"]
    - path: "src/messages/en.json"
      provides: "English translation keys for cpu calculators and cpu subcategory"
      contains: "cpu-comparison-calculator"
    - path: "src/messages/fr.json"
      provides: "French translations"
      contains: "cpu-comparison-calculator"
    - path: "src/messages/de.json"
      provides: "German translations"
      contains: "cpu-comparison-calculator"
    - path: "src/messages/it.json"
      provides: "Italian translations"
      contains: "cpu-comparison-calculator"
  key_links:
    - from: "src/lib/registry/infrastructure-converters.ts"
      to: "src/lib/registry/categories.ts"
      via: "subcategory: 'cpu' must match Subcategory id in categories.ts"
      pattern: "subcategory.*cpu"
    - from: "src/messages/en.json"
      to: "src/lib/registry/infrastructure-converters.ts"
      via: "converter id keys match translation keys"
      pattern: "\"cpu-comparison-calculator\""
---

<objective>
Wire the CPU calculator scaffolding into the registry and i18n system so Phase 38 and Phase 39 can add UI components without touching infrastructure again.

Purpose: The registry entries and translations must be in place before pages are built. Adding them here ensures the infrastructure category listing page shows CPU calculators and that no MISSING_MESSAGE build errors occur when Phase 38/39 render calculator pages.

Output:
- `src/lib/registry/categories.ts` — infrastructure category gains 'cpu' subcategory
- `src/lib/registry/infrastructure-converters.ts` — two new registry entries (cpu-comparison-calculator, server-refresh-calculator)
- `src/messages/en.json`, `fr.json`, `de.json`, `it.json` — translations for both calculators and the cpu subcategory label
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/registry/categories.ts
@src/lib/registry/infrastructure-converters.ts
@src/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cpu subcategory to infrastructure category + registry entries</name>
  <files>src/lib/registry/categories.ts
src/lib/registry/infrastructure-converters.ts</files>
  <action>
**Step A — categories.ts:**

In the `infrastructure` category entry (id: "infrastructure"), add a new subcategory to the `subcategories` array. The current array has: vmware, hyperv, kubernetes, cost. Append:

```typescript
{ id: "cpu", name: "CPU Performance", description: "CPU benchmarks and server refresh planning" },
```

Do NOT change any other subcategory or category. The infrastructure category description "Virtualization, Kubernetes, and datacenter tools" should remain unchanged.

**Step B — infrastructure-converters.ts:**

Add two new entries to the `infrastructureConverters` record. Import `Cpu` icon from `lucide-react` at the top of the file (add to the existing import statement). Use `Server` as fallback if `Cpu` is not available in the installed lucide-react version (check by attempting import; if no `Cpu` icon, use `Server`).

```typescript
"cpu-comparison-calculator": {
  id: "cpu-comparison-calculator",
  slug: "cpu-comparison-calculator",
  category: "infrastructure",
  subcategory: "cpu",
  keywords: [
    "cpu",
    "processor",
    "specint",
    "spec",
    "benchmark",
    "performance",
    "intel",
    "amd",
    "epyc",
    "xeon",
    "arm",
    "ampere",
    "altra",
    "comparison",
    "per-core",
    "per-watt",
    "sizing",
  ],
  icon: Cpu,  // or Server if Cpu unavailable
  featured: true,
},
"server-refresh-calculator": {
  id: "server-refresh-calculator",
  slug: "server-refresh-calculator",
  category: "infrastructure",
  subcategory: "cpu",
  keywords: [
    "server",
    "refresh",
    "fleet",
    "migration",
    "upgrade",
    "specint",
    "cpu",
    "performance",
    "headroom",
    "power",
    "tco",
    "datacenter",
    "rack",
    "socket",
  ],
  icon: Server,
  featured: true,
},
```

The icon for server-refresh-calculator MUST be `Server` (existing import). For cpu-comparison-calculator, attempt to use `Cpu` from lucide-react — if the package version doesn't export `Cpu`, fall back to `Server`.
  </action>
  <verify>
1. `npm run type-check` passes with zero errors.
2. `grep -n "id: \"cpu\"" src/lib/registry/categories.ts` — finds the new subcategory.
3. `grep -c "cpu-comparison-calculator\|server-refresh-calculator" src/lib/registry/infrastructure-converters.ts` — returns 2.
  </verify>
  <done>
- categories.ts infrastructure entry has subcategory {id: "cpu", name: "CPU Performance", description: "..."}
- infrastructure-converters.ts has cpu-comparison-calculator and server-refresh-calculator entries, both with subcategory: "cpu"
- TypeScript type-check passes with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add i18n translations in all 4 locales</name>
  <files>src/messages/en.json
src/messages/fr.json
src/messages/de.json
src/messages/it.json</files>
  <action>
Update all 4 locale files. For each file, make two additions:

**Addition 1 — subcategory label for "cpu":**

In the `categories.infrastructure.subcategories` object (which currently has vmware, hyperv, kubernetes, cost), add the "cpu" key:

- en.json: `"cpu": "CPU Performance"`
- fr.json: `"cpu": "Performance CPU"`
- de.json: `"cpu": "CPU-Leistung"`
- it.json: `"cpu": "Prestazioni CPU"`

Also add "cpu" to the top-level `subcategories` flat object (which mirrors category subcategories for the global subcategory label lookup):

- en.json: `"cpu": "CPU Performance"`
- fr.json: `"cpu": "Performance CPU"`
- de.json: `"cpu": "CPU-Leistung"`
- it.json: `"cpu": "Prestazioni CPU"`

**Addition 2 — converter translation keys:**

In the `converters` object, add entries for both calculators. All 4 locales must have BOTH entries with name, description, and metaDescription.

English (en.json):
```json
"cpu-comparison-calculator": {
  "name": "CPU Comparison",
  "description": "Compare up to 4 CPUs by SPECint2017 score, performance-per-core, and performance-per-watt",
  "metaDescription": "Free CPU comparison calculator. Compare Intel Xeon, AMD EPYC, and Ampere Altra CPUs by SPECint2017 benchmark scores, performance per core, and performance per watt."
},
"server-refresh-calculator": {
  "name": "Server Refresh",
  "description": "Plan a server fleet refresh: calculate how many new servers match your current fleet performance",
  "metaDescription": "Free server refresh calculator. Model a server fleet migration — calculate minimum new server count, headroom buffer, chassis constraints, and power budget impact."
}
```

French (fr.json):
```json
"cpu-comparison-calculator": {
  "name": "Comparaison CPU",
  "description": "Comparez jusqu'à 4 processeurs par score SPECint2017, performance par cœur et par watt",
  "metaDescription": "Calculateur de comparaison CPU gratuit. Comparez Intel Xeon, AMD EPYC et Ampere Altra par score SPECint2017, performance par cœur et par watt."
},
"server-refresh-calculator": {
  "name": "Renouvellement Serveurs",
  "description": "Planifiez le renouvellement d'un parc de serveurs : calculez combien de nouveaux serveurs correspondent aux performances actuelles",
  "metaDescription": "Calculateur de renouvellement serveurs gratuit. Modélisez la migration d'un parc de serveurs — nombre minimum de serveurs, marge de croissance, contraintes de châssis et budget d'alimentation."
}
```

German (de.json):
```json
"cpu-comparison-calculator": {
  "name": "CPU-Vergleich",
  "description": "Bis zu 4 CPUs nach SPECint2017-Score, Leistung pro Kern und pro Watt vergleichen",
  "metaDescription": "Kostenloser CPU-Vergleichsrechner. Intel Xeon, AMD EPYC und Ampere Altra nach SPECint2017-Benchmark, Leistung pro Kern und pro Watt vergleichen."
},
"server-refresh-calculator": {
  "name": "Server-Refresh",
  "description": "Server-Fleet-Erneuerung planen: Berechnen Sie, wie viele neue Server die aktuelle Fleet-Leistung erreichen",
  "metaDescription": "Kostenloser Server-Refresh-Rechner. Server-Fleet-Migration modellieren — minimale neue Serveranzahl, Wachstumspuffer, Chassis-Einschränkungen und Strombudget."
}
```

Italian (it.json):
```json
"cpu-comparison-calculator": {
  "name": "Confronto CPU",
  "description": "Confronta fino a 4 CPU per punteggio SPECint2017, performance per core e per watt",
  "metaDescription": "Calcolatore di confronto CPU gratuito. Confronta Intel Xeon, AMD EPYC e Ampere Altra per punteggio SPECint2017, performance per core e per watt."
},
"server-refresh-calculator": {
  "name": "Rinnovo Server",
  "description": "Pianifica il rinnovo di un parco server: calcola quanti nuovi server corrispondono alle prestazioni attuali",
  "metaDescription": "Calcolatore di rinnovo server gratuito. Modella la migrazione di un parco server — conteggio minimo di nuovi server, margine di crescita, vincoli chassis e budget energetico."
}
```

**Placement:** Insert the converter keys in alphabetical order within the converters object (after "compound-interest" entries, near other c-prefixed keys). Insert the subcategory "cpu" key after the "cost" key in both the categories.infrastructure.subcategories object and the top-level subcategories object.

**CRITICAL:** The JSON must remain valid after edits. Use proper comma placement — trailing commas are not allowed in JSON.
  </action>
  <verify>
1. `npm run type-check` passes with zero errors.
2. `node -e "JSON.parse(require('fs').readFileSync('src/messages/en.json','utf8')); console.log('en: valid JSON')"` — must not throw.
3. Same check for fr.json, de.json, it.json.
4. `grep -c "cpu-comparison-calculator" src/messages/en.json src/messages/fr.json src/messages/de.json src/messages/it.json` — each file must show count >= 1.
5. `grep -c "server-refresh-calculator" src/messages/en.json src/messages/fr.json src/messages/de.json src/messages/it.json` — each file must show count >= 1.
6. `npm run build` completes without MISSING_MESSAGE errors for cpu-comparison-calculator or server-refresh-calculator.
  </verify>
  <done>
- All 4 locale files have valid JSON with cpu-comparison-calculator and server-refresh-calculator entries (name, description, metaDescription)
- All 4 locale files have "cpu" key in categories.infrastructure.subcategories and in top-level subcategories
- npm run build passes without missing translation errors for these keys
  </done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes with zero errors
2. All 4 JSON files parse without errors
3. `grep "\"cpu\"" src/lib/registry/categories.ts` finds the infrastructure subcategory
4. `grep -c "cpu-comparison-calculator\|server-refresh-calculator" src/lib/registry/infrastructure-converters.ts` returns 2
5. `npm run build` completes (or at least does not fail on missing translation keys for these calculators)
</verification>

<success_criteria>
- Infrastructure category has cpu subcategory in categories.ts
- cpu-comparison-calculator and server-refresh-calculator are in infrastructure-converters.ts with subcategory: "cpu"
- All 4 locale files have translation keys for both calculators with name, description, metaDescription
- All 4 locale files have "cpu" subcategory label translated
- TypeScript type-check passes with zero errors
- Valid JSON across all 4 locale files
</success_criteria>

<output>
After completion, create `.planning/phases/37-cpu-database-foundation/37-02-SUMMARY.md`
</output>
