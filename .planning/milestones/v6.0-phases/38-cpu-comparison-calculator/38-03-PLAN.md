---
phase: 38-cpu-comparison-calculator
plan: 03
type: execute
wave: 3
depends_on:
  - 38-02
files_modified:
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/de.json
  - src/messages/it.json
autonomous: true
requirements:
  - CPUCMP-05
  - CPUCMP-06

must_haves:
  truths:
    - "Zero MISSING_MESSAGE errors appear when running the development server or build for any of the 4 locales"
    - "All UI labels in cpu-comparison-calculator.tsx have matching translation keys in all 4 locale files (en, fr, de, it)"
    - "npm run build completes successfully with no TypeScript errors and no missing-message warnings"
    - "The built output includes the cpu-comparison-calculator page for all 4 locales (en, fr, de, it)"
  artifacts:
    - path: "src/messages/en.json"
      provides: "Complete cpu-comparison-calculator translation keys in English"
      contains: "cpu-comparison-calculator"
    - path: "src/messages/fr.json"
      provides: "Complete cpu-comparison-calculator translation keys in French"
      contains: "cpu-comparison-calculator"
    - path: "src/messages/de.json"
      provides: "Complete cpu-comparison-calculator translation keys in German"
      contains: "cpu-comparison-calculator"
    - path: "src/messages/it.json"
      provides: "Complete cpu-comparison-calculator translation keys in Italian"
      contains: "cpu-comparison-calculator"
  key_links:
    - from: "src/app/[locale]/infrastructure/cpu-comparison-calculator/cpu-comparison-calculator.tsx"
      to: "src/messages/*.json"
      via: "useTranslations('converters.cpu-comparison-calculator')"
      pattern: "converters.cpu-comparison-calculator"
---

<objective>
Complete i18n for the CPU Comparison Calculator across all 4 locales and verify the build passes end-to-end.

Purpose: Zero MISSING_MESSAGE errors at runtime. The static export build must complete without failures, confirming the entire feature is production-ready.
Output: All 4 message files updated with complete translation keys; successful build output.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-cpu-comparison-calculator/38-02-SUMMARY.md
@src/app/[locale]/infrastructure/cpu-comparison-calculator/cpu-comparison-calculator.tsx
@src/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add complete i18n keys to all 4 locale files</name>
  <files>
    src/messages/en.json
    src/messages/fr.json
    src/messages/de.json
    src/messages/it.json
  </files>
  <action>
Read `src/app/[locale]/infrastructure/cpu-comparison-calculator/cpu-comparison-calculator.tsx` to discover all `t("key")` calls under the `converters.cpu-comparison-calculator` namespace. Then update all 4 message files.

**Required translation keys** (add inside `converters.cpu-comparison-calculator` object in each file; the stub with `name`, `description`, `metaDescription` already exists in en.json — extend it, do not replace):

```json
"cpu-comparison-calculator": {
  "name": "CPU Comparison",
  "description": "Compare up to 4 CPUs by SPECint2017 score, performance-per-core, and performance-per-watt",
  "metaDescription": "Free CPU comparison calculator. Compare Intel Xeon, AMD EPYC, and Ampere Altra CPUs by SPECint2017 benchmark scores, performance per core, and performance per watt.",
  "filterPanel": "Filters",
  "vendor": "Vendor",
  "vendorLabel": "Filter by Vendor",
  "generationLabel": "Filter by Generation",
  "vendors": {
    "all": "All Vendors",
    "intel": "Intel",
    "amd": "AMD",
    "arm": "ARM / Ampere"
  },
  "generations": {
    "all": "All Generations",
    "current": "Current Gen",
    "previous": "Previous Gen"
  },
  "selectCpus": "Select CPUs to Compare",
  "selectCpusHint": "Select 2 to 4 CPUs. First selected CPU is the baseline.",
  "selectedCount": "{count} selected",
  "emptyState": "Select at least 2 CPUs above to see the side-by-side comparison.",
  "resultsTitle": "Comparison Results",
  "baseline": "1.0x (baseline)",
  "cpuName": "CPU",
  "family": "Family",
  "generation": "Generation",
  "cores": "Cores",
  "tdpW": "TDP",
  "socketType": "Socket",
  "specint2017Base": "SPECint2017 Base",
  "specint2017Peak": "SPECint2017 Peak",
  "perfPerCore": "Perf / Core",
  "perfPerWatt": "Perf / Watt",
  "sizingRatio": "Sizing Ratio vs Baseline",
  "staleWarningTitle": "Data May Be Outdated",
  "staleWarningLink": "Verify at spec.org",
  "dataSource": "Source: SPEC.org SPECint2017 public submissions"
}
```

**Translations for each locale:**

fr.json — translate all values to French:
- filterPanel: "Filtres"
- vendor: "Fabricant"
- vendorLabel: "Filtrer par fabricant"
- generationLabel: "Filtrer par génération"
- vendors.all: "Tous les fabricants", vendors.intel: "Intel", vendors.amd: "AMD", vendors.arm: "ARM / Ampere"
- generations.all: "Toutes les générations", generations.current: "Génération actuelle", generations.previous: "Génération précédente"
- selectCpus: "Sélectionner les CPU à comparer"
- selectCpusHint: "Sélectionnez 2 à 4 CPU. Le premier CPU sélectionné est la référence."
- selectedCount: "{count} sélectionné(s)"
- emptyState: "Sélectionnez au moins 2 CPU ci-dessus pour voir la comparaison."
- resultsTitle: "Résultats de comparaison"
- baseline: "1,0x (référence)"
- cpuName: "CPU", family: "Famille", generation: "Génération", cores: "Cœurs", tdpW: "TDP", socketType: "Socket"
- specint2017Base: "SPECint2017 Base", specint2017Peak: "SPECint2017 Peak"
- perfPerCore: "Perf / Cœur", perfPerWatt: "Perf / Watt", sizingRatio: "Ratio de dimensionnement vs référence"
- staleWarningTitle: "Données potentiellement obsolètes", staleWarningLink: "Vérifier sur spec.org"
- dataSource: "Source : soumissions publiques SPECint2017 de SPEC.org"

de.json — translate all values to German:
- filterPanel: "Filter"
- vendor: "Hersteller"
- vendorLabel: "Nach Hersteller filtern"
- generationLabel: "Nach Generation filtern"
- vendors.all: "Alle Hersteller", vendors.intel: "Intel", vendors.amd: "AMD", vendors.arm: "ARM / Ampere"
- generations.all: "Alle Generationen", generations.current: "Aktuelle Generation", generations.previous: "Vorherige Generation"
- selectCpus: "CPUs zum Vergleich auswählen"
- selectCpusHint: "Wählen Sie 2 bis 4 CPUs. Der erste ausgewählte CPU ist die Referenz."
- selectedCount: "{count} ausgewählt"
- emptyState: "Wählen Sie mindestens 2 CPUs oben aus, um den Vergleich zu sehen."
- resultsTitle: "Vergleichsergebnisse"
- baseline: "1,0x (Referenz)"
- cpuName: "CPU", family: "Familie", generation: "Generation", cores: "Kerne", tdpW: "TDP", socketType: "Sockel"
- specint2017Base: "SPECint2017 Base", specint2017Peak: "SPECint2017 Peak"
- perfPerCore: "Leistung / Kern", perfPerWatt: "Leistung / Watt", sizingRatio: "Größenverhältnis zur Referenz"
- staleWarningTitle: "Daten möglicherweise veraltet", staleWarningLink: "Auf spec.org prüfen"
- dataSource: "Quelle: Öffentliche SPECint2017-Einreichungen von SPEC.org"

it.json — translate all values to Italian:
- filterPanel: "Filtri"
- vendor: "Produttore"
- vendorLabel: "Filtra per produttore"
- generationLabel: "Filtra per generazione"
- vendors.all: "Tutti i produttori", vendors.intel: "Intel", vendors.amd: "AMD", vendors.arm: "ARM / Ampere"
- generations.all: "Tutte le generazioni", generations.current: "Generazione attuale", generations.previous: "Generazione precedente"
- selectCpus: "Seleziona CPU da confrontare"
- selectCpusHint: "Seleziona da 2 a 4 CPU. La prima CPU selezionata è il riferimento."
- selectedCount: "{count} selezionata/e"
- emptyState: "Seleziona almeno 2 CPU sopra per vedere il confronto."
- resultsTitle: "Risultati del confronto"
- baseline: "1,0x (riferimento)"
- cpuName: "CPU", family: "Famiglia", generation: "Generazione", cores: "Core", tdpW: "TDP", socketType: "Socket"
- specint2017Base: "SPECint2017 Base", specint2017Peak: "SPECint2017 Peak"
- perfPerCore: "Perf / Core", perfPerWatt: "Perf / Watt", sizingRatio: "Rapporto dimensionamento vs riferimento"
- staleWarningTitle: "I dati potrebbero essere obsoleti", staleWarningLink: "Verifica su spec.org"
- dataSource: "Fonte: invii pubblici SPECint2017 di SPEC.org"

**Important:** Do NOT replace existing keys in the message files. Only add the missing keys to the existing `cpu-comparison-calculator` object. The `name`, `description`, and `metaDescription` keys already exist in en.json from Phase 37 — preserve them and add the remaining keys alongside them.

After editing all 4 files, verify with:
```bash
node -e "
const en = require('./src/messages/en.json');
const fr = require('./src/messages/fr.json');
const de = require('./src/messages/de.json');
const it = require('./src/messages/it.json');
const keys = Object.keys(en.converters['cpu-comparison-calculator']);
const missing = ['fr','de','it'].flatMap(l => {
  const msgs = {fr,de,it}[l].converters['cpu-comparison-calculator'];
  return keys.filter(k => !(k in msgs)).map(k => l + ':' + k);
});
console.log(missing.length === 0 ? 'ALL KEYS PRESENT' : missing);
"
```
  </action>
  <verify>node -e "const en = require('./src/messages/en.json'); const fr = require('./src/messages/fr.json'); const de = require('./src/messages/de.json'); const it = require('./src/messages/it.json'); const keys = Object.keys(en.converters['cpu-comparison-calculator']); const missing = ['fr','de','it'].flatMap(l => { const msgs = {fr,de,it}[l].converters['cpu-comparison-calculator']; return keys.filter(k => !(k in msgs)).map(k => l + ':' + k); }); console.log(missing.length === 0 ? 'ALL KEYS PRESENT' : JSON.stringify(missing));" 2>&1</verify>
  <done>Script outputs "ALL KEYS PRESENT"; all 4 locale files contain identical key structure under converters.cpu-comparison-calculator</done>
</task>

<task type="auto">
  <name>Task 2: Run type-check and build verification</name>
  <files></files>
  <action>
Run the following checks in sequence from the project root `/Users/fjacquet/Projects/converty`:

1. **TypeScript check:**
   ```bash
   npm run type-check
   ```
   Expected: exits 0, no errors.

2. **Lint check:**
   ```bash
   npm run check:fix
   ```
   Expected: exits 0 or fixes formatting automatically; no unfixable errors.

3. **Full build:**
   ```bash
   npm run build
   ```
   Expected: exits 0; the output directory `out/` contains files under `out/en/infrastructure/cpu-comparison-calculator/`, `out/fr/infrastructure/cpu-comparison-calculator/`, `out/de/infrastructure/cpu-comparison-calculator/`, `out/it/infrastructure/cpu-comparison-calculator/`.

If type-check fails: fix TypeScript errors in the cpu-comparison-calculator files before proceeding to build.
If build fails with MISSING_MESSAGE: identify the missing key from the error, add it to all 4 locale files, rebuild.
If build fails for other reasons: diagnose from the error output and fix.

Do NOT skip or suppress errors. The build must pass cleanly.
  </action>
  <verify>ls /Users/fjacquet/Projects/converty/out/en/infrastructure/cpu-comparison-calculator/ 2>/dev/null | head -5 || echo "Build output not found — build may have failed"</verify>
  <done>Build exits 0; out/en/infrastructure/cpu-comparison-calculator/index.html exists; out/fr/infrastructure/cpu-comparison-calculator/index.html exists; out/de/infrastructure/cpu-comparison-calculator/index.html exists; out/it/infrastructure/cpu-comparison-calculator/index.html exists</done>
</task>

</tasks>

<verification>
1. `node -e "..."` key-check script outputs "ALL KEYS PRESENT"
2. `npm run type-check` exits 0
3. `npm run build` exits 0
4. Built HTML pages exist for all 4 locales at out/[locale]/infrastructure/cpu-comparison-calculator/
5. No MISSING_MESSAGE in build output
</verification>

<success_criteria>
- All 4 locale files have complete and matching translation keys under converters.cpu-comparison-calculator
- Zero TypeScript errors
- Zero Biome lint errors
- Build completes without MISSING_MESSAGE warnings
- Static HTML pages generated for en, fr, de, it locales
</success_criteria>

<output>
After completion, create `.planning/phases/38-cpu-comparison-calculator/38-03-SUMMARY.md`
</output>
