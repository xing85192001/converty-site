---
phase: 06-dependency-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/decisions/0004-jspdf-upgrade.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false

must_haves:
  truths:
    - "Documentation accurately reflects jsPDF is already current (v4.0.0 from Jan 2025)"
    - "TypeScript compilation succeeds with jsPDF types"
    - "Build produces static export with working PDF export functionality"
    - "PDF export code uses correct v4.0.0 API patterns expected to generate valid PDFs"
    - "Requirements reflect verification scope (not upgrade scope)"
  artifacts:
    - path: ".planning/decisions/0004-jspdf-upgrade.md"
      provides: "Corrected ADR documenting jsPDF is current, no upgrade needed"
      contains: "Status: superseded"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 6 goal reflecting verification not upgrade"
      contains: "jsPDF verified current with working PDF export"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated DEP requirements reflecting current state"
      contains: "DEP-01: Verify jsPDF is current"
    - path: "src/lib/utils/pdf-export.ts"
      provides: "Unchanged, already uses correct v4.0.0 API"
      exports: ["exportToPdf", "exportSimpleResult"]
  key_links:
    - from: "ADR 0004"
      to: "package.json jsPDF version"
      via: "version claim must match reality"
      pattern: "jspdf.*4\\.0\\.0"
    - from: "src/lib/utils/pdf-export.ts"
      to: "jsPDF library"
      via: "named import (correct for v4.0.0)"
      pattern: "import.*\\{.*jsPDF.*\\}.*from.*\"jspdf\""
    - from: "TypeScript compiler"
      to: "jsPDF types"
      via: "built-in types (no @types/jspdf)"
      pattern: "npx tsc --noEmit"
---

<objective>
Correct jsPDF documentation and verify current implementation is working correctly.

Purpose: Phase 6 was planned based on incorrect information in ADR 0004, which claimed jsPDF v4.0.0 was outdated (from 2018) and needed upgrading to v2.5.2. Research revealed the opposite is true: jsPDF v4.0.0 is the LATEST version (released January 3, 2025), and the current implementation is already correct. This plan corrects the misinformation and verifies the implementation works as expected.

Output: Corrected documentation (ADR, roadmap, requirements) and verified PDF export functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dependency-upgrade/06-RESEARCH.md

# Current jsPDF implementation

@src/lib/utils/pdf-export.ts
@src/components/converter/pdf-export-button.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ADR 0004 with accurate jsPDF version information</name>
  <files>.planning/decisions/0004-jspdf-upgrade.md</files>
  <action>
Replace ADR 0004 content with corrected information:

**Status:** Change from "proposed" to "superseded" - this ADR contained incorrect information and is being replaced with accurate data

**Corrected context:** Explain that initial research contained errors:

- jsPDF v4.0.0 is the LATEST version (released January 3, 2025), not outdated
- Version progression: v1.x → v2.x → v3.x → v4.0.0 (semantic versioning, v4 is NEWER than v2)
- Original ADR incorrectly claimed v4.0.0 was from 2018 and v2.5.2 was "newer"
- This error cascaded to Phase 6 roadmap and requirements

**Decision outcome:** NO UPGRADE NEEDED

- Current implementation already uses correct v4.0.0 API (named import)
- TypeScript types are built-in (no @types/jspdf needed)
- No deprecated API methods in use
- PDF export functionality working correctly

**What was verified:**

1. package.json shows "jspdf": "^4.0.0" - confirmed latest via npm registry
2. src/lib/utils/pdf-export.ts uses named import: `import { jsPDF } from "jspdf"` - correct for v4.0.0
3. All API methods standard and current (setFontSize, text, addPage, save, etc.)
4. TypeScript compilation succeeds with no jsPDF type errors
5. Age Calculator successfully uses PDF export with PdfExportButton component

**Sources:**

- npm registry: <https://www.npmjs.com/package/jspdf> (v4.0.0 published 2025-01-03)
- GitHub releases: <https://github.com/parallax/jsPDF/releases/tag/v4.0.0>
- Official docs: <https://raw.githack.com/MrRio/jsPDF/master/docs/index.html>

Retain the implementation details section showing correct patterns for future reference.

Update links section to reference this plan (06-01-PLAN.md) instead of "TBD".
</action>
<verify>
git diff .planning/decisions/0004-jspdf-upgrade.md shows:

- Status changed to "superseded"
- Context corrected with accurate version history
- Decision outcome states "NO UPGRADE NEEDED"
- Links updated to reference 06-01-PLAN.md
  </verify>
  <done>ADR 0004 accurately reflects jsPDF v4.0.0 is current, no upgrade needed, with corrected version history and source citations</done>
  </task>

<task type="auto">
  <name>Task 2: Verify jsPDF implementation is correct and functional</name>
  <files>
src/lib/utils/pdf-export.ts
src/components/converter/pdf-export-button.tsx
src/app/[locale]/datetime/age/age-calculator.tsx
  </files>
  <action>
Verify current jsPDF implementation through automated checks:

1. **TypeScript compilation check:**

   - Run: `npx tsc --noEmit`
   - Verify: Zero jsPDF-related type errors
   - Confirm: Built-in types working (no @types/jspdf in package.json)

2. **Code pattern inspection:**

   - Verify src/lib/utils/pdf-export.ts uses named import: `import { jsPDF } from "jspdf"`
   - Verify API methods match v4.0.0 docs: new jsPDF(), doc.text(), doc.addPage(), doc.save()
   - Check no deprecated methods (no addHTML, fromHTML, or v1.x patterns)

3. **Build verification:**

   - Run: `npm run build`
   - Verify: Static export succeeds with no jsPDF errors
   - Check: PDF export code included in build output

4. **Integration verification:**
   - Verify PdfExportButton component imports from pdf-export.ts correctly
   - Verify Age Calculator uses PdfExportButton with proper data structure
   - Check pdfSections structure matches PdfSection interface

Do NOT attempt to actually generate PDFs (no DOM/browser available in CLI environment). Visual verification deferred to human testing.

Document findings in task output for SUMMARY.md.
</action>
<verify>

- `npx tsc --noEmit` exits 0 with no jsPDF type errors
- `npm run build` exits 0 with static export generated
- Code inspection confirms named import and v4.0.0 API patterns
- grep confirms no @types/jspdf in package.json
  </verify>
  <done>TypeScript compilation passes, build succeeds, code uses correct v4.0.0 API patterns, no deprecated methods detected</done>
  </task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md and REQUIREMENTS.md to reflect verification scope</name>
  <files>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
  </files>
  <action>
Update project documentation to correct Phase 6 scope:

**ROADMAP.md changes:**

Phase 6 section (lines 130-145):

- Update Goal: "jsPDF verified current (v4.0.0) with working PDF export functionality"
- Update Success Criteria:

  1. "Command `npm list jspdf` confirms version 4.0.0 (latest as of Jan 2025)"
  2. "Developer can export Age Calculator results to PDF and PDF opens correctly"
  3. "Code uses current jsPDF v4.0.0 API (named import, standard methods, built-in TypeScript types)"
  4. "TypeScript compilation succeeds with zero jsPDF-related errors"
  5. "PDF generation verified through type checking and build process"

- Update Plans: "Plans: 1 plan" with checkboxes:
  - [ ] 06-01-PLAN.md — Correct jsPDF documentation and verify implementation

**REQUIREMENTS.md changes:**

Update DEP-01, DEP-02, DEP-03 (lines 70-79):

- **DEP-01**: "Verify jsPDF is current (v4.0.0 latest)"

  - _Success criteria:_ `package.json` shows `jspdf@^4.0.0`, npm registry confirms this is latest stable, released Jan 2025

- **DEP-02**: "Verify PDF export functionality works correctly"

  - _Success criteria:_ TypeScript compilation passes, build succeeds with PDF export included, Age Calculator uses PdfExportButton correctly, code inspection confirms v4.0.0 API patterns

- **DEP-03**: "Verify jsPDF usage follows current API best practices"
  - _Success criteria:_ Code uses named import `{ jsPDF }`, built-in TypeScript types (no @types/jspdf), standard v4.0.0 API methods, no deprecated method calls

Update Traceability section (lines 307-309):

- DEP-01 | Phase 6 | 06-01 | Complete
- DEP-02 | Phase 6 | 06-01 | Complete
- DEP-03 | Phase 6 | 06-01 | Complete

Keep Success Criteria section (line 340) but update jsPDF line:

- Change: "jsPDF upgraded with PDF export verified" → "jsPDF verified current (v4.0.0) with PDF export working"
  </action>
  <verify>
  git diff shows:
- ROADMAP.md Phase 6 goal updated from "upgrade" to "verify"
- ROADMAP.md success criteria reflect verification not upgrade
- REQUIREMENTS.md DEP-01/02/03 updated to verification scope
- Traceability updated with 06-01 plan reference
  </verify>
  <done>ROADMAP.md and REQUIREMENTS.md reflect accurate Phase 6 scope (verification not upgrade), with updated success criteria and traceability mapping</done>
  </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify PDF export functionality works in browser</name>
  <what-built>
Automated verification confirms:
- jsPDF v4.0.0 API patterns correctly implemented
- TypeScript compilation passes with zero errors
- Production build succeeds with PDF export code included
- Age Calculator integrates PdfExportButton component

Human verification needed to confirm actual PDF generation works in browser.
</what-built>
<how-to-verify>

1. Start development server:

   ```bash
   npm run dev
   ```

2. Navigate to Age Calculator:

   - Open: <http://localhost:3000/en/datetime/age>
   - Enter sample data (e.g., birthdate: 1990-01-15)
   - Click "Export to PDF" button

3. Verify PDF export:

   - PDF download triggers automatically
   - Downloaded PDF opens without errors
   - PDF contains Age Calculator results with correct formatting
   - Data matches what's displayed on screen

4. Test edge cases:
   - Empty calculator state (button should be disabled)
   - Calculator with results (button enabled, export works)
   - Different browsers if available (Chrome, Firefox, Safari)

Expected behavior:

- PDF downloads as "converty-age-calculator-results.pdf"
- PDF contains calculator title, input values, and results
- PDF formatting is clean and readable
  </how-to-verify>
  <resume-signal>
  Type "approved" if PDF export works correctly, or describe any issues encountered (PDF won't download, errors in console, formatting problems, data mismatch, etc.)
  </resume-signal>
  </task>

</tasks>

<verification>
**Documentation verification:**
- [ ] ADR 0004 status changed to "superseded"
- [ ] ADR 0004 explains version confusion and correction
- [ ] ROADMAP.md Phase 6 goal reflects verification scope
- [ ] REQUIREMENTS.md DEP-01/02/03 updated for verification
- [ ] Traceability maps DEP-01/02/03 to 06-01

**Code verification:**

- [ ] `npx tsc --noEmit` passes with zero errors
- [ ] `npm run build` succeeds with static export
- [ ] Code inspection confirms named import pattern
- [ ] No deprecated jsPDF methods detected
- [ ] No @types/jspdf in package.json (built-in types)

**Implementation verification:**

- [ ] pdf-export.ts uses correct v4.0.0 API
- [ ] PdfExportButton component integrates correctly
- [ ] Age Calculator uses PDF export functionality
- [ ] Build output includes PDF export code

**Functional verification (checkpoint):**

- [ ] PDF export button appears in Age Calculator
- [ ] PDF downloads when button clicked
- [ ] Downloaded PDF opens correctly
- [ ] PDF contains accurate calculator results
      </verification>

<success_criteria>

1. ADR 0004 accurately documents jsPDF v4.0.0 is current (not outdated)
2. ROADMAP.md Phase 6 reflects verification scope (not upgrade scope)
3. REQUIREMENTS.md DEP-01/02/03 updated to match actual work
4. TypeScript compilation passes (`npx tsc --noEmit` exits 0)
5. Production build succeeds (`npm run build` exits 0)
6. Code uses correct jsPDF v4.0.0 API patterns (named import, standard methods)
7. No deprecated API methods detected in codebase
8. PDF export verified working in browser (checkpoint approved)
9. Documentation corrections committed to git with descriptive message
   </success_criteria>

<output>
After completion, create `.planning/phases/06-dependency-upgrade/06-01-SUMMARY.md` documenting:
- What was corrected (ADR 0004 misinformation)
- What was verified (jsPDF v4.0.0 current, implementation correct)
- What was updated (roadmap and requirements)
- Key findings (no upgrade needed, all functionality working)
- Checkpoint outcome (PDF export functional verification results)
</output>
