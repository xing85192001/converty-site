---
phase: 03-state-migration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Calculators load without errors"
    - "Input values update calculator state"
    - "Calculations return correct results"
    - "URL parameters sync when values change"
    - "Page refresh preserves values from URL"
    - "Reset button restores initial state"
  artifacts:
    - path: "src/stores/calculator-store.ts"
      provides: "Working Zustand factory for all calculators"
      exports: ["createCalculatorStore", "CalculatorState"]
      min_lines: 150
    - path: "src/lib/middleware/url-sync.ts"
      provides: "URL sync middleware integrated in stores"
      exports: ["createUrlSyncMiddleware"]
  key_links:
    - from: "Calculator components"
      to: "createCalculatorStore"
      via: "Store instantiation at module level"
      pattern: "const use.*Store = createCalculatorStore"
    - from: "Calculator stores"
      to: "URL sync middleware"
      via: "Factory integration (automatic)"
      pattern: "createUrlSyncMiddleware.*selectState"
    - from: "Input changes"
      to: "URL updates"
      via: "Debounced middleware (150ms)"
      pattern: "replaceState"
---

<objective>
Verify all calculators function correctly after legacy hook removal.

Purpose: Ensure the migration to Zustand is functionally complete by manually testing representative calculators across all categories. Confirms URL sync works, calculations are correct, and user experience is preserved.

Output: Manual testing verification complete, all calculator functionality confirmed working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-state-migration/03-RESEARCH.md
@.planning/phases/03-state-migration/03-01-SUMMARY.md

@src/stores/calculator-store.ts
@src/lib/middleware/url-sync.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Legacy hook removal complete (Plan 03-01). All 117 calculators now exclusively use Zustand stores via createCalculatorStore factory with integrated URL sync middleware.
  </what-built>

  <how-to-verify>
    Test representative calculators from each category to verify functionality:

    **Pre-testing setup:**
    1. Start dev server: `npm run dev`
    2. Open browser to http://localhost:3000/en

    **Test these calculators (minimum 10-12):**

    **Finance category:**
    - Mortgage Calculator (/en/finance/mortgage)
    - Compound Interest (/en/finance/compound-interest)
    - Loan Calculator (/en/finance/loan)

    **Math category:**
    - Percentage Calculator (/en/math/percentage)
    - Quadratic Calculator (/en/math/quadratic)
    - Triangle Calculator (/en/math/triangle)

    **DateTime category:**
    - Age Calculator (/en/datetime/age)
    - Date Calculator (/en/datetime/date)

    **Health category:**
    - BMI Calculator (/en/health/bmi)

    **Photo category:**
    - Aspect Ratio Calculator (/en/photo/aspect-ratio)

    **Video category:**
    - Bitrate Calculator (/en/video/video-bitrate)

    **For each calculator, verify:**
    1. **Page loads** - No errors in console, calculator renders
    2. **Input works** - Can type values, fields update
    3. **Calculation works** - Results appear, are correct
    4. **URL sync works** - Check address bar, parameters appear (wait ~200ms for debounce)
    5. **Persistence works** - Refresh page (Cmd+R / Ctrl+R), values preserved from URL
    6. **Reset works** - Click reset button (if present), values return to defaults
    7. **Validation works** - Enter invalid inputs (negative, empty), proper handling (null result or error message)

    **Expected behavior:**
    - All calculators load without errors
    - State updates reflect in UI immediately
    - URL parameters update after brief debounce (~150ms)
    - Refreshing page restores state from URL
    - Reset button clears state and URL parameters
    - Invalid inputs handled gracefully (no crashes)

    **If any issues found:**
    - Note which calculator(s) failed
    - Describe specific failure (error message, incorrect behavior)
    - Check browser console for errors
    - Include steps to reproduce

  </how-to-verify>

  <resume-signal>
    Type "approved" if all tested calculators work correctly, or describe any issues found with specific calculator names and failure descriptions.
  </resume-signal>
</task>

</tasks>

<verification>
Manual verification checklist completed for 10-12 representative calculators across all major categories.

All tested calculators must:

- Load without errors
- Accept input and calculate correctly
- Sync state to URL parameters
- Restore state from URL on refresh
- Handle invalid inputs gracefully
  </verification>

<success_criteria>

- [ ] Minimum 10 calculators tested from different categories
- [ ] All tested calculators load without console errors
- [ ] Input updates work correctly in all tested calculators
- [ ] Calculations return correct results
- [ ] URL parameters sync within ~200ms of input change
- [ ] Page refresh preserves calculator state from URL
- [ ] Reset functionality works (where applicable)
- [ ] Invalid inputs handled gracefully (no crashes)
- [ ] User confirms all tested functionality works as expected
      </success_criteria>

<output>
After completion, create `.planning/phases/03-state-migration/03-02-SUMMARY.md`
</output>
