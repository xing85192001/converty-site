---
phase: 04-progressive-web-app
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - public/sw.js
  - src/lib/pwa/offline-detector.ts
  - src/components/ui/offline-banner.tsx
autonomous: true

must_haves:
  truths:
    - "Service worker caches calculator pages for offline use"
    - "Static assets (JS, CSS, images) cached with Cache First strategy"
    - "User sees visual indication when offline"
    - "Offline detection updates when network status changes"
  artifacts:
    - path: "public/sw.js"
      provides: "Service worker with Workbox caching"
      contains: "importScripts.*workbox"
      min_lines: 50
    - path: "src/lib/pwa/offline-detector.ts"
      provides: "Offline detection hook"
      exports: ["useOnlineStatus"]
      min_lines: 20
    - path: "src/components/ui/offline-banner.tsx"
      provides: "Offline UI indicator"
      exports: ["OfflineBanner"]
      min_lines: 15
  key_links:
    - from: "public/sw.js"
      to: "cached resources"
      via: "fetch event interception"
      pattern: "addEventListener.*fetch"
    - from: "src/lib/pwa/offline-detector.ts"
      to: "navigator.onLine + events"
      via: "online/offline event listeners"
      pattern: "addEventListener.*online|offline"
    - from: "src/components/ui/offline-banner.tsx"
      to: "src/lib/pwa/offline-detector.ts"
      via: "hook import and usage"
      pattern: "useOnlineStatus"
---

<objective>
Implement service worker with Workbox caching strategies and offline detection UI.

Purpose: Enable offline calculator functionality (PWA-01) and provide user feedback when network is unavailable (PWA-04). Service worker intercepts requests and serves cached content, making calculators work without internet.

Output: Working service worker with intelligent caching, plus offline detection hook and UI component for user feedback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-progressive-web-app/04-RESEARCH.md
@src/app/[locale]/layout.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Install Workbox dependencies and create service worker</name>
  <files>
    package.json
    public/sw.js
  </files>
  <action>
Install Workbox runtime libraries for service worker implementation:

```bash
npm install workbox-window
npm install -D workbox-build
```

DO NOT install workbox-precaching, workbox-routing, workbox-strategies separately - these will be loaded via importScripts in the service worker from CDN (Workbox v7 standard pattern).

Create public/sw.js implementing:

1. Import Workbox from CDN using importScripts:

   ```javascript
   importScripts(
     "https://storage.googleapis.com/workbox-cdn/releases/7.0.0/workbox-sw.js"
   );
   ```

2. Configure Workbox with skipWaiting and clientsClaim for immediate activation

3. Implement caching strategies per resource type (from RESEARCH.md patterns):

   - HTML/Document requests: NetworkFirst with 7-day cache fallback (calculators work offline)
   - Static assets (JS, CSS, images): CacheFirst with 30-day expiration (long-lived, immutable)
   - Fonts: StaleWhileRevalidate with 20 entry limit (balanced freshness)

4. Add cache names: 'pages-cache-v1', 'static-cache-v1', 'font-cache-v1'

5. Add CacheableResponsePlugin (cache only status 0, 200)

6. Add ExpirationPlugin with maxEntries and maxAgeSeconds per strategy

7. Add offline fallback: If document fetch fails and not in cache, serve simple offline message

WHY Workbox CDN: Research shows importScripts pattern is standard for Workbox v7, avoids bundling issues with static export, and provides automatic cache versioning.

WHY these strategies: NetworkFirst for HTML ensures fresh content when online but offline fallback. CacheFirst for static assets leverages immutability (Next.js content hashes). Research confirms this pattern for static site PWAs.

IMPORTANT: Do NOT use precacheAndRoute with self.\_\_WB_MANIFEST yet - that requires build integration (Plan 03). Use runtime caching only for now.
</action>
<verify>

1. package.json shows workbox-window and workbox-build in dependencies/devDependencies
2. public/sw.js exists and contains:
   - importScripts call for Workbox CDN
   - registerRoute calls for HTML, static assets, fonts
   - CacheFirst, NetworkFirst, StaleWhileRevalidate strategies
3. npx tsc --noEmit passes (no TypeScript errors)
4. Service worker syntax valid: Open public/sw.js in VS Code, check for syntax errors
   </verify>
   <done>
   Workbox dependencies installed. Service worker file exists with caching strategies for HTML, static assets, and fonts. File is syntactically valid JavaScript ready for registration.
   </done>
   </task>

<task type="auto">
  <name>Create offline detection hook and banner component</name>
  <files>
    src/lib/pwa/offline-detector.ts
    src/components/ui/offline-banner.tsx
  </files>
  <action>
Create offline detection infrastructure following RESEARCH.md Pattern 4.

1. Create src/lib/pwa/offline-detector.ts:

   - Export useOnlineStatus() hook (client-side only)
   - useState(true) for isOnline (default online)
   - useEffect to:
     - Initialize: setIsOnline(navigator.onLine)
     - Add event listeners: window.addEventListener('online', handleOnline) and window.addEventListener('offline', handleOffline)
     - Cleanup: removeEventListener in return function
   - Return isOnline boolean

2. Create src/components/ui/offline-banner.tsx:

   - "use client" directive (client component)
   - Import useOnlineStatus from offline-detector
   - If isOnline, return null (no banner)
   - If offline, render fixed banner at top with:
     - Tailwind classes: "fixed top-0 left-0 right-0 z-50 bg-yellow-500 text-black px-4 py-2 text-center text-sm font-medium"
     - Message: "You are offline. Calculators will work from cache."
     - Icon: Optional - use lucide-react WifiOff icon if desired

3. Export OfflineBanner component for use in layout (Plan 03 will add to layout)

WHY navigator.onLine + events: Research shows navigator.onLine alone can be stale. Event listeners ('online', 'offline') provide real-time updates when network status changes.

WHY fixed banner: Non-intrusive, always visible, auto-dismisses when back online. Yellow (warning) color is standard for transient states.

IMPORTANT: Both files must have "use client" since they use React hooks and browser APIs not available server-side.
</action>
<verify>

1. src/lib/pwa/offline-detector.ts exists and exports useOnlineStatus
2. src/components/ui/offline-banner.tsx exists and exports OfflineBanner
3. npx tsc --noEmit passes (TypeScript validates hook and component)
4. Both files have "use client" directive at top
5. Biome lint passes: npm run lint (no React hook dependency issues)
   </verify>
   <done>
   Offline detection hook exists and properly manages online/offline state with event listeners. Banner component exists and uses hook to show/hide offline indicator. Both ready for integration in layout.
   </done>
   </task>

</tasks>

<verification>
After completing both tasks:

1. Workbox dependencies installed (workbox-window, workbox-build)
2. Service worker file exists with Workbox caching strategies
3. Service worker implements NetworkFirst for HTML, CacheFirst for static assets
4. Offline detection hook exists with navigator.onLine + event listeners
5. Offline banner component exists and uses detection hook
6. All TypeScript and lint checks pass
   </verification>

<success_criteria>

- [x] Service worker implemented with Workbox (PWA-01 core logic)
- [x] Caching strategies configured for HTML, JS, CSS, images, fonts
- [x] Offline detection hook created with real-time updates
- [x] Offline banner component created for user feedback (PWA-04)
- [x] All files pass TypeScript and Biome lint checks
- [x] No build errors introduced
      </success_criteria>

<output>
After completion, create `.planning/phases/04-progressive-web-app/04-02-SUMMARY.md` documenting:
- Workbox version and CDN approach used
- Caching strategies implemented (which strategy for which resource type)
- Cache names and expiration policies
- Offline detection implementation approach
- Offline banner UI design decisions
</output>
