---
phase: 04-progressive-web-app
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/lib/pwa/register-sw.ts
  - src/app/[locale]/layout.tsx
  - scripts/generate-sw.js
  - package.json
  - src/components/ui/install-prompt.tsx
autonomous: true

must_haves:
  truths:
    - "Calculators work offline after first visit"
    - "Install prompt appears on compatible browsers"
    - "iOS users receive installation guidance"
    - "Service worker activates without manual intervention"
    - "Offline functionality preserved across browser sessions"
  artifacts:
    - path: "src/lib/pwa/register-sw.ts"
      provides: "Service worker registration logic"
      exports: ["registerServiceWorker"]
      min_lines: 25
    - path: "scripts/generate-sw.js"
      provides: "Build-time SW manifest generation"
      min_lines: 40
    - path: "src/components/ui/install-prompt.tsx"
      provides: "PWA install UI component"
      exports: ["InstallPrompt"]
      min_lines: 60
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/lib/pwa/register-sw.ts"
      via: "useEffect registration call"
      pattern: "registerServiceWorker"
    - from: "scripts/generate-sw.js"
      to: "public/sw.js"
      via: "workbox-build injectManifest"
      pattern: "generateSW|injectManifest"
    - from: "src/components/ui/install-prompt.tsx"
      to: "beforeinstallprompt event"
      via: "window event listener"
      pattern: "beforeinstallprompt"
---

<objective>
Integrate service worker registration, build-time manifest generation, and install prompt UI.

Purpose: Complete PWA implementation by connecting all pieces. Registration activates service worker, build script generates precache manifest, install prompt enables "Add to Home Screen" functionality.

Output: Fully integrated PWA with automatic service worker registration, build-time optimization, and cross-platform install UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-progressive-web-app/04-RESEARCH.md
@.planning/phases/04-progressive-web-app/04-01-PLAN.md
@.planning/phases/04-progressive-web-app/04-02-PLAN.md
@src/app/[locale]/layout.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Create service worker registration and integrate into layout</name>
  <files>
    src/lib/pwa/register-sw.ts
    src/app/[locale]/layout.tsx
  </files>
  <action>
Create service worker registration infrastructure following RESEARCH.md Pattern 1.

1. Create src/lib/pwa/register-sw.ts:

   - Export registerServiceWorker() function
   - Check if 'serviceWorker' in navigator (browser support)
   - Check process.env.NODE_ENV === 'production' (dev mode check)
   - If both true, call navigator.serviceWorker.register('/sw.js', { scope: '/' })
   - Handle registration promise:
     - Success: console.log('Service worker registered:', registration.scope)
     - Error: console.error('Service worker registration failed:', error)
   - Return void (fire-and-forget pattern)
   - Add TypeScript types for all parameters and return values

2. Update src/app/[locale]/layout.tsx to add registration:

   - Create new client component: src/app/[locale]/sw-registration.tsx
   - "use client" directive
   - Import useEffect from react
   - Import registerServiceWorker from @/lib/pwa/register-sw
   - Import OfflineBanner from @/components/ui/offline-banner
   - Import InstallPrompt from @/components/ui/install-prompt
   - useEffect(() => { registerServiceWorker(); }, []) for one-time registration
   - Render OfflineBanner component (from Plan 02)
   - Render InstallPrompt component (from Task 3)
   - Return null for the registration effect itself (registration happens in effect, no UI)

3. In src/app/[locale]/layout.tsx:
   - Import SWRegistration component
   - Add <SWRegistration /> INSIDE the ThemeProvider but BEFORE the main div
   - This ensures SW registers on first page load regardless of route

WHY production-only registration: Research (RESEARCH.md Pitfall 5) shows service worker caching breaks hot reload in development. Development needs fresh code on every change.

WHY separate client component: Root layout is server component. Service worker registration requires browser APIs (navigator, useEffect). Separate client component provides clean boundary.

WHY scope '/' : Converty uses locale routing (/en/, /fr/, etc). Service worker at root scope intercepts all requests. If scope was /en/, would miss /fr/ requests.

WHY integrate InstallPrompt here: Task 3 creates the component, but it needs to be rendered in the layout to be visible to users. Including it in SWRegistration component ensures it's available globally.

IMPORTANT: Do NOT add to root layout (src/app/layout.tsx) - that's just HTML shell. Add to locale layout where application logic lives.
</action>
<verify>

1. src/lib/pwa/register-sw.ts exists and exports registerServiceWorker
2. src/app/[locale]/sw-registration.tsx exists as client component
3. src/app/[locale]/layout.tsx imports and renders SWRegistration
4. src/app/[locale]/sw-registration.tsx renders both OfflineBanner and InstallPrompt
5. npx tsc --noEmit passes (TypeScript validates all changes)
6. npm run lint passes (Biome validates React patterns)
7. File src/app/[locale]/sw-registration.tsx has "use client" directive
8. Registration code checks both navigator.serviceWorker AND NODE_ENV
   </verify>
   <done>
   Service worker registration implemented with production-only guard. Locale layout includes registration component, offline banner, and install prompt. Registration happens automatically on first page load in production builds.
   </done>
   </task>

<task type="auto">
  <name>Create build integration script for service worker</name>
  <files>
    scripts/generate-sw.js
    package.json
  </files>
  <action>
Create build-time script to generate service worker with precache manifest using Workbox CLI.

1. Create scripts/generate-sw.js:

   - Import workbox-build: const { generateSW } = require('workbox-build');
   - Configure generateSW with:
     - globDirectory: 'out' (Next.js static export output)
     - globPatterns: ['**/*.{html,js,css,png,jpg,jpeg,svg,webp,woff2}']
     - swDest: 'out/sw.js' (output location)
     - skipWaiting: true (activate immediately)
     - clientsClaim: true (take control of pages)
     - runtimeCaching array:
       - HTML documents: NetworkFirst with pages-cache, 7-day expiration
       - Images: CacheFirst with images-cache, 30-day expiration, 100 max entries
       - Fonts: StaleWhileRevalidate with font-cache, 20 max entries
   - Call generateSW and log result: `Generated SW, precaching ${count} files (${size} bytes)`
   - Add error handling: catch and exit with code 1 on failure

2. Update package.json scripts:

   - Rename current "build" to "build:next": "next build"
   - Create new "build": "next build && node scripts/generate-sw.js"
   - This ensures SW generation happens AFTER Next.js build completes

3. Add workbox-build validation:
   - Script should check workbox-build is installed (already added in Plan 02)
   - If missing, provide clear error: "workbox-build not found. Run: npm install -D workbox-build"

WHY post-build script: Research shows workbox-build needs static files to exist before generating precache manifest. Next.js build creates files in out/, then script scans and injects manifest.

WHY generateSW not injectManifest: public/sw.js from Plan 02 uses Workbox CDN runtime. generateSW creates complete SW file with precaching. We'll REPLACE public/sw.js with generated version during build.

IMPORTANT: This means public/sw.js serves as template/reference. Build process generates actual out/sw.js that production uses. This is standard pattern for static exports.

NOTE: After this task, public/sw.js can be simplified to just Workbox CDN import since generateSW handles everything. Or keep it for development reference.
</action>
<verify>

1. scripts/generate-sw.js exists and uses workbox-build
2. package.json "build" script runs "next build && node scripts/generate-sw.js"
3. Run test build: npm run build
4. Build succeeds without errors
5. File out/sw.js exists after build (generated service worker)
6. File out/sw.js contains precacheAndRoute with manifest entries
7. Console output shows: "Generated SW, precaching X files (Y bytes)"
   </verify>
   <done>
   Build integration script exists and generates service worker with precache manifest. Build process automatically creates optimized SW with all static assets precached. Production builds include complete, working service worker.
   </done>
   </task>

<task type="auto">
  <name>Create install prompt component with iOS detection</name>
  <files>src/components/ui/install-prompt.tsx</files>
  <action>
Create PWA install prompt component following RESEARCH.md Pattern 5 (complete example with iOS detection).

Implement src/components/ui/install-prompt.tsx:

- "use client" directive
- Import useState, useEffect from react
- Import lucide-react icons: Download, Share

Define interface for beforeinstallprompt event:

```typescript
interface BeforeInstallPromptEvent extends Event {
  prompt: () => Promise<void>;
  userChoice: Promise<{ outcome: "accepted" | "dismissed" }>;
}
```

Component state:

- installPrompt: BeforeInstallPromptEvent | null
- isIOS: boolean (detect iOS devices)
- isStandalone: boolean (detect if already installed)

useEffect for initialization:

1. Detect iOS: /iPad|iPhone|iPod/.test(navigator.userAgent)
2. Detect standalone mode: window.matchMedia('(display-mode: standalone)').matches
3. Add beforeinstallprompt listener (Android/Desktop Chrome only)
4. Prevent default on event, save to state
5. Cleanup: remove listener on unmount

Handle install button click:

- Call installPrompt.prompt()
- Await installPrompt.userChoice
- If accepted, clear prompt state (hide button)

Render logic:

1. If already installed (isStandalone), return null
2. If iOS and no install prompt: Show iOS instructions with Share icon
   - Message: "Install this app: tap Share then 'Add to Home Screen'"
   - Use info box styling (blue background)
3. If install prompt available: Show install button
   - Button text: "Install Converty"
   - Include Download icon
   - Primary button styling

Styling:

- Use Tailwind classes matching existing button/card components
- iOS instructions: Subtle info box, not intrusive
- Install button: Primary CTA button (prominent but not annoying)
- Fixed position bottom-right or bottom-center (non-blocking)

WHY feature detection: Research (Pitfall 4) shows iOS doesn't support beforeinstallprompt. Must detect platform and show appropriate UI.

WHY standalone check: Users who already installed shouldn't see install prompt. Display mode detection is reliable check.

IMPORTANT: Progressive enhancement - component gracefully handles all scenarios: iOS, Android, Desktop, already installed, not installable. Never breaks or shows broken UI.

NOTE: This component will be integrated into the layout in Task 1 via the SWRegistration component.
</action>
<verify>

1. src/components/ui/install-prompt.tsx exists and exports InstallPrompt
2. Component has "use client" directive
3. npx tsc --noEmit passes (TypeScript validates event types)
4. Component handles all states: iOS, Android, installed, not installable
5. Biome lint passes: npm run lint (no React hook dependency warnings)
6. BeforeInstallPromptEvent interface properly extends Event
   </verify>
   <done>
   Install prompt component exists with iOS detection and beforeinstallprompt handling. Component shows appropriate UI for each platform and install state. Component will be integrated into layout via Task 1's SWRegistration component.
   </done>
   </task>

</tasks>

<verification>
After completing all tasks:

1. Service worker registration implemented and integrated into locale layout
2. Registration only happens in production (NODE_ENV check present)
3. Build script exists and generates SW with precache manifest
4. Build process runs: next build -> generate-sw.js
5. Install prompt component exists with iOS/Android detection
6. Install prompt integrated into layout via SWRegistration component
7. All TypeScript and lint checks pass
8. Test build succeeds and creates out/sw.js
   </verification>

<success_criteria>

- [x] Service worker registration implemented (production-only)
- [x] Registration integrated into locale layout with offline banner AND install prompt
- [x] Build script generates service worker with Workbox
- [x] package.json build script updated for post-build SW generation
- [x] Install prompt component created with platform detection
- [x] Install prompt integrated into layout (not orphaned)
- [x] All PWA infrastructure connected and ready for testing
- [x] Build succeeds and generates complete service worker
      </success_criteria>

<output>
After completion, create `.planning/phases/04-progressive-web-app/04-03-SUMMARY.md` documenting:
- Service worker registration approach (where, when, how)
- Build integration strategy (post-build script vs build plugin)
- Install prompt implementation (iOS vs Android handling)
- Install prompt integration location (SWRegistration component in layout)
- Any decisions about SW scope, caching configuration, or UI placement
- Files modified and integration points
</output>
