---
phase: 01-type-safety-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.json
autonomous: true

must_haves:
  truths:
    - Developer runs npm run lint and sees explicit any type errors
    - Biome enforces noExplicitAny at error level
    - TypeScript strict mode remains enabled
  artifacts:
    - path: biome.json
      provides: Biome linter configuration with noExplicitAny enforcement
      contains: '"noExplicitAny": "error"'
      min_lines: 120
  key_links:
    - from: biome.json noExplicitAny rule
      to: all TypeScript files
      via: Biome linter enforcement
      pattern: '"noExplicitAny":\\s*"error"'
---

<objective>
Enable Biome's noExplicitAny rule to enforce type safety and surface all explicit any types in the codebase.

Purpose: Activate linting enforcement for TYPE-01 requirement. This will make all current `any` types visible as lint errors, driving the fixes in subsequent plans. TypeScript strict mode is already enabled, so this completes the type safety configuration.

Output: Updated biome.json with noExplicitAny set to "error" level.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-type-safety-foundation/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md

Current configuration:
@biome.json
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Biome noExplicitAny rule</name>
  <files>biome.json</files>
  <action>
Update biome.json line 92 to change noExplicitAny from "off" to "error":

BEFORE:

```json
"noExplicitAny": "off",
```

AFTER:

```json
"noExplicitAny": "error",
```

This is the only change needed. Do NOT modify any other Biome rules or configuration.

Context: This rule is under the "suspicious" section (starts around line 80).
The rule change will immediately surface all explicit `any` types as lint errors, which will be fixed in Plans 01-03 and 01-04.
</action>
<verify>
Run Biome lint to see noExplicitAny errors: npm run lint
Check biome.json has the updated rule: grep -A 1 "noExplicitAny" biome.json
Expected output shows "error" instead of "off"
</verify>
<done>
biome.json has noExplicitAny set to "error" and npm run lint shows any type violations
</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript strict mode configuration</name>
  <files>tsconfig.json</files>
  <action>
Verify (read-only check, no changes needed) that tsconfig.json has:
- "strict": true (line 6)
- All strict flags enabled by default

Add a comment in tsconfig.json documenting that strict mode is enabled:

After line 6 ("strict": true,), add:

```json
// Strict mode enabled - includes: strictNullChecks, strictFunctionTypes,
// strictBindCallApply, strictPropertyInitialization, noImplicitAny,
// noImplicitThis, alwaysStrict, useUnknownInCatchVariables
```

This documents the TYPE-05 requirement (strict TypeScript checks) for future developers.
</action>
<verify>
Confirm tsconfig.json shows "strict": true with documentation comment
Run TypeScript compiler to verify strict mode active: npx tsc --noEmit
</verify>
<done>
tsconfig.json has "strict": true with inline documentation comment explaining enabled strict flags
</done>
</task>

</tasks>

<verification>
Run these checks to verify the plan is complete:

```bash
# Verify Biome rule change
grep "noExplicitAny" biome.json

# Run lint to see any type errors surfaced
npm run lint 2>&1 | head -20

# Verify TypeScript strict mode
grep -A 3 '"strict"' tsconfig.json

# Run TypeScript compiler
npx tsc --noEmit
```

Expected: Biome shows "noExplicitAny": "error", lint shows errors in use-converter.ts, TypeScript strict mode confirmed.
</verification>

<success_criteria>

- [ ] biome.json has "noExplicitAny": "error" (changed from "off")
- [ ] npm run lint surfaces explicit any type errors
- [ ] tsconfig.json retains "strict": true
- [ ] tsconfig.json has comment documenting strict mode flags
- [ ] npx tsc --noEmit runs (may show errors, that's expected)
- [ ] TYPE-01 requirement satisfied (noExplicitAny enabled)
- [ ] TYPE-05 requirement documented (strict mode confirmed)
      </success_criteria>

<output>
After completion, create `.planning/phases/01-type-safety-foundation/01-02-SUMMARY.md`
</output>
