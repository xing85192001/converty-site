---
phase: 01-type-safety-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - Developer runs npx tsc --noEmit and sees zero errors
    - Developer runs npm run lint and sees zero noExplicitAny errors
    - All TYPE-01 through TYPE-06 requirements verified complete
  artifacts:
    - path: biome.json
      provides: Enforces noExplicitAny at error level
      contains: '"noExplicitAny": "error"'
    - path: src/lib/utils/url-params.ts
      provides: Type-safe URL parsing helpers
      exports: ["parseNumberParam", "parseStringParam", "parseBooleanParam"]
    - path: src/hooks/use-converter.ts
      provides: Generic hook without any types
      contains: "R = unknown"
    - path: src/stores/calculator-store.ts
      provides: Store with safe URL parsing
      contains: "parseNumberParam"
  key_links:
    - from: TypeScript compiler
      to: all source files
      via: strict mode type checking
      pattern: "tsc --noEmit"
    - from: Biome linter
      to: all source files
      via: noExplicitAny rule enforcement
      pattern: "npm run lint"
---

<objective>
Verify type safety foundation is complete with zero TypeScript errors and zero Biome noExplicitAny violations.

Purpose: Validate that all TYPE-01 through TYPE-06 requirements are satisfied. This checkpoint ensures the phase goal is achieved: strict TypeScript enabled with zero any types throughout the codebase critical paths (hooks, stores, URL parsing).

Output: Verified type safety with documented test results.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-type-safety-foundation/01-RESEARCH.md

Completed work from prior plans:
@.planning/phases/01-type-safety-foundation/01-01-SUMMARY.md
@.planning/phases/01-type-safety-foundation/01-02-SUMMARY.md
@.planning/phases/01-type-safety-foundation/01-03-SUMMARY.md

Phase success criteria from ROADMAP.md:

1. Developer runs `npx tsc --noEmit` and sees zero compiler errors
2. Biome configuration has `"noExplicitAny": "error"` and `npm run lint` passes
3. URL parameter parsing uses validated functions (e.g., `parseNumberParam(value, fallback)`) with no unsafe coercion
4. All files in `src/hooks/`, `src/stores/`, and URL parsing have explicit types (no `any`)
5. TypeScript strict mode enabled in tsconfig.json with all strict flags
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Run comprehensive type checks</name>
  <files>N/A</files>
  <action>
Run all type safety verification commands and capture output:

1. **TypeScript compiler check**:

```bash
npx tsc --noEmit
```

Expected: Zero errors. If errors exist, they should NOT be in:

- src/hooks/use-converter.ts
- src/hooks/use-url-state.ts
- src/stores/calculator-store.ts
- src/lib/utils/url-params.ts

Any errors in calculator components are acceptable for this phase (they may use the old patterns and will migrate in Phase 3).

1. **Biome lint check**:

```bash
npm run lint
```

Expected: Zero noExplicitAny errors in state management files (hooks, stores).
Other lint errors are acceptable.

1. **Verify noExplicitAny rule**:

```bash
grep "noExplicitAny" biome.json
```

Expected: Shows "noExplicitAny": "error"

1. **Check for any types in critical files**:

```bash
grep -n ": any" src/hooks/*.ts src/stores/*.ts
grep -n "= any" src/hooks/*.ts src/stores/*.ts
```

Expected: Zero matches in use-converter.ts, calculator-store.ts, use-url-state.ts

1. **Verify URL helper usage**:

```bash
grep -l "parseNumberParam\|parseStringParam" src/hooks/*.ts src/stores/*.ts
```

Expected: Shows calculator-store.ts and use-url-state.ts

Document all results in task completion notes.
</action>
<verify>
TypeScript compilation passes for critical files
Biome lint shows noExplicitAny is enforced
Zero `any` types in hooks and stores
URL helpers imported and used
</verify>
<done>
All verification commands run successfully with documented results confirming type safety foundation is complete
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Type safety foundation including:
- URL parameter parsing helpers (parseNumberParam, parseStringParam, parseBooleanParam)
- Biome noExplicitAny rule enabled at error level
- useConverter hook with proper generic constraints (R = unknown)
- calculator-store.ts and use-url-state.ts using type-safe URL parsing
- TypeScript strict mode confirmed active
  </what-built>
  <how-to-verify>
1. **Verify TypeScript compilation**:
   ```bash
   npx tsc --noEmit
   ```
   Expected: Zero errors in src/hooks/, src/stores/, src/lib/utils/url-params.ts

1. **Verify Biome lint enforcement**:

   ```bash
   npm run lint 2>&1 | grep -i "noExplicitAny"
   ```

   Expected: No noExplicitAny errors in use-converter.ts, calculator-store.ts

2. **Spot-check URL helper usage**:

   ```bash
   grep -A 5 "parseNumberParam" src/stores/calculator-store.ts
   ```

   Expected: Shows import and usage of URL parsing helpers

3. **Verify requirements mapping**:

   - TYPE-01: Biome noExplicitAny enabled ✓
   - TYPE-02: useConverter hook fixed ✓
   - TYPE-03: calculator stores fixed ✓
   - TYPE-04: URL parsing fixed ✓
   - TYPE-05: TypeScript strict mode enabled ✓
   - TYPE-06: Type-safe URL helpers created ✓

4. **Test calculator behavior** (optional manual check):
   - Run dev server: `npm run dev`
   - Open any calculator (e.g., <http://localhost:3000/en/health/bmi>)
   - Enter values and check URL updates
   - Verify URL parameters parse correctly on page refresh
   - Expected: No console errors, URL state works as before

If all checks pass, type "approved" to continue.
If issues found, describe what failed for investigation.
</how-to-verify>
<resume-signal>Type "approved" to confirm type safety foundation complete, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
This plan is complete when:

```bash
# All verification commands pass
npx tsc --noEmit  # Zero errors in critical files
npm run lint      # No noExplicitAny errors in hooks/stores
grep ": any\|= any" src/hooks/*.ts src/stores/*.ts  # Zero matches

# All requirements verified
grep "noExplicitAny.*error" biome.json  # TYPE-01 ✓
grep "R = unknown" src/hooks/use-converter.ts  # TYPE-02 ✓
grep "parseNumberParam" src/stores/calculator-store.ts  # TYPE-03, TYPE-04 ✓
grep "strict.*true" tsconfig.json  # TYPE-05 ✓
ls src/lib/utils/url-params.ts  # TYPE-06 ✓
```

Human approval confirms functional testing passes.
</verification>

<success_criteria>

- [ ] npx tsc --noEmit shows zero errors in hooks, stores, url-params.ts
- [ ] npm run lint shows zero noExplicitAny errors in state management files
- [ ] biome.json has "noExplicitAny": "error"
- [ ] src/hooks/use-converter.ts has R = unknown (not R = any)
- [ ] src/stores/calculator-store.ts uses parseNumberParam/parseStringParam
- [ ] src/hooks/use-url-state.ts uses URL parsing helpers
- [ ] src/lib/utils/url-params.ts exists and exports all three helpers
- [ ] tsconfig.json has "strict": true with documentation comment
- [ ] Calculator URL state works correctly (functional test)
- [ ] All TYPE-01 through TYPE-06 requirements verified complete
- [ ] Human approval received
      </success_criteria>

<output>
After completion, create `.planning/phases/01-type-safety-foundation/01-04-SUMMARY.md`

Summary should include:

- Verification test results (pass/fail for each check)
- Any remaining type errors (with file locations and plan for resolution)
- Confirmation that TYPE-01 through TYPE-06 are satisfied
- Notes on any edge cases discovered during testing
  </output>
