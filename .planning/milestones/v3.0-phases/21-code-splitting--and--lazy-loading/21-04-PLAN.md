---
phase: 21-code-splitting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/crypto/hash/page.tsx
  - src/app/[locale]/crypto/wallet-validator/page.tsx
  - src/app/[locale]/crypto/exchange-rate/page.tsx
  - src/app/[locale]/crypto/mining/page.tsx
  - src/app/[locale]/cooking/food-cost/page.tsx
  - src/app/[locale]/cooking/recipe-scaler/page.tsx
  - src/app/[locale]/cooking/nutrition-calculator/page.tsx
  - src/app/[locale]/cooking/cooking-units/page.tsx
  - src/app/[locale]/automotive/fuel-efficiency/page.tsx
  - src/app/[locale]/automotive/tire-sizing/page.tsx
  - src/app/[locale]/automotive/maintenance-intervals/page.tsx
  - src/app/[locale]/automotive/vehicle-financing/page.tsx
  - src/app/[locale]/realestate/mortgage-swiss/page.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "CalculatorSkeleton shows during component loading in Suspense fallback"
    - "All calculator pages have consistent loading UX"
    - "No <div>Loading...</div> fallbacks remain in calculator pages"
  artifacts:
    - path: "src/app/[locale]/crypto/hash/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/crypto/wallet-validator/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/crypto/exchange-rate/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/crypto/mining/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/cooking/food-cost/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/cooking/recipe-scaler/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/cooking/nutrition-calculator/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/cooking/cooking-units/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/automotive/fuel-efficiency/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/automotive/tire-sizing/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/automotive/maintenance-intervals/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/automotive/vehicle-financing/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
    - path: "src/app/[locale]/realestate/mortgage-swiss/page.tsx"
      provides: "Consistent Suspense fallback"
      contains: "<Suspense fallback={<CalculatorSkeleton"
  key_links:
    - from: "Suspense"
      to: "CalculatorSkeleton"
      via: "fallback prop"
      pattern: "<Suspense fallback=\\{<CalculatorSkeleton"
---

<objective>
Fix CalculatorSkeleton consistency gap in 13 calculator pages.

Purpose: Ensure consistent loading UX across all calculators by replacing plain `<div>Loading...</div>` fallbacks with the reusable `<CalculatorSkeleton />` component.

Output: All 13 affected calculator pages updated to use CalculatorSkeleton in Suspense fallback.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-code-splitting--and--lazy-loading/21-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Suspense fallbacks in all 13 calculator pages</name>
  <files>
    src/app/[locale]/crypto/hash/page.tsx
    src/app/[locale]/crypto/wallet-validator/page.tsx
    src/app/[locale]/crypto/exchange-rate/page.tsx
    src/app/[locale]/crypto/mining/page.tsx
    src/app/[locale]/cooking/food-cost/page.tsx
    src/app/[locale]/cooking/recipe-scaler/page.tsx
    src/app/[locale]/cooking/nutrition-calculator/page.tsx
    src/app/[locale]/cooking/cooking-units/page.tsx
    src/app/[locale]/automotive/fuel-efficiency/page.tsx
    src/app/[locale]/automotive/tire-sizing/page.tsx
    src/app/[locale]/automotive/maintenance-intervals/page.tsx
    src/app/[locale]/automotive/vehicle-financing/page.tsx
    src/app/[locale]/realestate/mortgage-swiss/page.tsx
  </files>
  <action>
    In each of the 13 files listed above:

    1. Find the line containing: `<Suspense fallback={<div>Loading...</div>}>`
    2. Replace with: `<Suspense fallback={<CalculatorSkeleton />}>`

    Note: Each file already has `CalculatorSkeleton` imported (line 5 in most files).
    No import changes needed - the component is already imported but not used in Suspense.

    Pattern to find (exact):
    ```tsx
    <Suspense fallback={<div>Loading...</div>}>
    ```

    Replace with:
    ```tsx
    <Suspense fallback={<CalculatorSkeleton />}>
    ```

    Line numbers from VERIFICATION.md:
    - crypto/hash/page.tsx: line 52
    - crypto/wallet-validator/page.tsx: line 52
    - crypto/exchange-rate/page.tsx: line 77
    - crypto/mining/page.tsx: line 72
    - cooking/food-cost/page.tsx: line 48
    - cooking/recipe-scaler/page.tsx: line 52
    - cooking/nutrition-calculator/page.tsx: line 52
    - cooking/cooking-units/page.tsx: line 52
    - automotive/fuel-efficiency/page.tsx: line 52
    - automotive/tire-sizing/page.tsx: line 52
    - automotive/maintenance-intervals/page.tsx: line 52
    - automotive/vehicle-financing/page.tsx: line 52
    - realestate/mortgage-swiss/page.tsx: line 48
  </action>
  <verify>
    Run the following to confirm no `<div>Loading...</div>` fallbacks remain:

    ```bash
    grep -r "fallback={<div>Loading" src/app/[locale]/crypto src/app/[locale]/cooking src/app/[locale]/automotive src/app/[locale]/realestate
    ```

    Expected: No output (grep returns nothing)

    Then verify CalculatorSkeleton is used in all Suspense fallbacks:

    ```bash
    grep -r "fallback={<CalculatorSkeleton" src/app/[locale]/crypto src/app/[locale]/cooking src/app/[locale]/automotive src/app/[locale]/realestate | wc -l
    ```

    Expected: 13 matches (all affected files now use CalculatorSkeleton)
  </verify>
  <done>
    All 13 calculator pages use `<CalculatorSkeleton />` in their Suspense fallback.
    No `<div>Loading...</div>` fallbacks remain in the affected categories.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build succeeds and no TypeScript errors</name>
  <files>None (verification only)</files>
  <action>
    Run TypeScript check to ensure no type errors introduced:

    ```bash
    npm run type-check
    ```

    Run linting to ensure code style is consistent:

    ```bash
    npm run check
    ```

    Both should pass with no errors.
  </action>
  <verify>
    ```bash
    npm run type-check && npm run check
    ```

    Expected: Both commands exit with code 0
  </verify>
  <done>
    TypeScript compilation succeeds with no errors.
    Biome linting passes with no errors.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Consistency check**: Verify all 168 calculator pages now use CalculatorSkeleton in Suspense:
   ```bash
   grep -r "fallback={<CalculatorSkeleton" src/app/[locale] --include="page.tsx" | wc -l
   ```
   Expected: 168 (all calculator pages)

2. **No remaining div fallbacks**: Verify no calculator pages use div fallback:
   ```bash
   grep -r "fallback={<div>Loading" src/app/[locale] --include="page.tsx"
   ```
   Expected: No output

3. **Build test** (optional): Run `npm run build` to confirm static export works
</verification>

<success_criteria>
- All 13 affected files updated with CalculatorSkeleton in Suspense fallback
- Zero `<div>Loading...</div>` fallbacks in calculator pages
- TypeScript check passes
- Linting passes
- Phase 21 must-have "CalculatorSkeleton shows during component loading" fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/21-code-splitting--and--lazy-loading/21-04-SUMMARY.md`
</output>
