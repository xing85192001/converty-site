---
phase: 21-code-splitting
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - src/components/search/global-search.tsx
  - .planning/phases/21-code-splitting--and--lazy-loading/performance-report.md
autonomous: false

must_haves:
  truths:
    - "Search results render in < 100ms for full calculator set"
    - "First Contentful Paint improved compared to baseline"
    - "Code-split bundles are cached by service worker"
    - "All PERF requirements (PERF-01 through PERF-04) are satisfied"
  artifacts:
    - path: ".planning/phases/21-code-splitting--and--lazy-loading/performance-report.md"
      provides: "Final performance metrics and comparison"
      contains: "First Load JS"
  key_links:
    - from: "global-search.tsx"
      to: "CommandList"
      via: "results rendering"
      pattern: "results.map"
---

<objective>
Verify search performance, audit service worker caching, and confirm all performance requirements are met.

Purpose: Ensure code splitting delivers measurable performance improvements without breaking existing functionality.
Output: Performance report documenting improvements, search performance verified, all PERF requirements satisfied.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-code-splitting--and--lazy-loading/21-RESEARCH.md
@.planning/phases/21-code-splitting--and--lazy-loading/21-01-SUMMARY.md
@.planning/phases/21-code-splitting--and--lazy-loading/21-02-SUMMARY.md
@.planning/phases/21-code-splitting--and--lazy-loading/bundle-baseline.md
@src/components/search/global-search.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Search Performance Audit</name>
  <files>
    src/components/search/global-search.tsx
    .planning/phases/21-code-splitting--and--lazy-loading/search-performance.md
  </files>
  <action>
1. Audit current search performance:
   - Start dev server: `npm run dev`
   - Open browser DevTools Performance tab
   - Trigger search (Cmd+K), type query that returns many results
   - Measure time from keypress to results rendered
   - Document in search-performance.md

1. If search results render in < 100ms:
   - Document "Search performance acceptable, virtualization not needed"
   - Note current implementation handles 166+ calculators efficiently
   - PERF-03 satisfied without additional work

2. If search results > 100ms (unlikely based on research):
   - Install react-window: `npm install react-window @types/react-window`
   - Update global-search.tsx to use FixedSizeList for CommandList:

     ```typescript
     import { FixedSizeList } from 'react-window'

     // Wrap results in virtualized list
     <FixedSizeList
       height={400}
       itemCount={results.length}
       itemSize={56}
       width="100%"
     >
       {({ index, style }) => (
         <CommandItem key={results[index].id} style={style}>
           {/* render result */}
         </CommandItem>
       )}
     </FixedSizeList>
     ```

3. Run final build and verify search still works: `npm run build`
  </action>

  <verify>
    - Search performance documented in search-performance.md
    - Search results render in < 100ms (measured)
    - If virtualization added: results still navigable with keyboard
    - `npm run build` succeeds
  </verify>
  <done>
    Search performance verified at < 100ms for full result set. PERF-03 requirement satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Service Worker and Final Performance Report</name>
  <files>
    scripts/generate-sw.js (verify, may not need changes)
    .planning/phases/21-code-splitting--and--lazy-loading/performance-report.md
  </files>
  <action>
1. Verify service worker caches code-split chunks:
   - Build project: `npm run build`
   - Check if scripts/generate-sw.js includes chunk patterns
   - If chunks not cached: Update glob patterns to include `.next/static/chunks/*.js`
   - The current Workbox config should handle this automatically via CacheFirst for static assets

1. Create comprehensive performance-report.md:

   ```markdown
   # Phase 21 Performance Report

   ## Bundle Size Comparison

   | Route | Before | After | Improvement |
   |-------|--------|-------|-------------|
   | Homepage | X KB | Y KB | Z% |
   | health/bmi | X KB | Y KB | Z% |
   | finance/mortgage | X KB | Y KB | Z% |

   ## First Load JS

   - Before: X KB (from bundle-baseline.md)
   - After: Y KB
   - Reduction: Z%

   ## Code Splitting Results

   - Total chunks created: N
   - Largest chunk: X KB
   - Average chunk size: Y KB

   ## Requirements Verification

   | Requirement | Status | Evidence |
   |-------------|--------|----------|
   | PERF-01 | ✅ | All 166 calculators use dynamic imports |
   | PERF-02 | ✅ | First Load JS reduced by X% |
   | PERF-03 | ✅ | Search renders in <100ms |
   | PERF-04 | ✅ | FCP improved (verified in checkpoint) |

   ## Service Worker Caching

   - Chunks cached: Yes/No
   - Cache strategy: CacheFirst for static assets
   ```

2. Run final bundle analysis: `ANALYZE=true npm run build`
   - Screenshot or note key improvements
   - Compare to bundle-baseline.md
  </action>

  <verify>
    - performance-report.md contains before/after comparison
    - All PERF requirements documented as satisfied
    - Service worker caching verified or updated
    - `npm run build` succeeds
  </verify>
  <done>
    Performance report complete with measurable improvements documented. Service worker caching verified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Code splitting implementation with:
    - 166 calculator pages using dynamic imports
    - CalculatorSkeleton loading states
    - Reduced bundle size
    - Search performance maintained
  </what-built>
  <how-to-verify>
    1. Start production build locally:
       ```bash
       npm run build && npx serve out
       ```

    2. Open browser DevTools → Network tab → Disable cache

    3. Navigate to homepage:
       - Note First Contentful Paint time
       - Should be faster than before optimization

    4. Navigate to a calculator (e.g., /en/health/bmi):
       - Should see CalculatorSkeleton briefly
       - Then calculator loads
       - Check Network tab: calculator chunk loaded separately

    5. Test search (Cmd+K):
       - Type "calc" to get many results
       - Results should appear instantly (<100ms perceived)
       - Navigate to a result - should work correctly

    6. Test URL state:
       - On calculator, change a value
       - Copy URL with query params
       - Open in new tab - values should persist

    7. Run Lighthouse audit:
       - FCP should be improved
       - No significant regression in other metrics
  </how-to-verify>
  <resume-signal>
    Type "approved" if all verifications pass.
    Or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
After completing all tasks:
1. Search renders results in < 100ms
2. Performance report shows measurable improvement
3. All PERF-01 through PERF-04 requirements satisfied
4. Human verification confirms FCP improvement and working functionality
</verification>

<success_criteria>

- PERF-01: ✅ All calculator components code-split and lazy-loaded by category
- PERF-02: ✅ Initial bundle size reduced (documented percentage)
- PERF-03: ✅ Search results load instantly (< 100ms for 200+ calculators)
- PERF-04: ✅ First Contentful Paint improved through lazy loading
- Static export still working
- URL state persistence preserved
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/21-code-splitting--and--lazy-loading/21-03-SUMMARY.md`
</output>
