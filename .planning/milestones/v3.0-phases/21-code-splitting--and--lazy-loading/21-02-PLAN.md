---
phase: 21-code-splitting
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/app/[locale]/health/*/page.tsx
  - src/app/[locale]/finance/*/page.tsx
  - src/app/[locale]/math/*/page.tsx
  - src/app/[locale]/photo/*/page.tsx
  - src/app/[locale]/network/*/page.tsx
  - src/app/[locale]/crypto/*/page.tsx
  - src/app/[locale]/real-estate/*/page.tsx
  - src/app/[locale]/cooking/*/page.tsx
  - src/app/[locale]/automotive/*/page.tsx
autonomous: true

must_haves:
  truths:
    - "Calculator pages use next/dynamic for lazy loading components"
    - "CalculatorSkeleton shows during component loading"
    - "Static export still works (npm run build succeeds)"
    - "URL state persistence works on lazy-loaded calculators"
  artifacts:
    - path: "src/app/[locale]/health/bmi/page.tsx"
      provides: "Example of dynamic import pattern"
      contains: "next/dynamic"
    - path: "src/app/[locale]/finance/mortgage/page.tsx"
      provides: "Example of dynamic import pattern"
      contains: "next/dynamic"
  key_links:
    - from: "page.tsx"
      to: "*-calculator.tsx"
      via: "next/dynamic import"
      pattern: "dynamic\\(.*import\\("
    - from: "Suspense"
      to: "CalculatorSkeleton"
      via: "fallback prop"
      pattern: "fallback=\\{<CalculatorSkeleton"
---

<objective>
Convert calculator pages to use next/dynamic for lazy loading, reducing initial bundle size.

Purpose: Implement category-based code splitting to defer non-critical calculator bundles.
Output: All calculator pages use dynamic imports with CalculatorSkeleton fallback.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-code-splitting--and--lazy-loading/21-RESEARCH.md
@.planning/phases/21-code-splitting--and--lazy-loading/21-01-SUMMARY.md
@src/components/calculator-skeleton.tsx
@src/app/[locale]/health/bmi/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert High-Impact Category Calculator Pages to Dynamic Imports</name>
  <files>
    src/app/[locale]/health/*/page.tsx (28 calculators)
    src/app/[locale]/finance/*/page.tsx (24 calculators)
    src/app/[locale]/math/*/page.tsx (38 calculators)
    src/app/[locale]/photo/*/page.tsx (22 calculators)
  </files>
  <action>
For each calculator page.tsx in high-impact categories (health, finance, math, photo - 112 calculators total):

1. Change static import to dynamic import:

   BEFORE:
   ```typescript
   import { BMICalculator } from "./bmi-calculator";
   ```

   AFTER:
   ```typescript
   import dynamic from "next/dynamic";
   import { CalculatorSkeleton } from "@/components/calculator-skeleton";

   const BMICalculator = dynamic(
     () => import("./bmi-calculator").then((mod) => mod.BMICalculator),
     {
       loading: () => <CalculatorSkeleton />,
     }
   );
   ```

2. Keep existing Suspense boundary but update fallback to use CalculatorSkeleton:
   ```typescript
   <Suspense fallback={<CalculatorSkeleton />}>
     <BMICalculator />
   </Suspense>
   ```

3. Pattern for conversion (use find/sed or multi-cursor editing):
   - Find: `import { ([A-Za-z]+) } from "./([a-z-]+)";`
   - Replace with dynamic import pattern

4. Categories to convert in priority order:
   - math/ (38 calculators - highest impact)
   - health/ (28 calculators)
   - finance/ (24 calculators)
   - photo/ (22 calculators)

5. After each category, verify build succeeds: `npm run build`

Note: Do NOT add `ssr: false` - calculators should render on server for SEO. Dynamic imports still work with static export through client-side hydration.
  </action>
  <verify>
    - All 112 page.tsx files in high-impact categories use `dynamic` from "next/dynamic"
    - All dynamic imports use CalculatorSkeleton as loading fallback
    - `npm run build` succeeds (static export works)
    - No TypeScript errors: `npm run type-check`
    - Spot check: Open health/bmi in dev mode, see skeleton then calculator
  </verify>
  <done>
    112 calculator pages in high-impact categories converted to dynamic imports with CalculatorSkeleton fallback.
  </done>
</task>

<task type="auto">
  <name>Task 2: Convert Remaining Category Calculator Pages</name>
  <files>
    src/app/[locale]/network/*/page.tsx (5 calculators)
    src/app/[locale]/crypto/*/page.tsx (4 calculators)
    src/app/[locale]/real-estate/*/page.tsx (4 calculators)
    src/app/[locale]/cooking/*/page.tsx (4 calculators)
    src/app/[locale]/automotive/*/page.tsx (4 calculators)
    src/app/[locale]/datetime/*/page.tsx (8 calculators)
    src/app/[locale]/video/*/page.tsx (9 calculators)
    src/app/[locale]/web/*/page.tsx (10 calculators)
    src/app/[locale]/data/*/page.tsx (3 calculators)
    src/app/[locale]/physics/*/page.tsx (1 calculator)
    src/app/[locale]/music/*/page.tsx (1 calculator)
    src/app/[locale]/color/*/page.tsx (1 calculator)
  </files>
  <action>
Apply the same dynamic import pattern to remaining categories (~54 calculators):

1. Convert each category following the pattern from Task 1:
   - network/ (5)
   - crypto/ (4)
   - real-estate/ (4)
   - cooking/ (4)
   - automotive/ (4)
   - datetime/ (8)
   - video/ (9)
   - web/ (10)
   - data/ (3)
   - physics/ (1)
   - music/ (1)
   - color/ (1)

2. For very small categories (1-3 calculators), still use dynamic imports for consistency and future scalability.

3. After all conversions, run full build: `npm run build`

4. Run bundle analysis to see improvement: `ANALYZE=true npm run build`

5. Document improvement in bundle-improvement.md:
   - Compare First Load JS before/after for key routes
   - Note number of chunks created
   - Identify any unexpected large bundles
  </action>
  <verify>
    - All remaining calculator pages use dynamic imports
    - `npm run build` succeeds
    - `ANALYZE=true npm run build` shows split bundles
    - Total: ~166 calculator pages converted to dynamic imports
  </verify>
  <done>
    All 166 calculator pages converted to dynamic imports. Bundle analysis shows code splitting in effect.
  </done>
</task>

</tasks>

<verification>
After completing all tasks:
1. `npm run build` succeeds with no errors
2. Bundle analyzer shows separate chunks per category
3. First Load JS reduced compared to baseline (check bundle-baseline.md)
4. Dev server shows skeleton then calculator on navigation
5. URL state works: add ?amount=100 to calculator URL, value persists
</verification>

<success_criteria>
- All ~166 calculator pages use next/dynamic imports
- CalculatorSkeleton appears during lazy load
- Static export preserved (no server-side features)
- First Load JS reduced by measurable amount (target: 20-40% reduction)
- URL state persistence still working
</success_criteria>

<output>
After completion, create `.planning/phases/21-code-splitting--and--lazy-loading/21-02-SUMMARY.md`
</output>
