---
phase: 24-export-functionality
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/components/converter/pdf-export-button.tsx
  - src/components/converter/csv-export-button.tsx
  - src/components/converter/index.ts
autonomous: true

must_haves:
  truths:
    - "PdfExportButton uses translated text instead of hardcoded 'Export PDF'"
    - "CsvExportButton exists and triggers CSV downloads"
    - "Both buttons generate timestamped filenames"
    - "Both components are exported from converter/index.ts"
  artifacts:
    - path: "src/components/converter/pdf-export-button.tsx"
      provides: "i18n-enabled PDF export button"
      exports: ["PdfExportButton"]
    - path: "src/components/converter/csv-export-button.tsx"
      provides: "CSV export button component"
      exports: ["CsvExportButton"]
    - path: "src/components/converter/index.ts"
      provides: "barrel exports for converter components"
      exports: ["CsvExportButton"]
  key_links:
    - from: "src/components/converter/pdf-export-button.tsx"
      to: "src/lib/utils/pdf-export.ts"
      via: "import exportToPdf"
      pattern: "import.*exportToPdf"
    - from: "src/components/converter/csv-export-button.tsx"
      to: "src/lib/utils/csv-export.ts"
      via: "import exportToCsv"
      pattern: "import.*exportToCsv"
    - from: "src/components/converter/pdf-export-button.tsx"
      to: "next-intl"
      via: "useTranslations hook"
      pattern: "useTranslations.*calculator.export"
---

<objective>
Update PDF export button for i18n support and create CSV export button component.

Purpose: Transform the existing hardcoded PdfExportButton into an internationalized component and create a matching CsvExportButton for CSV exports. Both buttons will use timestamped filenames for better file management.

Output:
- Updated `pdf-export-button.tsx` with i18n support and timestamped filenames
- New `csv-export-button.tsx` component with matching API
- Updated `index.ts` barrel exports
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-export-functionality/24-RESEARCH.md
@.planning/phases/24-export-functionality/24-01-SUMMARY.md

Reference files:
@src/components/converter/pdf-export-button.tsx (current implementation)
@src/lib/utils/csv-export.ts (created in 24-01)
@src/lib/utils/pdf-export.ts (PDF export utility)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PdfExportButton for i18n</name>
  <files>src/components/converter/pdf-export-button.tsx</files>
  <action>
Update `src/components/converter/pdf-export-button.tsx` to:

1. **Add i18n support:**
   - Import `useTranslations` from `next-intl`
   - Create translation hook: `const t = useTranslations("calculator.export");`
   - Replace hardcoded "Export PDF" with `{t("exportPdf")}`
   - Add `title={t("pdfTooltip")}` to Button for tooltip

2. **Add timestamped filename generation:**
   - In `handleExport()`, generate timestamp:
     ```typescript
     const timestamp = new Date()
       .toISOString()
       .slice(0, 19)
       .replace('T', '_')
       .replace(/:/g, '-');
     ```
   - Compute filename: `${baseFilename}_${timestamp}.pdf`
   - Base filename from `options.filename` or sanitized `options.title`

3. **Add disabled state:**
   - Add `disabled?: boolean` prop (default false)
   - Disable button when `disabled || sections.length === 0`

4. **Use FileText icon** (currently uses Download, change to FileText for PDF):
   - Import `FileText` from `lucide-react` instead of `Download`
   - Use `<FileText className="h-4 w-4 mr-2" />`

Keep the existing props interface but add `disabled?: boolean`.
  </action>
  <verify>
TypeScript compiles:
```bash
npx tsc --noEmit src/components/converter/pdf-export-button.tsx
```

i18n hook present:
```bash
grep "useTranslations" src/components/converter/pdf-export-button.tsx
```
  </verify>
  <done>
- `useTranslations("calculator.export")` hook is used
- Button text uses `{t("exportPdf")}` instead of hardcoded string
- Button has `title={t("pdfTooltip")}` for tooltip
- Filename includes timestamp (format: `name_YYYY-MM-DD_HH-MM-SS.pdf`)
- Uses FileText icon from lucide-react
- Has `disabled` prop support
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CsvExportButton component</name>
  <files>src/components/converter/csv-export-button.tsx</files>
  <action>
Create `src/components/converter/csv-export-button.tsx` with:

1. **"use client" directive** at top

2. **Imports:**
   - `Download` from `lucide-react`
   - `useTranslations` from `next-intl`
   - `Button` from `@/components/ui/button`
   - `exportToCsv`, `type CsvRow` from `@/lib/utils/csv-export`

3. **Interface `CsvExportButtonProps`:**
   ```typescript
   interface CsvExportButtonProps {
     data: CsvRow[];
     filename: string;          // Base filename (without extension)
     variant?: "default" | "outline" | "ghost" | "secondary";
     size?: "default" | "sm" | "lg" | "icon";
     className?: string;
     disabled?: boolean;
   }
   ```

4. **Component implementation:**
   - Use `useTranslations("calculator.export")` hook
   - Generate timestamp in same format as PdfExportButton
   - Compute full filename: `${filename}_${timestamp}.csv`
   - Call `exportToCsv(data, { filename: fullFilename })`
   - Disable when `disabled || data.length === 0`

5. **Return Button with:**
   - Download icon (`<Download className="h-4 w-4 mr-2" />`)
   - Text: `{t("exportCsv")}`
   - `title={t("csvTooltip")}`
   - Props: variant, size, onClick, disabled, className, title

Export the component as named export.
  </action>
  <verify>
TypeScript compiles:
```bash
npx tsc --noEmit src/components/converter/csv-export-button.tsx
```

File exists with correct structure:
```bash
grep "CsvExportButton" src/components/converter/csv-export-button.tsx
```
  </verify>
  <done>
- File exists at `src/components/converter/csv-export-button.tsx`
- Has "use client" directive
- Exports `CsvExportButton` component
- Uses `useTranslations("calculator.export")` hook
- Uses `exportToCsv` from csv-export utility
- Generates timestamped filenames
- Has disabled state when no data
  </done>
</task>

<task type="auto">
  <name>Task 3: Update converter barrel exports</name>
  <files>src/components/converter/index.ts</files>
  <action>
Update `src/components/converter/index.ts` to export the new CsvExportButton:

1. Add export line: `export { CsvExportButton } from "./csv-export-button";`

2. Ensure existing exports are preserved:
   - `PdfExportButton` (already there)
   - `InputField`
   - `OutputDisplay`
   - `ResultGrid`
   - `ConverterLayout`
   - Any other existing exports

Place new export in alphabetical order with other exports.
  </action>
  <verify>
Export exists:
```bash
grep "CsvExportButton" src/components/converter/index.ts
```

All exports work:
```bash
npx tsc --noEmit
```
  </verify>
  <done>
- `CsvExportButton` is exported from `src/components/converter/index.ts`
- All existing exports still work
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. Full build compiles: `npm run type-check`
2. Biome passes: `npm run check`
3. i18n usage verified: `grep -r "useTranslations.*calculator.export" src/components/converter/`
4. Exports verified: `grep "CsvExportButton" src/components/converter/index.ts`
</verification>

<success_criteria>
- PdfExportButton uses translated text and has timestamped filenames
- CsvExportButton exists with matching API to PdfExportButton
- Both components use `useTranslations("calculator.export")` hook
- Both components are exported from converter/index.ts
- TypeScript and Biome pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-export-functionality/24-02-SUMMARY.md`
</output>
