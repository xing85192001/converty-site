---
phase: 09-visual-subnet-foundation
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/lib/converters/network/types.ts
  - src/lib/converters/network/ip-parser.ts
  - src/lib/converters/network/subnet-calculator.ts
autonomous: true

must_haves:
  truths:
    - "Pure functions can parse CIDR notation (192.168.1.0/24, 2001:db8::/32)"
    - "Pure functions can parse subnet mask notation (192.168.1.0 + 255.255.255.0)"
    - "IPv4 calculations return network address, broadcast, usable range, host count"
    - "IPv6 calculations return network address, last address, usable range (no broadcast)"
    - "Special cases handled: /31, /32 for IPv4; /128 for IPv6"
  artifacts:
    - path: "src/lib/converters/network/types.ts"
      provides: "TypeScript interfaces for subnet calculations"
      exports: ["SubnetInput", "SubnetResult", "ParsedInput"]
      min_lines: 30
    - path: "src/lib/converters/network/ip-parser.ts"
      provides: "IP parsing and validation utilities"
      exports: ["parseIPInput", "validateIPAddress"]
      min_lines: 80
    - path: "src/lib/converters/network/subnet-calculator.ts"
      provides: "Subnet calculation logic"
      exports: ["calculateSubnet"]
      min_lines: 200
  key_links:
    - from: "src/lib/converters/network/subnet-calculator.ts"
      to: "ipaddr.js"
      via: "import ipaddr"
      pattern: 'import.*ipaddr.*from.*["\']ipaddr\\.js["\']'
    - from: "src/lib/converters/network/ip-parser.ts"
      to: "ipaddr.js"
      via: "import ipaddr"
      pattern: 'import.*ipaddr.*from.*["\']ipaddr\\.js["\']'
    - from: "src/lib/converters/network/subnet-calculator.ts"
      to: "src/lib/converters/network/ip-parser.ts"
      via: "import parseIPInput"
      pattern: "import.*parseIPInput"
---

<objective>
Implement pure calculation functions for subnet calculations supporting IPv4, IPv6, CIDR notation, and subnet mask input.

Purpose: Create framework-agnostic calculation logic that handles IP address parsing, validation, and subnet calculations. This follows the project pattern of separating business logic from UI, enabling testing and reusability.

Output: Three TypeScript modules providing type definitions, IP parsing utilities, and subnet calculation functions with full IPv4/IPv6 support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-subnet-foundation/09-RESEARCH.md
@.planning/phases/09-visual-subnet-foundation/09-01-SUMMARY.md
@src/lib/converters/datetime/age.ts
@src/lib/converters/health/bmi.ts
</context>

<tasks>

<task type="auto">
  <name>Create TypeScript interfaces for subnet calculations</name>
  <files>src/lib/converters/network/types.ts</files>
  <action>
    Create type definitions following project patterns (see context: age.ts, bmi.ts).

    Export the following interfaces:

    ```typescript
    export interface SubnetInput {
      ipAddress: string;
      cidr?: number;
      subnetMask?: string;
    }

    export interface SubnetResult {
      networkAddress: string;
      broadcastAddress: string | null; // null for IPv6
      firstUsable: string;
      lastUsable: string;
      usableHosts: bigint;
      totalHosts: bigint;
      cidr: number;
      subnetMask: string | null; // null for IPv6
      ipVersion: 4 | 6;
    }

    export interface ParsedInput {
      ipAddress: string;
      cidr: number;
      format: 'cidr' | 'mask';
    }

    export interface IPValidationResult {
      valid: boolean;
      version: 4 | 6 | null;
      error?: string;
    }
    ```

    Add JSDoc comments explaining:
    - Why broadcastAddress is null for IPv6 (no broadcast, uses multicast)
    - Why subnetMask is null for IPv6 (only CIDR notation)
    - BigInt usage for host counts (IPv6 can exceed Number.MAX_SAFE_INTEGER)

    Follow project conventions: PascalCase for interfaces, descriptive names, explicit types.

  </action>
  <verify>
    grep -q "export interface SubnetResult" src/lib/converters/network/types.ts
    grep -q "broadcastAddress: string | null" src/lib/converters/network/types.ts
    grep -q "usableHosts: bigint" src/lib/converters/network/types.ts
    npx tsc --noEmit
  </verify>
  <done>Type definitions created with full IPv4/IPv6 support and BigInt for large numbers</done>
</task>

<task type="auto">
  <name>Implement IP parsing and validation utilities</name>
  <files>src/lib/converters/network/ip-parser.ts</files>
  <action>
    Create IP parsing utilities using ipaddr.js. Follow research recommendations (RESEARCH.md).

    **Import and types:**
    ```typescript
    import ipaddr from 'ipaddr.js';
    import type { ParsedInput, IPValidationResult } from './types';
    ```

    **Function 1: parseIPInput(input: string, subnetMask?: string): ParsedInput**
    - Try CIDR notation first (e.g., "192.168.1.0/24", "2001:db8::/32")
    - If slash detected, validate with ipaddr.isValidCIDR()
    - Split on slash, parse CIDR number, validate range (0-32 for IPv4, 0-128 for IPv6)
    - If no slash and subnetMask provided, convert mask to CIDR using ipaddr.IPv4.parse().prefixLengthFromSubnetMask()
    - Throw descriptive errors for invalid inputs (function can throw, store will catch)
    - Return { ipAddress, cidr, format: 'cidr' | 'mask' }

    **Function 2: validateIPAddress(ip: string): IPValidationResult**
    - Use ipaddr.isValid(ip) for validation
    - If valid, parse with ipaddr.parse(ip) and check .kind() for version
    - Return { valid: true, version: 4 | 6 } or { valid: false, version: null, error: string }

    **Error messages:**
    - "Invalid CIDR notation" for malformed CIDR
    - "CIDR must be between 0 and 32 for IPv4" (or 0-128 for IPv6)
    - "Invalid IP address format"
    - "Subnet mask notation only supported for IPv4" (if IPv6 with mask)
    - "Invalid subnet mask" for malformed subnet masks

    Follow project error handling pattern: throw errors from pure functions, let caller handle.

    See RESEARCH.md Pattern 2 for implementation details.

  </action>
  <verify>
    grep -q "export function parseIPInput" src/lib/converters/network/ip-parser.ts
    grep -q "export function validateIPAddress" src/lib/converters/network/ip-parser.ts
    grep -q "import.*ipaddr.*from.*'ipaddr.js'" src/lib/converters/network/ip-parser.ts
    npx tsc --noEmit
  </verify>
  <done>IP parsing functions handle CIDR and subnet mask notation with proper validation</done>
</task>

<task type="auto">
  <name>Implement subnet calculation functions</name>
  <files>src/lib/converters/network/subnet-calculator.ts</files>
  <action>
    Create subnet calculation logic following RESEARCH.md Pattern 1. Handle IPv4/IPv6 differences.

    **Import dependencies:**
    ```typescript
    import ipaddr from 'ipaddr.js';
    import type { SubnetResult } from './types';
    import { parseIPInput } from './ip-parser';
    ```

    **Main function: calculateSubnet(ipAddress: string, cidr: number): SubnetResult**
    - Parse IP address with ipaddr.parse()
    - Check addr.kind() to determine IPv4 vs IPv6
    - Delegate to calculateIPv4Subnet() or calculateIPv6Subnet()

    **IPv4 calculation (private function):**
    - Network address: ipaddr.IPv4.networkAddressFromCIDR(`${addr}/${cidr}`)
    - Broadcast address: ipaddr.IPv4.broadcastAddressFromCIDR(`${addr}/${cidr}`)
    - Subnet mask: ipaddr.IPv4.subnetMaskFromPrefixLength(cidr)
    - Host count: totalHosts = BigInt(2) ** BigInt(32 - cidr)

    **Special cases for IPv4:**
    - /32 (single host): usableHosts = 1, firstUsable = lastUsable = address
    - /31 (point-to-point, RFC 3021): usableHosts = 2, no network/broadcast reservation
    - Normal: usableHosts = totalHosts - 2, firstUsable = network + 1, lastUsable = broadcast - 1

    **IPv6 calculation (private function):**
    - Network address: calculate by zeroing host bits (bitwise operations on addr.parts array)
    - Last address: calculate by setting all host bits to 1
    - No broadcast address (return null)
    - No subnet mask (return null)
    - Host count: totalHosts = BigInt(2) ** BigInt(128 - cidr)
    - usableHosts = totalHosts (no network/broadcast reservation, except /128 = 1)

    **IPv6 bitwise helpers (private functions):**
    - calculateIPv6NetworkAddress(parts: number[], cidr: number): number[]
      - Zero out parts after network portion
      - Handle partial parts with bit masking
    - calculateIPv6LastAddress(parts: number[], cidr: number): number[]
      - Set all parts after network portion to 0xFFFF
      - Handle partial parts with bit masking

    Follow RESEARCH.md code examples exactly for IPv6 bitwise operations.

    **Return SubnetResult with all fields populated correctly based on IP version.**

    IMPORTANT pitfalls to avoid (from RESEARCH.md):
    - Don't apply 2^n - 2 formula to /31 and /32 subnets
    - Don't calculate broadcast for IPv6 (return null)
    - Use BigInt for all host count calculations
    - IPv6 firstUsable === networkAddress (no reservation)

  </action>
  <verify>
    grep -q "export function calculateSubnet" src/lib/converters/network/subnet-calculator.ts
    grep -q "calculateIPv4Subnet" src/lib/converters/network/subnet-calculator.ts
    grep -q "calculateIPv6Subnet" src/lib/converters/network/subnet-calculator.ts
    grep -q "BigInt(2)" src/lib/converters/network/subnet-calculator.ts
    npx tsc --noEmit
  </verify>
  <done>Subnet calculation functions handle IPv4 and IPv6 with correct special case logic</done>
</task>

</tasks>

<verification>
TypeScript compilation:
```bash
npx tsc --noEmit
```

Manual testing (after Plan 03 creates UI):

1. IPv4 /24: calculateSubnet("192.168.1.0", 24) should return 254 usable hosts
2. IPv4 /32: calculateSubnet("192.168.1.1", 32) should return 1 usable host
3. IPv4 /31: calculateSubnet("10.0.0.0", 31) should return 2 usable hosts (RFC 3021)
4. IPv6 /64: calculateSubnet("2001:db8::", 64) should return massive host count, null broadcast
5. IPv6 /128: calculateSubnet("2001:db8::1", 128) should return 1 usable host

File structure verification:

```bash
ls -la src/lib/converters/network/
# Should show: types.ts, ip-parser.ts, subnet-calculator.ts
```

</verification>

<success_criteria>

- All TypeScript interfaces exported from types.ts
- parseIPInput() handles CIDR and subnet mask notation
- validateIPAddress() correctly identifies IPv4 vs IPv6
- calculateSubnet() returns correct results for IPv4 and IPv6
- Special cases handled: /31, /32 for IPv4; /128 for IPv6
- BigInt used for host count calculations
- No TypeScript errors, all types properly defined
- Code ready for integration into Zustand store in Plan 03
  </success_criteria>

<output>
After completion, create `.planning/phases/09-visual-subnet-foundation/09-02-SUMMARY.md`
</output>
