---
phase: 09-visual-subnet-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/registry/categories.ts
  - src/lib/registry/network-converters.ts
  - src/lib/registry/converters.ts
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/de.json
  - src/messages/it.json
autonomous: true

must_haves:
  truths:
    - "ipaddr.js library is available as a dependency"
    - "Network category exists in the registry"
    - "Subnet calculator is registered with correct metadata"
    - "All 4 locales have subnet calculator translations"
  artifacts:
    - path: "package.json"
      provides: "ipaddr.js dependency"
      contains: '"ipaddr.js"'
    - path: "src/lib/registry/categories.ts"
      provides: "Network category definition"
      contains: 'id: "network"'
      min_lines: 150
    - path: "src/lib/registry/network-converters.ts"
      provides: "Network calculator registry"
      exports: ["networkConverters"]
      min_lines: 15
    - path: "src/messages/en.json"
      provides: "English translations for subnet calculator"
      contains: '"subnet-calculator"'
  key_links:
    - from: "src/lib/registry/converters.ts"
      to: "src/lib/registry/network-converters.ts"
      via: "import and spread"
      pattern: "import.*networkConverters"
    - from: "src/lib/registry/network-converters.ts"
      to: "Network icon from lucide-react"
      via: "icon property"
      pattern: "icon: Network"
---

<objective>
Set up foundation for subnet calculator: install dependencies, create network category, and add translations.

Purpose: Establish the infrastructure needed for implementing subnet calculation logic in subsequent plans. This includes the ipaddr.js library for IP address manipulation, category structure for organizing network calculators, and translations for UI labels.

Output: Network category in registry, subnet calculator metadata, and complete translations across all 4 locales (en, fr, de, it).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-visual-subnet-foundation/09-RESEARCH.md
@src/lib/registry/categories.ts
@src/lib/registry/data-converters.ts
@src/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Install ipaddr.js dependency</name>
  <files>package.json, package-lock.json</files>
  <action>
    Install ipaddr.js library version 2.2.0 or later for IPv4/IPv6 parsing and validation.

    Run: npm install ipaddr.js

    Why ipaddr.js:
    - 55M+ weekly downloads, battle-tested
    - Lightweight (1.9K minified)
    - Built-in subnet calculation methods
    - Handles IPv4/IPv6 edge cases (compression, validation, CIDR)

    This is the standard library for IP address manipulation (see RESEARCH.md).

  </action>
  <verify>
    npm list ipaddr.js shows version 2.2.0+
    grep -q '"ipaddr.js"' package.json
  </verify>
  <done>ipaddr.js dependency added to package.json and installed</done>
</task>

<task type="auto">
  <name>Create network category and register subnet calculator</name>
  <files>src/lib/registry/categories.ts, src/lib/registry/network-converters.ts, src/lib/registry/converters.ts</files>
  <action>
    Create dedicated network category and register the subnet calculator.

    **1. Add network category to categories.ts:**
    - ID: "network"
    - Slug: "network"
    - Name: "Network" (placeholder, translations will override)
    - Description: "Network calculators and tools"
    - Icon: Network (import from lucide-react)
    - Subcategories:
      - { id: "subnet", name: "Subnetting", description: "Subnet and CIDR calculators" }
      - { id: "ip", name: "IP Address", description: "IP address tools and utilities" }
    - Insert alphabetically between "music" and "photo" categories

    **2. Create src/lib/registry/network-converters.ts:**
    Follow existing pattern from data-converters.ts, health-converters.ts.

    Export networkConverters object with "subnet-calculator" entry:
    - id: "subnet-calculator"
    - slug: "subnet-calculator"
    - category: "network"
    - subcategory: "subnet"
    - keywords: ["subnet", "cidr", "ip", "ipv4", "ipv6", "network", "mask", "supernet", "calculator"]
    - icon: Network
    - featured: true (first network calculator, showcase on homepage)

    **3. Update src/lib/registry/converters.ts:**
    - Import networkConverters
    - Spread into converterRegistry object
    - Maintain alphabetical order in imports

    Match existing registry patterns exactly (see context files).

  </action>
  <verify>
    grep -q 'id: "network"' src/lib/registry/categories.ts
    grep -q 'subnet-calculator' src/lib/registry/network-converters.ts
    grep -q 'networkConverters' src/lib/registry/converters.ts
    npm run build
  </verify>
  <done>Network category exists in registry, subnet calculator registered with correct metadata</done>
</task>

<task type="auto">
  <name>Add subnet calculator translations</name>
  <files>src/messages/en.json, src/messages/fr.json, src/messages/de.json, src/messages/it.json</files>
  <action>
    Add translations for subnet calculator to all 4 locale files. Use kebab-case key matching registry ID.

    **English (en.json):**
    Add to "categories" section:
    ```json
    "network": {
      "name": "Network",
      "description": "Network calculators and IP address tools",
      "subcategories": {
        "subnet": "Subnetting",
        "ip": "IP Address"
      }
    }
    ```

    Add to "converters" section:
    ```json
    "subnet-calculator": {
      "name": "Visual Subnet Calculator",
      "description": "Calculate IPv4 and IPv6 subnet details with CIDR notation",
      "metaDescription": "Free online subnet calculator for IPv4 and IPv6 networks. Calculate network address, broadcast, usable IP range, and host count from CIDR notation or subnet mask."
    }
    ```

    Add to "calculator.labels" section:
    ```json
    "ipAddress": "IP Address",
    "ipInput": "IP Address / CIDR",
    "subnetMask": "Subnet Mask",
    "cidrNotation": "CIDR Notation",
    "networkAddress": "Network Address",
    "broadcastAddress": "Broadcast Address",
    "firstUsable": "First Usable IP",
    "lastUsable": "Last Usable IP",
    "usableHosts": "Usable Hosts",
    "totalHosts": "Total Hosts",
    "ipVersion": "IP Version",
    "invalidInput": "Invalid IP address or CIDR notation",
    "enterIpOrCidr": "Enter IP address with CIDR (e.g., 192.168.1.0/24) or use subnet mask field"
    ```

    **French (fr.json):**
    Translate to French:
    - "Réseau" (Network)
    - "Calculatrice de Sous-réseau Visuelle" (Visual Subnet Calculator)
    - "Adresse IP" (IP Address)
    - "Masque de Sous-réseau" (Subnet Mask)
    - "Adresse Réseau" (Network Address)
    - "Adresse de Diffusion" (Broadcast Address)
    - "Première IP Utilisable" (First Usable IP)
    - "Dernière IP Utilisable" (Last Usable IP)
    - "Hôtes Utilisables" (Usable Hosts)
    - "Hôtes Totaux" (Total Hosts)
    - "Version IP" (IP Version)

    **German (de.json):**
    Translate to German:
    - "Netzwerk" (Network)
    - "Visueller Subnetz-Rechner" (Visual Subnet Calculator)
    - "IP-Adresse" (IP Address)
    - "Subnetzmaske" (Subnet Mask)
    - "Netzwerkadresse" (Network Address)
    - "Broadcast-Adresse" (Broadcast Address)
    - "Erste verwendbare IP" (First Usable IP)
    - "Letzte verwendbare IP" (Last Usable IP)
    - "Verwendbare Hosts" (Usable Hosts)
    - "Hosts Gesamt" (Total Hosts)
    - "IP-Version" (IP Version)

    **Italian (it.json):**
    Translate to Italian:
    - "Rete" (Network)
    - "Calcolatore Subnet Visuale" (Visual Subnet Calculator)
    - "Indirizzo IP" (IP Address)
    - "Maschera di Sottorete" (Subnet Mask)
    - "Indirizzo di Rete" (Network Address)
    - "Indirizzo di Broadcast" (Broadcast Address)
    - "Primo IP Utilizzabile" (First Usable IP)
    - "Ultimo IP Utilizzabile" (Last Usable IP)
    - "Host Utilizzabili" (Usable Hosts)
    - "Host Totali" (Total Hosts)
    - "Versione IP" (IP Version)

    Maintain JSON formatting and structure. Insert in alphabetical order within each section.

  </action>
  <verify>
    grep -q '"subnet-calculator"' src/messages/en.json
    grep -q '"subnet-calculator"' src/messages/fr.json
    grep -q '"subnet-calculator"' src/messages/de.json
    grep -q '"subnet-calculator"' src/messages/it.json
    grep -q '"network"' src/messages/en.json
    npm run build
  </verify>
  <done>All 4 locales have complete subnet calculator translations with network category</done>
</task>

</tasks>

<verification>
Run build to verify:
1. No TypeScript errors from registry changes
2. Translations valid JSON
3. ipaddr.js installed correctly
4. Network category appears in category list
5. Subnet calculator registered and discoverable

Check registry functionality:

```bash
# Verify category exists
grep -A 10 'id: "network"' src/lib/registry/categories.ts

# Verify converter registered
grep -A 10 'subnet-calculator' src/lib/registry/network-converters.ts

# Verify translations in all locales
grep '"subnet-calculator"' src/messages/*.json
```

</verification>

<success_criteria>

- ipaddr.js dependency present in package.json and node_modules
- Network category defined with subcategories for subnet and IP tools
- Subnet calculator registered with correct metadata (id, slug, category, keywords)
- All 4 translation files contain subnet calculator strings
- Build completes successfully with no errors
- Foundation ready for implementing calculation logic in Plan 02
  </success_criteria>

<output>
After completion, create `.planning/phases/09-visual-subnet-foundation/09-01-SUMMARY.md`
</output>
