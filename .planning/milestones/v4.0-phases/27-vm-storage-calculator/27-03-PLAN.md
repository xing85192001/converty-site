---
phase: 27-vm-storage-calculator
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/de.json
  - src/messages/it.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Page loads without MISSING_MESSAGE errors"
    - "PDF export generates labels like 'Profile 1 - Disk Size'"
    - "CSV export includes profile labels in all 4 locales"
  artifacts:
    - path: "src/messages/en.json"
      provides: "English 'profile' translation"
      contains: '"profile": "Profile"'
    - path: "src/messages/fr.json"
      provides: "French 'profile' translation"
      contains: '"profile": "Profil"'
    - path: "src/messages/de.json"
      provides: "German 'profile' translation"
      contains: '"profile": "Profil"'
    - path: "src/messages/it.json"
      provides: "Italian 'profile' translation"
      contains: '"profile": "Profilo"'
  key_links:
    - from: "src/app/[locale]/infrastructure/vm-storage-calculator/vm-storage-calculator.tsx"
      to: "src/messages/*.json"
      via: "useTranslations('calculator.vmStorage')"
      pattern: 't\\("profile"\\)'
---

<objective>
Add missing 'profile' translation key to fix VM Storage Calculator page load error.

Purpose: Close UAT gap - Test 1 fails with MISSING_MESSAGE error for `calculator.vmStorage.profile`
Output: All 4 locale files updated, calculator page loads without errors
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-vm-storage-calculator/27-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'profile' translation to all locale files</name>
  <files>
    src/messages/en.json
    src/messages/fr.json
    src/messages/de.json
    src/messages/it.json
  </files>
  <action>
    Add the missing 'profile' key to the calculator.vmStorage namespace in each locale file.
    Insert after the existing "removeProfile" key (line ~1448 in each file):

    en.json: "profile": "Profile",
    fr.json: "profile": "Profil",
    de.json: "profile": "Profil",
    it.json: "profile": "Profilo",

    Gap reason: The key is used 6 times in vm-storage-calculator.tsx for PDF/CSV export labels
    to create strings like "Profile 1 - Disk Size" but was never added to translations.

    Reference locations in component:
    - Line 62: label: `${t("profile")} ${index + 1} - ${t("diskSize")}`
    - Line 67: label: `${t("profile")} ${index + 1} - ${t("ramSize")}`
    - Line 72: label: `${t("profile")} ${index + 1} - ${t("vmCount")}`
    - Line 135: Field: `${t("profile")} ${index + 1} - ${t("diskSize")}`
    - Line 140: Field: `${t("profile")} ${index + 1} - ${t("ramSize")}`
    - Line 144: Field: `${t("profile")} ${index + 1} - ${t("vmCount")}`
  </action>
  <verify>
    npm run build 2>&1 | grep -i "MISSING_MESSAGE.*profile" || echo "No profile translation errors"
    grep -A1 '"vmStorage"' src/messages/en.json | head -10
  </verify>
  <done>
    All 4 locale files contain "profile" key in calculator.vmStorage namespace.
    Build succeeds without MISSING_MESSAGE errors for profile key.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify calculator page loads</name>
  <files>None (verification only)</files>
  <action>
    Run production build to verify no translation errors remain.
    Check that the VM Storage Calculator page compiles successfully.
  </action>
  <verify>
    npm run build
    Check output for any MISSING_MESSAGE errors related to vmStorage namespace.
  </verify>
  <done>
    Build completes successfully.
    No MISSING_MESSAGE errors in build output for vmStorage keys.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `npm run build` completes without errors
2. All locales have key: `grep '"profile"' src/messages/*.json` shows 4 matches
3. No runtime errors: Page at /en/infrastructure/vm-storage-calculator loads
</verification>

<success_criteria>
- [x] 'profile' key exists in en.json calculator.vmStorage
- [x] 'profile' key exists in fr.json calculator.vmStorage
- [x] 'profile' key exists in de.json calculator.vmStorage
- [x] 'profile' key exists in it.json calculator.vmStorage
- [x] npm run build succeeds without MISSING_MESSAGE errors
- [x] VM Storage Calculator page loads in browser
</success_criteria>

<output>
After completion, create `.planning/phases/27-vm-storage-calculator/27-03-SUMMARY.md`
</output>
