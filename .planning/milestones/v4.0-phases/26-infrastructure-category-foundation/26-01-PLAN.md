---
phase: 26-infrastructure-category-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/registry/categories.ts
  - src/lib/registry/infrastructure-converters.ts
  - src/lib/registry/converters.ts
autonomous: true

must_haves:
  truths:
    - "Infrastructure category exists in the registry"
    - "Infrastructure category has correct icon and subcategories"
    - "System ready to accept new infrastructure calculators"
    - "Build succeeds with new category"
  artifacts:
    - path: "src/lib/registry/categories.ts"
      provides: "Infrastructure category definition"
      contains: 'id: "infrastructure"'
    - path: "src/lib/registry/infrastructure-converters.ts"
      provides: "Infrastructure converter registry scaffold"
      exports: ["infrastructureConverters"]
    - path: "src/lib/registry/converters.ts"
      provides: "Infrastructure converters integration"
      contains: "infrastructureConverters"
  key_links:
    - from: "src/lib/registry/converters.ts"
      to: "src/lib/registry/infrastructure-converters.ts"
      via: "import and spread"
      pattern: "import.*infrastructureConverters"
    - from: "src/lib/registry/infrastructure-converters.ts"
      to: "Server icon from lucide-react"
      via: "icon property"
      pattern: "icon: Server"
---

<objective>
Create the infrastructure category in the registry with proper icon and subcategories.

Purpose: Establish the category structure for VM, Kubernetes, and VMware calculators planned for Phases 27-30. This follows the exact pattern used for network category (Phase 9).

Output: Infrastructure category registered with subcategories, empty converter registry ready for calculator additions.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/registry/categories.ts
@src/lib/registry/network-converters.ts
@src/lib/registry/converters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add infrastructure category to categories.ts</name>
  <files>src/lib/registry/categories.ts</files>
  <action>
Add a new infrastructure category to categories.ts following the established pattern.

**1. Add Server icon import:**
Add `Server` to the lucide-react import statement (alphabetically after `Palette`).

**2. Add infrastructure category object (insert alphabetically between "health" and "math"):**
```typescript
{
  id: "infrastructure",
  slug: "infrastructure",
  name: "Infrastructure",
  description: "Virtualization, Kubernetes, and datacenter tools",
  icon: Server,
  subcategories: [
    { id: "vmware", name: "VMware", description: "vSphere and ESX calculators" },
    { id: "kubernetes", name: "Kubernetes", description: "K8s capacity planning" },
    { id: "cost", name: "Cost Analysis", description: "TCO and licensing calculators" },
  ],
},
```

**Why Server icon:** Represents infrastructure/servers, distinct from Network icon (already used). Server is the standard icon for datacenter/virtualization tools.

Match exact formatting and pattern from existing categories (see network, crypto, cooking as examples).
  </action>
  <verify>
    grep -q 'id: "infrastructure"' src/lib/registry/categories.ts
    grep -q 'Server' src/lib/registry/categories.ts
    npm run type-check
  </verify>
  <done>Infrastructure category added to registry with Server icon and 3 subcategories</done>
</task>

<task type="auto">
  <name>Task 2: Create infrastructure-converters.ts</name>
  <files>src/lib/registry/infrastructure-converters.ts</files>
  <action>
Create a new converter registry file for infrastructure calculators.

**Create src/lib/registry/infrastructure-converters.ts:**

```typescript
import { Server } from "lucide-react";
import type { ConverterMeta } from "@/types";

export const infrastructureConverters: Record<string, ConverterMeta> = {
  // VM Storage Calculator - Phase 27
  // K8s Capacity Calculator - Phase 28
  // VMware Server Calculator - Phase 29
  // VMware Licensing Calculator - Phase 29
  // Virtualization Cost Calculator - Phase 30
};
```

**Why empty with comments:** Following the pattern from Phase 9, we create the registry file first with placeholder comments. Actual calculator entries will be added in Phases 27-30 as each calculator is implemented.

**Why Server icon:** Consistent icon for all infrastructure calculators, matching the category icon.
  </action>
  <verify>
    test -f src/lib/registry/infrastructure-converters.ts
    grep -q 'infrastructureConverters' src/lib/registry/infrastructure-converters.ts
    npm run type-check
  </verify>
  <done>Infrastructure converters registry file created with placeholder structure</done>
</task>

<task type="auto">
  <name>Task 3: Update converters.ts to include infrastructure converters</name>
  <files>src/lib/registry/converters.ts</files>
  <action>
Update the main converter registry to include infrastructure converters.

**1. Add import (alphabetically after "healthConverters"):**
```typescript
import { infrastructureConverters } from "./infrastructure-converters";
```

**2. Spread into converterRegistry (alphabetically after "...healthConverters"):**
```typescript
...infrastructureConverters,
```

**Match existing pattern exactly:** See how networkConverters, cryptoConverters, automotiveConverters are imported and spread.
  </action>
  <verify>
    grep -q 'infrastructureConverters' src/lib/registry/converters.ts
    grep -q 'infrastructure-converters' src/lib/registry/converters.ts
    npm run build
  </verify>
  <done>Infrastructure converters integrated into main registry</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Verify category exists:
   ```bash
   grep -A 15 'id: "infrastructure"' src/lib/registry/categories.ts
   ```

2. Verify converter registry:
   ```bash
   grep 'infrastructureConverters' src/lib/registry/converters.ts
   ```

3. Build succeeds:
   ```bash
   npm run build
   ```

4. Type check passes:
   ```bash
   npm run type-check
   ```
</verification>

<success_criteria>
- Infrastructure category defined in categories.ts with Server icon
- Three subcategories: vmware, kubernetes, cost
- infrastructure-converters.ts exists with empty registry
- converters.ts imports and spreads infrastructure converters
- Build completes successfully with no TypeScript errors
- Ready for Phase 27 to add first calculator
</success_criteria>

<output>
After completion, create `.planning/phases/26-infrastructure-category-foundation/26-01-SUMMARY.md`
</output>
