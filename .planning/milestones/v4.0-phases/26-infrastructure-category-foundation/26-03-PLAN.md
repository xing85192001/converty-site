---
phase: 26-infrastructure-category-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/de.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Production build generates all locale pages without MISSING_MESSAGE errors"
    - "German k8sCapacity translations match English structure (51 keys)"
    - "npm run build completes with 0 errors"
  artifacts:
    - path: "src/messages/de.json"
      provides: "Complete k8sCapacity German translations"
      contains: "k8sCapacity"
  key_links:
    - from: "src/messages/de.json"
      to: "K8s Capacity Calculator component"
      via: "useTranslations('calculator.k8sCapacity')"
      pattern: "k8sCapacity.*podWorkload"
---

<objective>
Fix missing German translations for k8sCapacity calculator causing build errors.

Purpose: Close UAT gap - German locale (de.json) has outdated k8sCapacity section with 36 keys instead of required 51 keys, causing 19 MISSING_MESSAGE errors during production build.

Output: Updated de.json with complete 51-key k8sCapacity section matching en.json structure.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-infrastructure-category-foundation/26-UAT.md

# Reference files
@src/messages/en.json - English reference (correct 51-key structure)
@src/messages/de.json - German file to fix
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace k8sCapacity section in German translations</name>
  <files>src/messages/de.json</files>
  <action>
Replace the entire `calculator.k8sCapacity` section in de.json with German translations of the current 51 keys.

**Current state (outdated - 36 keys):** Has obsolete keys like `podCount`, `cpuPerPodMillicores`, `ramPerPodMi`, etc.

**Required state (51 keys):** Match en.json structure with proper German translations.

**German translations for all 51 keys:**

```json
"k8sCapacity": {
  "podWorkload": "Pod-Arbeitslast",
  "podCpuRequest": "CPU-Anforderung pro Pod (Millicores)",
  "podMemoryRequest": "Speicheranforderung pro Pod (MiB)",
  "podReplicas": "Pod-Replikate",
  "nodeSpecs": "Knoten-Spezifikationen",
  "nodeCpuCores": "CPU-Kerne pro Knoten",
  "nodeMemoryMb": "Speicher pro Knoten (MB)",
  "systemOverhead": "System-Overhead (pro Knoten)",
  "systemReservedCpu": "Reservierte System-CPU (Millicores)",
  "systemReservedMemory": "Reservierter System-Speicher (MiB)",
  "daemonSetCpuPerNode": "DaemonSet-CPU (Millicores)",
  "daemonSetMemoryPerNode": "DaemonSet-Speicher (MiB)",
  "targetUtilization": "Ziel-Auslastung",
  "targetCpuUtilization": "Ziel-CPU-Auslastung (%)",
  "targetMemoryUtilization": "Ziel-Speicherauslastung (%)",
  "results": "Kapazitätsergebnisse",
  "nodesNeeded": "Benötigte Knoten",
  "limitingFactor": "Begrenzender Faktor",
  "cpuConstraint": "CPU-Beschränkung",
  "memoryConstraint": "Speicher-Beschränkung",
  "allocatableCpu": "Verfügbare CPU pro Knoten",
  "allocatableMemory": "Verfügbarer Speicher pro Knoten",
  "finalCpuUtilization": "Endgültige CPU-Auslastung",
  "finalMemoryUtilization": "Endgültige Speicherauslastung",
  "breakdown": "Kapazitätsaufschlüsselung",
  "cpuBreakdown": "CPU-Aufschlüsselung",
  "memoryBreakdown": "Speicher-Aufschlüsselung",
  "calculationSteps": "Berechnungsschritte",
  "overUtilizationWarning": "Warnung: Auslastung überschreitet 80%. Erwägen Sie das Hinzufügen weiterer Knoten oder die Reduzierung der Ziel-Auslastung für bessere Zuverlässigkeit und Autoscaling-Puffer.",
  "systemReservedHelp": "System-reserviert umfasst OS-Daemons, Kubernetes-Komponenten (kubelet, Container-Runtime) und Eviction-Schwellenwerte. Typische Werte: 500-1000m CPU, 500-1000 MiB Speicher.",
  "daemonSetHelp": "DaemonSets laufen auf jedem Knoten (Monitoring-Agenten, Log-Collectors, Netzwerk-Plugins). Typische Werte: 200-500m CPU, 256-512 MiB Speicher pro Knoten.",
  "targetUtilizationHelp": "Ziel-Auslastung lässt Puffer für Autoscaling und Verkehrsspitzen. Industriestandard: 70% CPU, 80% Speicher. 100% lässt keinen Puffer.",
  "limitingFactorHelp": "Kubernetes-Scheduling ist mehrdimensional. Speicher ist typischerweise der begrenzende Faktor (nicht komprimierbar, verursacht OOM-Kills).",
  "cores": "Kerne",
  "millicores": "Millicores",
  "mib": "MiB",
  "mb": "MB",
  "cpuConstrained": "CPU-begrenzt",
  "memoryConstrained": "Speicher-begrenzt",
  "cpuResults": "CPU-Ergebnisse",
  "memoryResults": "Speicher-Ergebnisse",
  "allocatableCpuPerNode": "Verfügbare CPU pro Knoten",
  "allocatableMemoryPerNode": "Verfügbarer Speicher pro Knoten",
  "nodesNeededByCpu": "Benötigte Knoten (CPU)",
  "nodesNeededByMemory": "Benötigte Knoten (Speicher)",
  "step1": "Schritt 1: Verfügbare Ressourcen pro Knoten berechnen",
  "step2": "Schritt 2: Gesamte Pod-Ressourcenanforderungen berechnen",
  "step3": "Schritt 3: Ziel-Auslastung anwenden",
  "step4": "Schritt 4: Benötigte Knoten pro Ressource berechnen",
  "step5": "Schritt 5: Endgültige Knotenanzahl bestimmen",
  "reset": "Zurücksetzen"
}
```

**Instructions:**
1. Read de.json
2. Locate the `calculator.k8sCapacity` section
3. Replace the entire section with the German translations above
4. Maintain JSON formatting consistency with rest of file
5. Write updated de.json
  </action>
  <verify>
Run: `node -e "const de = require('./src/messages/de.json'); const keys = Object.keys(de.calculator.k8sCapacity); console.log('Key count:', keys.length); console.log('Has podWorkload:', 'podWorkload' in de.calculator.k8sCapacity);"`

Expected: Key count: 51, Has podWorkload: true
  </verify>
  <done>German k8sCapacity section has exactly 51 keys matching English structure</done>
</task>

<task type="auto">
  <name>Task 2: Verify production build succeeds without MISSING_MESSAGE errors</name>
  <files>N/A (verification only)</files>
  <action>
Run full production build to verify no MISSING_MESSAGE errors for k8sCapacity keys in any locale.

1. Run `npm run build`
2. Check output for any MISSING_MESSAGE errors
3. Verify all infrastructure pages generated for all 4 locales

**Expected outcome:** Build completes with 0 errors, no MISSING_MESSAGE warnings for k8sCapacity.
  </action>
  <verify>
Run: `npm run build 2>&1 | grep -E "(MISSING_MESSAGE|Error:)" | head -20`

Expected: No output (no errors)
  </verify>
  <done>Production build completes with 0 MISSING_MESSAGE errors for any locale</done>
</task>

</tasks>

<verification>
1. German k8sCapacity has 51 keys: `node -e "console.log(Object.keys(require('./src/messages/de.json').calculator.k8sCapacity).length)"`
2. All required keys present: Check for podWorkload, nodeSpecs, results, breakdown, calculationSteps
3. No obsolete keys: Ensure podCount, cpuPerPodMillicores, ramPerPodMi are removed
4. Production build clean: `npm run build` succeeds with no MISSING_MESSAGE errors
</verification>

<success_criteria>
- German locale has complete 51-key k8sCapacity section
- All keys match English structure (same key names)
- Production build generates all locale pages without errors
- UAT Test 7 can now pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-infrastructure-category-foundation/26-03-SUMMARY.md`
</output>
