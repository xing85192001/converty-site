---
phase: 29-vmware-server-licensing
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/app/[locale]/infrastructure/server-virtualization/server-virtualization-calculator.tsx
  - src/app/[locale]/infrastructure/server-virtualization/page.tsx
autonomous: true

must_haves:
  truths:
    - "User can input VM workload specifications (count, vCPU, RAM)"
    - "User can configure ESX host specifications (cores, RAM)"
    - "User can adjust over-subscription ratios and utilization targets"
    - "User can toggle N+1 high availability"
    - "Calculator shows hosts needed by CPU and RAM constraints"
    - "Limiting factor (CPU vs RAM) is clearly indicated"
    - "Calculator state persists to URL for sharing"
    - "Interface is responsive and translated in all 4 locales"
  artifacts:
    - path: "src/app/[locale]/infrastructure/server-virtualization/server-virtualization-calculator.tsx"
      provides: "Interactive UI component for server virtualization calculator"
      min_lines: 150
    - path: "src/app/[locale]/infrastructure/server-virtualization/page.tsx"
      provides: "Next.js page wrapper with metadata"
      exports: ["default", "generateMetadata"]
  key_links:
    - from: "server-virtualization-calculator.tsx"
      to: "src/lib/converters/infrastructure/server-virtualization.ts"
      via: "import calculateServerVirtualization"
      pattern: "calculateServerVirtualization"
    - from: "server-virtualization-calculator.tsx"
      to: "createCalculatorStore"
      via: "Zustand store with URL sync"
      pattern: "useServerVirtualizationStore"
    - from: "page.tsx"
      to: "server-virtualization-calculator.tsx"
      via: "renders in ConverterLayout"
      pattern: "ServerVirtualizationCalculator"
---

<objective>
Create interactive UI component and page for the server virtualization calculator with Zustand state management and URL persistence.

Purpose: Provide a user-friendly interface for IT professionals to calculate ESX host requirements based on VM workload.
Output: Responsive calculator component, Next.js page, URL state sync working.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/[locale]/infrastructure/vm-storage/vm-storage-calculator.tsx
@src/app/[locale]/infrastructure/k8s-capacity/k8s-capacity-calculator.tsx
@src/stores/calculator-store.ts
@src/lib/converters/infrastructure/server-virtualization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create server virtualization calculator component</name>
  <files>src/app/[locale]/infrastructure/server-virtualization/server-virtualization-calculator.tsx</files>
  <action>
Create client component for server virtualization calculator following infrastructure calculator pattern.

**File structure**:

```typescript
"use client";

import { useFormatter, useTranslations } from "next-intl";
import { InputField, ResultGrid } from "@/components/converter";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Switch } from "@/components/ui/switch";
import { Label } from "@/components/ui/label";
import {
  calculateServerVirtualization,
  type ServerVirtualizationInput,
  type ServerVirtualizationResult,
} from "@/lib/converters/infrastructure/server-virtualization";
import { createCalculatorStore } from "@/stores/calculator-store";

const useServerVirtualizationStore = createCalculatorStore<
  ServerVirtualizationInput,
  ServerVirtualizationResult
>({
  name: "server-virtualization",
  initialValues: {
    vmCount: 100,
    vCpuPerVm: 4,
    ramPerVmGb: 16,
    hostCores: 32,
    hostRamGb: 512,
    vCpuToCoreRatio: 4,
    targetCpuUtilization: 80,
    targetRamUtilization: 85,
    highAvailability: true,
  },
  calculate: calculateServerVirtualization,
});

export default function ServerVirtualizationCalculator() {
  const t = useTranslations("calculator.infrastructure.server-virtualization-calculator");
  const format = useFormatter();
  const { values, setValue, result } = useServerVirtualizationStore();

  return (
    <div className="grid gap-6 lg:grid-cols-2">
      {/* Input Section - 3 Cards */}
      <div className="space-y-6">
        <Card>
          <CardHeader>
            <CardTitle>VM Workload</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <InputField
              id="vmCount"
              label={t("vm-count")}
              type="number"
              value={values.vmCount}
              onChange={(val) => setValue("vmCount", val)}
              min={1}
            />
            <InputField
              id="vCpuPerVm"
              label={t("vcpu-per-vm")}
              type="number"
              value={values.vCpuPerVm}
              onChange={(val) => setValue("vCpuPerVm", val)}
              min={1}
            />
            <InputField
              id="ramPerVmGb"
              label={t("ram-per-vm-gb")}
              type="number"
              value={values.ramPerVmGb}
              onChange={(val) => setValue("ramPerVmGb", val)}
              min={1}
            />
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>ESX Host Specs</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <InputField
              id="hostCores"
              label={t("host-cores")}
              type="number"
              value={values.hostCores}
              onChange={(val) => setValue("hostCores", val)}
              min={1}
            />
            <InputField
              id="hostRamGb"
              label={t("host-ram-gb")}
              type="number"
              value={values.hostRamGb}
              onChange={(val) => setValue("hostRamGb", val)}
              min={1}
            />
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>Constraints</CardTitle>
          </CardHeader>
          <CardContent className="space-y-4">
            <InputField
              id="vCpuToCoreRatio"
              label={t("vcpu-to-core-ratio")}
              type="number"
              value={values.vCpuToCoreRatio}
              onChange={(val) => setValue("vCpuToCoreRatio", val)}
              min={1}
              step={0.5}
            />
            <InputField
              id="targetCpuUtilization"
              label={t("target-cpu-utilization")}
              type="number"
              value={values.targetCpuUtilization}
              onChange={(val) => setValue("targetCpuUtilization", val)}
              min={1}
              max={100}
            />
            <InputField
              id="targetRamUtilization"
              label={t("target-ram-utilization")}
              type="number"
              value={values.targetRamUtilization}
              onChange={(val) => setValue("targetRamUtilization", val)}
              min={1}
              max={100}
            />
            <div className="flex items-center space-x-2">
              <Switch
                id="highAvailability"
                checked={values.highAvailability}
                onCheckedChange={(checked) => setValue("highAvailability", checked)}
              />
              <Label htmlFor="highAvailability">{t("high-availability")}</Label>
            </div>
          </CardContent>
        </Card>
      </div>

      {/* Results Section */}
      <div className="space-y-6">
        {result && (
          <>
            <Card>
              <CardHeader>
                <CardTitle>Host Requirements</CardTitle>
              </CardHeader>
              <CardContent>
                <div className="space-y-6">
                  {/* Primary result - large and prominent */}
                  <div className="text-center">
                    <div className="text-5xl font-bold text-primary">
                      {result.hostsNeededTotal}
                    </div>
                    <div className="text-sm text-muted-foreground mt-2">
                      {t("hosts-needed-total")}
                    </div>
                  </div>

                  {/* Limiting factor indicator */}
                  <div className={`p-4 rounded-lg ${
                    result.limitingFactor === "cpu"
                      ? "bg-blue-50 dark:bg-blue-950"
                      : "bg-green-50 dark:bg-green-950"
                  }`}>
                    <div className="text-sm font-medium">
                      {t("limiting-factor")}: {result.limitingFactor.toUpperCase()}
                    </div>
                  </div>

                  <ResultGrid
                    items={[
                      {
                        label: t("vcpu-consolidation-ratio"),
                        value: result.vCpuConsolidationRatio.toFixed(2) + ":1",
                      },
                      {
                        label: t("final-cpu-utilization"),
                        value: result.finalCpuUtilization.toFixed(1) + "%",
                      },
                      {
                        label: t("final-ram-utilization"),
                        value: result.finalRamUtilization.toFixed(1) + "%",
                      },
                    ]}
                  />
                </div>
              </CardContent>
            </Card>

            {/* Calculation steps */}
            <Card>
              <CardHeader>
                <CardTitle>Calculation Details</CardTitle>
              </CardHeader>
              <CardContent>
                <div className="space-y-2 text-sm">
                  {result.steps.map((step, index) => (
                    <div key={index} className="flex items-start gap-2">
                      <span className="text-muted-foreground min-w-[1.5rem]">
                        {index + 1}.
                      </span>
                      <span>{step}</span>
                    </div>
                  ))}
                </div>
              </CardContent>
            </Card>
          </>
        )}
      </div>
    </div>
  );
}
```

**Key implementation notes**:
- Use Switch component for highAvailability toggle (boolean input)
- Display hosts needed total prominently (large text, centered)
- Color-code limiting factor (blue for CPU, green for RAM)
- Show consolidation ratio and final utilization percentages
- Display calculation steps for transparency
- Follow responsive grid pattern (1 col mobile, 2 cols desktop)
  </action>
  <verify>
- TypeScript compilation: npm run type-check
- Biome lint: npm run check
- Component renders without errors
- Zustand store manages state correctly
  </verify>
  <done>server-virtualization-calculator.tsx exists with working UI and Zustand store</done>
</task>

<task type="auto">
  <name>Task 2: Create Next.js page wrapper</name>
  <files>src/app/[locale]/infrastructure/server-virtualization/page.tsx</files>
  <action>
Create Next.js page following infrastructure calculator pattern.

```typescript
import { setRequestLocale } from "next-intl/server";
import { getTranslations } from "next-intl/server";
import type { Metadata } from "next";
import { ConverterLayout } from "@/components/layout/converter-layout";
import ServerVirtualizationCalculator from "./server-virtualization-calculator";
import { Suspense } from "react";
import { CalculatorSkeleton } from "@/components/converter/calculator-skeleton";

export async function generateMetadata({
  params,
}: {
  params: Promise<{ locale: string }>;
}): Promise<Metadata> {
  const { locale } = await params;
  const t = await getTranslations({
    locale,
    namespace: "calculator.infrastructure.server-virtualization-calculator",
  });

  return {
    title: t("title"),
    description: t("description"),
  };
}

export default async function ServerVirtualizationPage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);

  const t = await getTranslations({
    locale,
    namespace: "calculator.infrastructure.server-virtualization-calculator",
  });

  return (
    <ConverterLayout
      title={t("title")}
      description={t("description")}
      slug="server-virtualization"
    >
      <Suspense fallback={<CalculatorSkeleton inputCount={9} showResults />}>
        <ServerVirtualizationCalculator />
      </Suspense>
    </ConverterLayout>
  );
}
```

**Pattern notes**:
- Use async params (Next.js 16 requirement)
- setRequestLocale for static generation
- ConverterLayout with title, description, slug
- Suspense with CalculatorSkeleton fallback (inputCount=9 for 9 input fields)
- Standard metadata generation from translations
  </action>
  <verify>
- TypeScript compilation: npm run type-check
- Page accessible at /[locale]/infrastructure/server-virtualization
- Metadata generation works
- Suspense fallback renders
  </verify>
  <done>page.tsx exists with metadata and proper Next.js 16 structure</done>
</task>

</tasks>

<verification>
**Overall verification**:

1. **Functionality**: Calculator computes host requirements correctly
2. **URL state**: State persists to URL and loads from URL
3. **Responsiveness**: Grid adapts to mobile (1 col) and desktop (2 cols)
4. **Translations**: Component uses translations for all user-facing text
5. **Type safety**: TypeScript compilation passes
6. **N+1 HA**: Toggle adds 1 host to result when enabled
7. **Limiting factor**: Correctly shows CPU or RAM as constraint
</verification>

<success_criteria>
- [x] server-virtualization-calculator.tsx component created
- [x] page.tsx wrapper created
- [x] Zustand store with URL sync working
- [x] Calculator displays hosts needed prominently
- [x] Limiting factor indicated with color coding
- [x] N+1 HA toggle functional
- [x] All inputs use translated labels
- [x] TypeScript and lint checks pass
- [x] Responsive design working
</success_criteria>

<output>
After completion, create `.planning/phases/29-vmware-server-licensing/29-02-SUMMARY.md`
</output>
