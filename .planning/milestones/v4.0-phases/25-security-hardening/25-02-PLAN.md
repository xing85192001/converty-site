---
phase: 25-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .trivyignore
  - src/app/[locale]/automotive/vehicle-financing/vehicle-financing-calculator.tsx
  - src/app/[locale]/network/ip-calculator/ip-calculator.tsx
  - src/lib/converters/automotive/fuel-efficiency.ts
  - src/lib/converters/cooking/cooking-units.ts
  - src/stores/cooking-units-store.ts
  - src/stores/food-cost-store.ts
  - src/stores/maintenance-intervals-store.ts
  - src/stores/recipe-scaler-store.ts
  - src/stores/tire-sizing-store.ts
autonomous: true

must_haves:
  truths:
    - "Biome lint passes with 0 unused import warnings"
    - "All container vulnerabilities are documented with review dates"
    - "Pre-commit hooks prevent re-introduction of lint issues"
  artifacts:
    - path: ".trivyignore"
      provides: "Documented security suppressions with review dates"
      contains: "exp:2026-07"
    - path: "src/stores/cooking-units-store.ts"
      provides: "Clean imports with no unused symbols"
      min_lines: 10
  key_links:
    - from: ".husky/pre-commit"
      to: "npx lint-staged"
      via: "pre-commit hook execution"
      pattern: "lint-staged"
---

<objective>
Document container vulnerabilities and clean up unused code.

Purpose: Address SEC-02 (container documentation) and SEC-03 (code quality) by updating Trivy suppression documentation and removing all unused imports identified by Biome lint.

Output: Clean codebase with zero Biome lint warnings for unused imports and comprehensive security documentation for container-related false positives.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Security configuration

@.trivyignore
@.husky/pre-commit
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update .trivyignore with comprehensive documentation</name>
  <files>.trivyignore</files>
  <action>
Update .trivyignore to include libpng CVE documentation and improve existing entries:

1. Add header section explaining the static export context
2. Add libpng CVE-2024-44191 suppression (if not already present)
3. Update all entries with:
   - Clear explanation of why it's a false positive
   - Impact assessment (NONE for static exports)
   - Review date (July 2026 - 6 months from now)

Structure:

```
# Trivy Security Suppressions
# ==========================
#
# Context: Converty is a STATIC SITE (Next.js output: "export")
# - No Docker containers are used in production
# - Site is deployed to GitHub Pages as static HTML/JS/CSS
# - All vulnerabilities below are container-related false positives
#
# Review Date: July 2026 (6-month cycle)
# Verified: npm audit --production shows 0 vulnerabilities

# @surma/rollup-plugin-off-main-thread (transitive dev dependency)
# Source: workbox-build -> @surma/rollup-plugin-off-main-thread
# Issue: Dockerfile exists in package for browser testing
# Impact: NONE - Dockerfile never executed, dev dependency only
AVD-DS-0002 exp:2026-07-25
AVD-DS-0017 exp:2026-07-25

# libpng CVE-2024-44191 (container base image)
# Issue: Vulnerability in libpng used by container base image
# Impact: NONE - No Docker containers in production (static export)
# Note: This only affects the development Dockerfile, not production
CVE-2024-44191 exp:2026-07-25
```

If libpng CVE is not triggering Trivy alerts currently, add a comment documenting the static export context for future reference.
  </action>
  <verify>

1. Check file structure: `cat .trivyignore`
2. Verify expiration dates: `grep "exp:2026-07" .trivyignore`
3. Verify documentation: `grep -c "NONE" .trivyignore` should show impact assessments
  </verify>

  <done>
.trivyignore has comprehensive documentation explaining why container vulnerabilities don't affect production, with 6-month review dates set for July 2026.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove all unused imports with Biome auto-fix</name>
  <files>
    src/app/[locale]/automotive/vehicle-financing/vehicle-financing-calculator.tsx
    src/app/[locale]/network/ip-calculator/ip-calculator.tsx
    src/lib/converters/automotive/fuel-efficiency.ts
    src/lib/converters/cooking/cooking-units.ts
    src/stores/cooking-units-store.ts
    src/stores/food-cost-store.ts
    src/stores/maintenance-intervals-store.ts
    src/stores/recipe-scaler-store.ts
    src/stores/tire-sizing-store.ts
  </files>
  <action>
Run Biome auto-fix with unsafe mode to remove all unused imports:

1. Run unsafe fix (required for removing imports):

   ```bash
   npx biome check --write --unsafe .
   ```

2. The following unused imports should be removed:
   - `CircleDollarSign` from vehicle-financing-calculator.tsx
   - Unused type from ip-calculator.tsx
   - Unused imports from fuel-efficiency.ts
   - Unused imports from cooking-units.ts
   - `UnitCategory`, `parseNumberParam` from cooking-units-store.ts
   - Unused imports from food-cost-store.ts
   - Unused imports from maintenance-intervals-store.ts
   - `IngredientType` from recipe-scaler-store.ts
   - `parseTireNotation` from tire-sizing-store.ts

3. Verify the fix worked:

   ```bash
   npm run check
   ```

Note: The `noArrayIndexKey` warnings for steps.map((step, i) => <div key={i}>) are ACCEPTABLE. These are static lists that never reorder, so array indices are valid keys. Do NOT fix these - they would require adding unique IDs to calculation steps which adds unnecessary complexity.
  </action>
  <verify>

1. Run: `npm run check 2>&1 | grep -c "noUnusedImports"` should return 0
2. Run: `npm run check 2>&1 | grep "Found .* warnings"` should show reduced warning count
3. All affected files should still compile: `npm run type-check`
  </verify>

  <done>
All 10 files with unused imports are cleaned. Biome check shows 0 unused import warnings. Only acceptable noArrayIndexKey warnings remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify pre-commit hooks prevent lint regression</name>
  <files>None (verification only)</files>
  <action>
Verify the existing pre-commit hooks will prevent re-introduction of lint issues:

1. Check Husky is configured:

   ```bash
   ls -la .husky/
   cat .husky/pre-commit
   ```

2. Check lint-staged configuration:

   ```bash
   cat package.json | grep -A 10 '"lint-staged"'
   ```

3. Verify Biome is in the lint-staged config for TypeScript files

4. Test the hook (dry run):

   ```bash
   # Stage a file and run the hook manually
   git stash
   echo "// test" >> src/lib/utils/url-params.ts
   git add src/lib/utils/url-params.ts
   npx lint-staged --dry-run
   git checkout src/lib/utils/url-params.ts
   git stash pop
   ```

5. If lint-staged is not configured to run Biome, add it:

   ```json
   "lint-staged": {
     "*.{ts,tsx}": ["biome check --write --unsafe"]
   }
   ```

  </action>
  <verify>
1. `.husky/pre-commit` exists and contains `lint-staged` or `biome`
2. `package.json` has lint-staged configuration for TypeScript files
3. Biome or equivalent lint command runs on staged files
  </verify>
  <done>
Pre-commit hooks are configured to run Biome on staged TypeScript files, preventing re-introduction of unused imports and other lint issues.
  </done>
</task>

</tasks>

<verification>
## Code Quality Verification

1. **Unused imports eliminated:**

   ```bash
   npm run check 2>&1 | grep "noUnusedImports" | wc -l
   # Expected: 0
   ```

2. **Build still passes:**

   ```bash
   npm run build
   # Expected: Success with all 167 calculators
   ```

3. **Type check passes:**

   ```bash
   npm run type-check
   # Expected: 0 errors
   ```

## Security Documentation Verification

1. **Trivy suppressions documented:**

   ```bash
   grep -c "Impact: NONE" .trivyignore
   # Expected: >= 2 entries documented
   ```

2. **Review dates set:**

   ```bash
   grep -c "exp:2026-07" .trivyignore
   # Expected: >= 2 entries with review dates
   ```

## Pre-commit Hook Verification

1. **Hook exists:**

   ```bash
   test -f .husky/pre-commit && echo "exists"
   ```

2. **Lint-staged configured:**

   ```bash
   grep "lint-staged" package.json
   ```

</verification>

<success_criteria>

- [ ] .trivyignore updated with libpng CVE documentation
- [ ] All entries have Impact: NONE explanation
- [ ] All entries have exp:2026-07 review date
- [ ] 0 unused import warnings from Biome
- [ ] npm run type-check passes
- [ ] npm run build passes
- [ ] Pre-commit hooks configured for lint enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/25-security-hardening/25-02-SUMMARY.md`
</output>
