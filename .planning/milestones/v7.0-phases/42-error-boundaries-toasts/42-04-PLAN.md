---
phase: 42-error-boundaries-toasts
plan: 04
type: execute
wave: 2
depends_on:
  - 42-01
  - 42-02
files_modified:
  - src/components/converter/csv-export-button.tsx
  - src/components/converter/pdf-export-button.tsx
  - src/components/converter/converter-layout.tsx
autonomous: true
requirements:
  - R2.1
  - R2.4
must_haves:
  truths:
    - "CSV export shows toast.success on success and toast.error on failure"
    - "PDF export shows toast.success on success and toast.error on failure"
    - "All calculator pages show CalculatorErrorBoundary fallback UI when the calculator component throws"
    - "The error boundary wraps calculator content in ConverterLayout without requiring changes to 169 individual page.tsx files"
  artifacts:
    - path: "src/components/converter/csv-export-button.tsx"
      provides: "CSV export with toast feedback"
      contains: "toast.success"
    - path: "src/components/converter/pdf-export-button.tsx"
      provides: "PDF export with toast feedback"
      contains: "toast.success"
    - path: "src/components/converter/converter-layout.tsx"
      provides: "ConverterLayout wraps children with CalculatorErrorBoundary"
      contains: "CalculatorErrorBoundary"
  key_links:
    - from: "src/components/converter/converter-layout.tsx"
      to: "src/components/error-boundary/calculator-error-boundary.tsx"
      via: "import and JSX wrapping of children"
      pattern: "CalculatorErrorBoundary"
    - from: "src/components/converter/csv-export-button.tsx"
      to: "sonner toast"
      via: "import { toast } from 'sonner'"
      pattern: "toast\\.success|toast\\.error"
---

<objective>
Wire toast notifications into CSV/PDF export buttons and wrap `ConverterLayout` children with `CalculatorErrorBoundary`. The ConverterLayout change applies the error boundary to all 169 calculators in one edit.

Purpose: Completes R2.4 (export feedback) and R2.1 (error boundary wiring) without touching individual page files.
Output: 3 modified files. All export actions give toast feedback. All calculator pages get error boundary protection.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-error-boundaries-toasts/42-RESEARCH.md
@.planning/phases/42-error-boundaries-toasts/42-01-SUMMARY.md
@.planning/phases/42-error-boundaries-toasts/42-02-SUMMARY.md
@src/components/converter/csv-export-button.tsx
@src/components/converter/pdf-export-button.tsx
@src/components/converter/converter-layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toast feedback to CSV and PDF export buttons</name>
  <files>
    src/components/converter/csv-export-button.tsx,
    src/components/converter/pdf-export-button.tsx
  </files>
  <action>
    Both files already have `"use client"` and use `useTranslations("calculator.export")`.

    **csv-export-button.tsx:**
    - Add `import { toast } from "sonner";` after existing imports
    - Wrap the `handleExport` body in try/catch:
      ```typescript
      const handleExport = () => {
        try {
          const timestamp = new Date().toISOString().slice(0, 19).replace("T", "_").replace(/:/g, "-");
          const fullFilename = `${filename}_${timestamp}.csv`;
          exportToCsv(data, { filename: fullFilename });
          toast.success(t("csvExportSuccess"));
        } catch (error) {
          console.error("CSV export failed:", error);
          toast.error(t("csvExportError"));
        }
      };
      ```
    - The translation keys `csvExportSuccess` and `csvExportError` are under `calculator.export` (added to locale files via Plan 01). Check Plan 01 — if keys were placed under `common.toast` not `calculator.export`, use `useTranslations("common.toast")` for the toast messages instead.

    **IMPORTANT: Plan 01 puts toast keys under `common.toast`. So for toast messages, use a separate `const tToast = useTranslations("common.toast");` and call `tToast("csvExportSuccess")` and `tToast("csvExportError")`. The existing `t` from `"calculator.export"` still handles button labels.**

    **pdf-export-button.tsx:**
    - Same pattern: add `import { toast } from "sonner";`, add `const tToast = useTranslations("common.toast");`, wrap handleExport in try/catch with `tToast("pdfExportSuccess")` / `tToast("pdfExportError")`.

    For Biome compliance: do NOT access `error.message` in catch blocks without `instanceof Error` guard. Plain `console.error("...", error)` is fine.
  </action>
  <verify>
    Run `npm run type-check` — zero errors.
    Run `npm run check:fix` — zero Biome violations.
    Both files contain `toast.success` and `toast.error` calls.
  </verify>
  <done>csv-export-button.tsx and pdf-export-button.tsx show toast feedback on export success/failure. This propagates to all 10 calculator components using these buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap ConverterLayout children with CalculatorErrorBoundary</name>
  <files>src/components/converter/converter-layout.tsx</files>
  <action>
    Update `src/components/converter/converter-layout.tsx` to import and use `CalculatorErrorBoundary`.

    Current file is a server component (no `"use client"`). Importing a client component into a server component is valid in Next.js App Router — the client component boundary is at the client component file itself.

    Changes:
    1. Add import: `import { CalculatorErrorBoundary } from "@/components/error-boundary/calculator-error-boundary";`
    2. Wrap `{children}` in the `CardContent` with `<CalculatorErrorBoundary>`:

    ```tsx
    <Card className="mb-8">
      <CardContent className="pt-6">
        <CalculatorErrorBoundary>
          {children}
        </CalculatorErrorBoundary>
      </CardContent>
    </Card>
    ```

    Do NOT wrap the entire component in ErrorBoundary — only the children slot inside CardContent. This ensures header/footer remain visible if a calculator throws, while the calculator area shows the fallback UI.

    The `ConverterLayout` itself does NOT need `"use client"` — it remains a server component. The ErrorBoundary activation happens on the client side where React renders the tree.
  </action>
  <verify>
    Run `npm run type-check` — zero TypeScript errors.
    Run `npm run check:fix` — zero Biome violations.
    `grep "CalculatorErrorBoundary" src/components/converter/converter-layout.tsx` returns a match.
    `converter-layout.tsx` does NOT have `"use client"` at the top (it must remain a server component).
  </verify>
  <done>ConverterLayout imports CalculatorErrorBoundary and wraps {children} with it inside CardContent. All 169 calculators that use ConverterLayout now have error boundary protection with zero individual page changes.</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes with zero errors
- `npm run check:fix` shows zero Biome violations
- `src/components/converter/csv-export-button.tsx` contains `toast.success` and `toast.error`
- `src/components/converter/pdf-export-button.tsx` contains `toast.success` and `toast.error`
- `src/components/converter/converter-layout.tsx` imports and uses `CalculatorErrorBoundary`
- `converter-layout.tsx` does NOT have `"use client"` directive
- `npm run build` completes without errors (static export still works)
</verification>

<success_criteria>
Export toast feedback wired (R2.4 export portion complete). Error boundary applied to all ConverterLayout consumers via single edit (R2.1 complete). Build remains clean.
</success_criteria>

<output>
After completion, create `.planning/phases/42-error-boundaries-toasts/42-04-SUMMARY.md` following the summary template.
</output>
