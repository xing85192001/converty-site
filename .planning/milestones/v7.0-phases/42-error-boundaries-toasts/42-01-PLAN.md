---
phase: 42-error-boundaries-toasts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/[locale]/layout.tsx
  - src/messages/en.json
  - src/messages/fr.json
  - src/messages/de.json
  - src/messages/it.json
autonomous: true
requirements:
  - R2.3
must_haves:
  truths:
    - "sonner Toaster is mounted in the locale layout and visible on all calculator pages"
    - "react-error-boundary and isomorphic-dompurify packages are installed"
    - "Toast i18n keys exist in all 4 locale files under common.toast"
  artifacts:
    - path: "package.json"
      provides: "sonner, react-error-boundary, isomorphic-dompurify in dependencies"
      contains: "sonner"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Toaster mounted inside ThemeProvider after Footer"
      contains: "Toaster"
    - path: "src/messages/en.json"
      provides: "Toast translation keys under common.toast"
      contains: "copySuccess"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "sonner Toaster"
      via: "import and JSX mount"
      pattern: "Toaster"
---

<objective>
Install the three new packages (react-error-boundary, sonner, isomorphic-dompurify), mount `<Toaster>` in the locale layout, and add toast i18n keys to all 4 locale files.

Purpose: Establishes the infrastructure that all Wave 2 plans depend on — packages available, Toaster mounted, translation keys ready.
Output: Updated package.json, locale layout with Toaster, 7 new keys in all 4 locale files.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-error-boundaries-toasts/42-RESEARCH.md
@src/app/[locale]/layout.tsx
@src/messages/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and mount Toaster in locale layout</name>
  <files>package.json, src/app/[locale]/layout.tsx</files>
  <action>
    Install the three packages:
    ```
    npm install react-error-boundary sonner isomorphic-dompurify
    ```
    Then update `src/app/[locale]/layout.tsx`:
    - Add import: `import { Toaster } from "sonner";`
    - Inside the `ThemeProvider` JSX, add `<Toaster richColors position="bottom-right" />` immediately after the closing `</div>` tag of the flex container (after `<Footer />` closes), but still inside `</ThemeProvider>`.

    The final layout JSX inside `ThemeProvider` should look like:
    ```tsx
    <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
      <SWRegistration />
      <div className="relative flex min-h-screen flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </div>
      <Toaster richColors position="bottom-right" />
    </ThemeProvider>
    ```

    Do NOT add "use client" to layout.tsx — `<Toaster>` works fine as a server component import.
  </action>
  <verify>
    Run `npm run type-check` — zero TypeScript errors.
    Run `npm run check:fix` — zero Biome errors.
    Visually confirm `<Toaster>` appears after the flex div in layout.tsx.
  </verify>
  <done>package.json has sonner, react-error-boundary, isomorphic-dompurify as dependencies. layout.tsx imports and mounts Toaster.</done>
</task>

<task type="auto">
  <name>Task 2: Add toast translation keys to all 4 locale files</name>
  <files>
    src/messages/en.json,
    src/messages/fr.json,
    src/messages/de.json,
    src/messages/it.json
  </files>
  <action>
    Add a `"toast"` object inside the `"common"` section of each locale file. Insert it after the `"end"` key (the last key in `"common"` before the closing brace of that section).

    Keys to add in all 4 locales:
    - `copySuccess`: success message for clipboard copy
    - `copyError`: error message for clipboard copy failure
    - `csvExportSuccess`: success message for CSV export
    - `csvExportError`: error message for CSV export failure
    - `pdfExportSuccess`: success message for PDF export
    - `pdfExportError`: error message for PDF export failure
    - `calculationError`: generic message for calculation returning no result

    **en.json** additions under `common`:
    ```json
    "toast": {
      "copySuccess": "Copied to clipboard",
      "copyError": "Failed to copy to clipboard",
      "csvExportSuccess": "CSV exported successfully",
      "csvExportError": "CSV export failed",
      "pdfExportSuccess": "PDF exported successfully",
      "pdfExportError": "PDF export failed",
      "calculationError": "Could not calculate — check your inputs"
    }
    ```

    **fr.json** translations:
    ```json
    "toast": {
      "copySuccess": "Copié dans le presse-papiers",
      "copyError": "Échec de la copie dans le presse-papiers",
      "csvExportSuccess": "Export CSV réussi",
      "csvExportError": "Échec de l'export CSV",
      "pdfExportSuccess": "Export PDF réussi",
      "pdfExportError": "Échec de l'export PDF",
      "calculationError": "Calcul impossible — vérifiez vos données"
    }
    ```

    **de.json** translations:
    ```json
    "toast": {
      "copySuccess": "In die Zwischenablage kopiert",
      "copyError": "Kopieren in die Zwischenablage fehlgeschlagen",
      "csvExportSuccess": "CSV erfolgreich exportiert",
      "csvExportError": "CSV-Export fehlgeschlagen",
      "pdfExportSuccess": "PDF erfolgreich exportiert",
      "pdfExportError": "PDF-Export fehlgeschlagen",
      "calculationError": "Berechnung nicht möglich — bitte Eingaben prüfen"
    }
    ```

    **it.json** translations:
    ```json
    "toast": {
      "copySuccess": "Copiato negli appunti",
      "copyError": "Copia negli appunti non riuscita",
      "csvExportSuccess": "CSV esportato con successo",
      "csvExportError": "Esportazione CSV non riuscita",
      "pdfExportSuccess": "PDF esportato con successo",
      "pdfExportError": "Esportazione PDF non riuscita",
      "calculationError": "Calcolo impossibile — controllare i dati inseriti"
    }
    ```

    After editing, verify JSON validity: run `node -e "JSON.parse(require('fs').readFileSync('src/messages/en.json', 'utf8'))"` for each file.
  </action>
  <verify>
    Run `node -e "JSON.parse(require('fs').readFileSync('src/messages/en.json', 'utf8')); console.log('OK')"` for all 4 files.
    Run `npm run type-check` — zero errors.
  </verify>
  <done>All 4 locale files have common.toast object with 7 keys. JSON is valid.</done>
</task>

</tasks>

<verification>
- `npm install` completes without errors
- `npm run type-check` passes with zero TypeScript errors
- `npm run check:fix` reports zero Biome violations
- `src/app/[locale]/layout.tsx` contains `<Toaster richColors position="bottom-right" />`
- `src/messages/en.json` contains `"common": { ... "toast": { "copySuccess": ... } }`
- All 4 locale JSON files parse without errors
</verification>

<success_criteria>
Three packages installed. Toaster mounted in locale layout (applies to all 169 calculator pages). Seven toast translation keys present in all 4 locale files.
</success_criteria>

<output>
After completion, create `.planning/phases/42-error-boundaries-toasts/42-01-SUMMARY.md` following the summary template.
</output>
