---
phase: 40-vitest-foundation
plan: "04"
type: execute
wave: 3
depends_on:
  - "40-02"
  - "40-03"
files_modified:
  - src/__tests__/lib/converters/network/bb-credit-calculator.test.ts
  - src/__tests__/lib/converters/network/subnet-calculator.test.ts
  - src/__tests__/lib/converters/health/bmi.test.ts
  - src/__tests__/lib/converters/finance/compound-interest.test.ts
  - src/__tests__/lib/converters/chemistry/molecular-weight.test.ts
autonomous: true
requirements:
  - R1.1
  - R1.2
  - R1.3
  - R1.4
  - R1.5

must_haves:
  truths:
    - "`npm run test:coverage` exits 0 with ≥75% lines, functions, branches, statements on src/lib/converters/**"
    - "All 5 test files pass with zero failures"
    - "Coverage report shows the 5 priority converter files are covered"
    - "No TypeScript errors in any test file"
  artifacts:
    - path: "src/__tests__/lib/converters/network/bb-credit-calculator.test.ts"
      provides: "Verified passing tests at ≥75% coverage"
      exports: []
    - path: "src/__tests__/lib/converters/network/subnet-calculator.test.ts"
      provides: "Verified passing tests at ≥75% coverage"
      exports: []
    - path: "src/__tests__/lib/converters/health/bmi.test.ts"
      provides: "Verified passing tests at ≥75% coverage"
      exports: []
    - path: "src/__tests__/lib/converters/finance/compound-interest.test.ts"
      provides: "Verified passing tests at ≥75% coverage"
      exports: []
    - path: "src/__tests__/lib/converters/chemistry/molecular-weight.test.ts"
      provides: "Verified passing tests at ≥75% coverage"
      exports: []
  key_links:
    - from: "npm run test:coverage"
      to: "vitest.config.ts coverage thresholds"
      via: "@vitest/coverage-v8 provider"
      pattern: "thresholds.*75"
---

<objective>
Run full coverage report for the 5 priority converter test files, verify all coverage thresholds at ≥75%, and fix any gaps. This plan is the acceptance gate for Phase 40.

Purpose: Confirms Vitest is correctly configured end-to-end — tests run, coverage is measured, thresholds enforce the 75% minimum. If any converter falls below 75%, add targeted tests to close the gap.
Output: Clean `npm run test:coverage` run with all thresholds satisfied.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-vitest-foundation/40-RESEARCH.md
@.planning/phases/40-vitest-foundation/40-01-SUMMARY.md
@.planning/phases/40-vitest-foundation/40-02-SUMMARY.md
@.planning/phases/40-vitest-foundation/40-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and coverage report</name>
  <files></files>
  <action>
    Run the complete test suite with coverage:

    ```bash
    npm run test:coverage 2>&1
    ```

    This runs `vitest run --coverage` which:
    1. Executes all 5 test files
    2. Generates V8 coverage for `src/lib/converters/**/*.ts`
    3. Checks thresholds: lines ≥75%, functions ≥75%, branches ≥75%, statements ≥75%
    4. Exits non-zero if any threshold fails

    Interpret the output:
    - "All files" row in coverage table — overall percentage for covered files
    - Per-file rows — individual converter coverage
    - "ERROR: Coverage for lines (X%) does not meet threshold (75%)" — means tests need to be added
    - "X passed | Y failed" — test result summary

    If ALL thresholds pass and ALL tests pass: Task is complete.

    If thresholds FAIL: Identify which converters have low coverage from the coverage table. Proceed to add tests in Task 2 to address specific uncovered branches.

    If TESTS FAIL: Diagnose the failure from the error output. Common causes:
    - Wrong field name in assertion (re-read source, adapt test)
    - Wrong category string (re-read source enum/constant values)
    - Wrong function name (re-read source exports)
    Fix the failing test by reading the actual source file and updating the assertion to match.
  </action>
  <verify>
    Run: `npm run test:coverage 2>&1 | tail -20`
    Expected output includes:
    - "X passed" (no failures)
    - Coverage percentages ≥ 75% for lines, functions, branches, statements
    - OR specific threshold violation messages to address in Task 2
  </verify>
  <done>
    Coverage report generated. Test results known. Either all thresholds pass (done) or specific gaps identified for Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix coverage gaps if thresholds not met</name>
  <files>
    src/__tests__/lib/converters/network/bb-credit-calculator.test.ts
    src/__tests__/lib/converters/network/subnet-calculator.test.ts
    src/__tests__/lib/converters/health/bmi.test.ts
    src/__tests__/lib/converters/finance/compound-interest.test.ts
    src/__tests__/lib/converters/chemistry/molecular-weight.test.ts
  </files>
  <action>
    ONLY perform this task if Task 1 revealed coverage gaps below 75%.

    If Task 1 passed all thresholds: This task is a no-op — mark as done immediately.

    For each converter file below threshold:
    1. Read the source file to identify uncovered branches
    2. Look at the coverage report "Uncovered Lines" column for specific line numbers
    3. Add targeted tests to exercise those branches

    Common coverage gaps and how to fix them:

    **bb-credit-calculator.ts:** Check if the Cisco MDS format string generation has conditional branches. Add tests for portIds with different formats (e.g., "1", "1/1", "1/1/1").

    **subnet-calculator.ts:** IPv6 may have untested prefix length edge cases. Test /0, /48, /64, /128 if not already covered. Check if `firstUsable`/`lastUsable` for IPv6 is covered.

    **bmi.ts:** Check if `obese-2` or `obese-3` categories exist in the source but aren't tested. Add tests for extreme values (BMI > 40 for obese-3 if present).

    **compound-interest.ts:** Check if `contributionTiming: 'beginning'` is tested (vs 'end'). If present in source, add a test comparing 'beginning' vs 'end' timing.

    **molecular-weight.ts:** Check if single-character element formulas are covered (e.g., `'C'`, `'N'`). Add simple single-element tests if missing.

    After adding tests, re-run:
    ```bash
    npm run test:coverage 2>&1 | tail -20
    ```

    Repeat until all thresholds pass.
  </action>
  <verify>
    Run: `npm run test:coverage 2>&1`
    Expected: Exit code 0. Output shows all coverage thresholds ≥75%. No "does not meet threshold" errors.

    Final check: `echo "Exit code: $?"`  — must be 0.
  </verify>
  <done>
    `npm run test:coverage` exits 0. All 5 converter files have ≥75% coverage on lines, functions, branches, and statements. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: TypeScript check and Biome lint on test files</name>
  <files></files>
  <action>
    Verify the test files pass TypeScript strict mode and Biome linting.

    Run TypeScript check:
    ```bash
    npm run type-check 2>&1
    ```
    Expected: zero errors. If errors exist in test files, fix them (no `any` types, proper type assertions).

    Run Biome lint:
    ```bash
    npm run check:fix 2>&1
    ```
    Expected: auto-fixes applied cleanly. If unfixable errors remain, fix them manually.

    Common issues to fix if they appear:
    - `let` where `const` should be used → change to `const`
    - Unused imports → remove them
    - Missing return types (only in non-test utility functions) → add return type annotation
    - `any` type usage → use proper type narrowing

    After fixes, verify tests still pass:
    ```bash
    npm run test:run 2>&1 | tail -10
    ```
  </action>
  <verify>
    Run: `npm run type-check 2>&1 | grep -c "error TS"` → must output `0`
    Run: `npm run check 2>&1 | grep -c "error"` → must output `0` (or only pre-existing errors unrelated to test files)
    Run: `npm run test:run 2>&1 | grep -E "passed|failed"` → must show "X passed" with no failures
  </verify>
  <done>
    TypeScript check passes. Biome check passes. All tests pass. Phase 40 acceptance criteria met.
  </done>
</task>

</tasks>

<verification>
Final acceptance gate — run all three checks:

1. `npm run test:coverage 2>&1 | tail -5` — exits 0, ≥75% all thresholds
2. `npm run type-check 2>&1 | tail -5` — exits 0, zero TS errors
3. `npm run check 2>&1 | tail -5` — exits 0, zero Biome errors

Phase 40 is complete when all three pass.
</verification>

<success_criteria>
- `npm run test:coverage` exits 0 with lines/functions/branches/statements all ≥75% on `src/lib/converters/**`
- All 5 priority converter test files pass (zero test failures)
- `npm run type-check` passes (no TypeScript errors in test files)
- `npm run check` passes (no Biome lint errors in test files)
- Phase 40 goal achieved: Vitest running with ≥75% coverage on the 5 priority converter modules
</success_criteria>

<output>
After completion, create `.planning/phases/40-vitest-foundation/40-04-SUMMARY.md` following the summary template.
</output>
