---
phase: 41-full-converter-test-coverage
plan: "10"
type: execute
wave: 4
depends_on:
  - "41-02"
  - "41-03"
  - "41-04"
  - "41-05"
  - "41-06"
  - "41-07"
  - "41-08"
  - "41-09"
files_modified:
  - vitest.config.ts
autonomous: true
requirements:
  - R1.6
  - R1.7
must_haves:
  truths:
    - "npm run test:coverage exits 0 — global 75% threshold passes across all converters"
    - "npm run test:run exits 0 — all tests from plans 41-01 through 41-09 pass"
    - "Total test count exceeds 400 new tests (66 from Phase 40 plus 350+ new)"
    - "npm run type-check exits 0"
    - "npx biome check src/ --diagnostic-level=error exits 0"
    - "All 19 converter categories have at least one test file"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Global 75% coverage threshold"
      contains: "lines: 75"
  key_links:
    - from: "vitest.config.ts"
      to: "src/lib/converters/**/*.ts"
      via: "global threshold enforcement"
      pattern: "thresholds.*lines.*75"
---

<objective>
Run the global coverage suite after all test files have been written (plans 41-02 through 41-09), fix any coverage gaps to reach the global 75% threshold, and verify the complete test suite is clean.

Purpose: This is the only plan that runs `npm run test:coverage`. All Wave 2–3 plans run only `npm run test:run` to avoid failing on 0%-covered files. Wave 4 is the final gate.
Output: Passing global coverage suite, any needed vitest.config.ts adjustments.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-full-converter-test-coverage/41-RESEARCH.md
@.planning/phases/41-full-converter-test-coverage/41-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full coverage suite and identify gaps</name>
  <files>vitest.config.ts</files>
  <action>
Run the full coverage suite with all tests from all Wave 2–3 plans now complete:

```bash
cd /Users/fjacquet/Projects/converty && npm run test:coverage 2>&1 | tail -80
```

**Expected outcome:** `npm run test:coverage` exits 0 with global 75% threshold passing.

**If coverage PASSES (exits 0):** No changes needed. Proceed to Task 2 verification.

**If coverage FAILS with threshold error (lines/functions/branches/statements < 75%):**

1. Read the coverage output to identify which files are below threshold
2. Check if any converter files were missed (not in any test plan)
3. For files with 0% coverage: the test file was likely not created in the correct path — check `src/__tests__/lib/converters/[category]/[file].test.ts`
4. For files with partial coverage (30-70%): add targeted tests to cover missing branches. Common causes:
   - Multiple `switch` cases where only 2-3 were tested
   - Error/null paths not tested
   - Mode variants not covered with it.each
5. If a specific file is structurally difficult to reach 75% (e.g., large lookup tables with rare branches): add it to the vitest.config.ts exclude list with justification comment

**Pitfall from research:** If aggregate coverage is below 75% because a category was entirely missed, add the missing test file rather than raising the threshold.

**CRITICAL:** Do NOT lower the 75% threshold. Do NOT add exclusions without justification.
  </action>
  <verify>
`npm run test:coverage` exits 0. Coverage report shows global percentages:
- Lines: ≥ 75%
- Functions: ≥ 75%
- Branches: ≥ 75%
- Statements: ≥ 75%
  </verify>
  <done>npm run test:coverage exits 0, global 75% threshold satisfied across all included converter files.</done>
</task>

<task type="auto">
  <name>Task 2: Full quality verification</name>
  <files></files>
  <action>
Run the complete quality verification sequence:

1. `npm run test:run` — all tests pass, exit 0
2. `npm run test:coverage` — coverage thresholds met, exit 0
3. `npm run type-check` — zero TypeScript errors, exit 0
4. `npx biome check src/ --diagnostic-level=error` — zero Biome errors, exit 0

If any check fails:
- TypeScript error in a test file: Fix type issue (no `any` allowed; use non-null assertion `result!.field` instead)
- Biome error: Run `npm run check:fix` to auto-fix, then verify again
- Test failure: Debug the specific failing test (read the error, inspect the source file, fix assertion)

Verify test counts:
- Count total tests: `npm run test:run 2>&1 | grep "Tests "`
- Expect: > 400 total tests (66 Phase 40 + ~350+ new)
- Expect: All pass (0 failed)

Verify category coverage:
- `ls src/__tests__/lib/converters/` — should show all 19 category directories
- `ls src/__tests__/lib/converters/math/ | wc -l` — should be 38 (or 39 with index excluded)
  </action>
  <verify>All 4 quality checks exit 0: test:run, test:coverage, type-check, biome check.</verify>
  <done>Full quality gate passes: all tests green, global 75% coverage threshold met, TypeScript clean, Biome clean.</done>
</task>

</tasks>

<verification>
1. `npm run test:run` exits 0 — zero failures
2. `npm run test:coverage` exits 0 — global ≥75% on lines/functions/branches/statements
3. `npm run type-check` exits 0 — zero TS errors
4. `npx biome check src/ --diagnostic-level=error` exits 0 — zero Biome errors
5. Total test count > 400
6. `ls src/__tests__/lib/converters/` shows 19 directories (all categories covered)
</verification>

<success_criteria>
- Global coverage threshold enforced and passing (≥75% lines/functions/branches/statements)
- All tests pass with npm run test:run
- TypeScript and Biome clean
- R1.6 fully satisfied: all converter files have tests at ≥75% aggregate coverage
- R1.7 fully satisfied: static.yml CI gate added in plan 41-01
</success_criteria>

<output>
After completion, create `.planning/phases/41-full-converter-test-coverage/41-10-SUMMARY.md`
</output>
