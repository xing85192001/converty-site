---
phase: 41-full-converter-test-coverage
plan: "02"
type: execute
wave: 2
depends_on:
  - "41-01"
files_modified:
  - src/__tests__/lib/converters/math/area.test.ts
  - src/__tests__/lib/converters/math/average.test.ts
  - src/__tests__/lib/converters/math/basic-calculator.test.ts
  - src/__tests__/lib/converters/math/binary.test.ts
  - src/__tests__/lib/converters/math/circle.test.ts
  - src/__tests__/lib/converters/math/exponent.test.ts
  - src/__tests__/lib/converters/math/factor.test.ts
  - src/__tests__/lib/converters/math/fraction.test.ts
  - src/__tests__/lib/converters/math/gcd-lcm.test.ts
  - src/__tests__/lib/converters/math/half-life.test.ts
  - src/__tests__/lib/converters/math/hex.test.ts
  - src/__tests__/lib/converters/math/logarithm.test.ts
  - src/__tests__/lib/converters/math/percent-error.test.ts
  - src/__tests__/lib/converters/math/percentage.test.ts
  - src/__tests__/lib/converters/math/permutation-combination.test.ts
  - src/__tests__/lib/converters/math/pythagorean.test.ts
  - src/__tests__/lib/converters/math/quadratic.test.ts
  - src/__tests__/lib/converters/math/random-number.test.ts
  - src/__tests__/lib/converters/math/ratio.test.ts
  - src/__tests__/lib/converters/math/root.test.ts
  - src/__tests__/lib/converters/math/rounding.test.ts
  - src/__tests__/lib/converters/math/scientific-notation.test.ts
  - src/__tests__/lib/converters/math/slope.test.ts
  - src/__tests__/lib/converters/math/surface-area.test.ts
  - src/__tests__/lib/converters/math/volume.test.ts
  - src/__tests__/lib/converters/math/z-score.test.ts
autonomous: true
requirements:
  - R1.6
must_haves:
  truths:
    - "26 math test files exist under src/__tests__/lib/converters/math/"
    - "npm run test:run exits 0 with all new math tests passing"
    - "Each test file has at minimum: null/invalid input case, one happy path case, mode coverage via it.each where applicable"
  artifacts:
    - path: "src/__tests__/lib/converters/math/percentage.test.ts"
      provides: "Percentage calculator tests"
      min_lines: 20
    - path: "src/__tests__/lib/converters/math/area.test.ts"
      provides: "Area calculator tests"
      min_lines: 15
    - path: "src/__tests__/lib/converters/math/z-score.test.ts"
      provides: "Z-score tests"
      min_lines: 10
  key_links:
    - from: "src/__tests__/lib/converters/math/*.test.ts"
      to: "src/lib/converters/math/*.ts"
      via: "@/ alias import"
      pattern: "import.*@/lib/converters/math"
---

<objective>
Write unit tests for 26 trivial-to-simple math converters. These are the fastest converters to test (3-6 tests each), establishing baseline math category coverage.

Purpose: Math is the largest category (38 files). This plan covers the 26 simpler files; plan 41-03 covers the 12 more complex ones. Parallel execution with health (41-04) and finance (41-05).
Output: 26 test files in src/__tests__/lib/converters/math/
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-full-converter-test-coverage/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Math trivial converters (area through pythagorean)</name>
  <files>
    src/__tests__/lib/converters/math/area.test.ts
    src/__tests__/lib/converters/math/average.test.ts
    src/__tests__/lib/converters/math/basic-calculator.test.ts
    src/__tests__/lib/converters/math/binary.test.ts
    src/__tests__/lib/converters/math/circle.test.ts
    src/__tests__/lib/converters/math/exponent.test.ts
    src/__tests__/lib/converters/math/factor.test.ts
    src/__tests__/lib/converters/math/fraction.test.ts
    src/__tests__/lib/converters/math/gcd-lcm.test.ts
    src/__tests__/lib/converters/math/half-life.test.ts
    src/__tests__/lib/converters/math/hex.test.ts
    src/__tests__/lib/converters/math/logarithm.test.ts
    src/__tests__/lib/converters/math/percent-error.test.ts
    src/__tests__/lib/converters/math/percentage.test.ts
  </files>
  <action>
Create test files for 14 math converters. All follow the same pattern established in Phase 40. Use `describe`/`it` blocks, import via `@/lib/converters/math/[name]`, use `toBeCloseTo` for floats, `it.each` for mode coverage.

DO NOT use `any` types. Use non-null assertion `result!.field` not `(result as any).field`.
DO NOT run `npm run test:coverage` — only `npm run test:run`.

Read each source file before writing tests to understand the exported function signature and return shape.

**area.test.ts** — import `calculateArea`. Test: null for negative dimensions, circle area from radius, rectangle area, triangle area. Use it.each for shape modes (read source for available modes).

**average.test.ts** — import `calculateAverage`. Test: null for empty array, mean of [1,2,3]=2, median of even-length array, mode of array with repeated value.

**basic-calculator.test.ts** — import the calculate function. Test: addition, subtraction, multiplication, division by zero returns null, division happy path.

**binary.test.ts** — import conversion functions. Test: decimal 10 to binary "1010", decimal to hex, binary to decimal, invalid input returns null.

**circle.test.ts** — import `calculateCircle`. Test: null for negative radius, circumference from radius 5, area from radius 5.

**exponent.test.ts** — import calculate function. Test: 2^10=1024, square root of 9=3, null for invalid (e.g. sqrt of negative).

**factor.test.ts** — import factorization function. Test: factors of 12=[1,2,3,4,6,12], prime number has only 1 and itself, null for 0 or negative.

**fraction.test.ts** — import fraction simplification function. Test: 6/4 simplifies to 3/2, 0/5 is valid, null for denominator 0. Use toBeCloseTo for decimal equivalent.

**gcd-lcm.test.ts** — import gcd and lcm functions. Test: gcd(12,8)=4, lcm(4,6)=12, gcd(0,5) edge case.

**half-life.test.ts** — import `calculateHalfLife`. Test: null for invalid input (negative amount or half-life), after 1 half-life 100g becomes 50g, after 2 half-lives becomes 25g.

**hex.test.ts** — import hex conversion functions. Test: hex "FF" = decimal 255, decimal 255 = hex "FF", hex to binary conversion, invalid hex returns null.

**logarithm.test.ts** — import `calculateLogarithm`. Test: log base 10 of 100 = 2, natural log of e ≈ 1, null for log of 0 or negative.

**percent-error.test.ts** — import calculate function. Test: actual=100 measured=110 gives 10% error, null for actual=0 (division by zero), negative error for underestimate.

**percentage.test.ts** — import `calculatePercentage`. Test: null for percentOf mode when value2=0, 50% of 200=100, it.each over ["percentOf","whatPercent","percentChange","percentDifference"] modes each returns non-null for valid inputs.
  </action>
  <verify>Run `npm run test:run` — all tests in this batch pass. Check for no TypeScript errors with `npm run type-check`.</verify>
  <done>14 math test files exist, all tests pass, no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Math simple converters (permutation through z-score)</name>
  <files>
    src/__tests__/lib/converters/math/permutation-combination.test.ts
    src/__tests__/lib/converters/math/pythagorean.test.ts
    src/__tests__/lib/converters/math/quadratic.test.ts
    src/__tests__/lib/converters/math/random-number.test.ts
    src/__tests__/lib/converters/math/ratio.test.ts
    src/__tests__/lib/converters/math/root.test.ts
    src/__tests__/lib/converters/math/rounding.test.ts
    src/__tests__/lib/converters/math/scientific-notation.test.ts
    src/__tests__/lib/converters/math/slope.test.ts
    src/__tests__/lib/converters/math/surface-area.test.ts
    src/__tests__/lib/converters/math/volume.test.ts
    src/__tests__/lib/converters/math/z-score.test.ts
  </files>
  <action>
Create test files for 12 more math converters. Read each source file first to understand exported functions and signatures.

**permutation-combination.test.ts** — import nPr and nCr functions (or a combined calculate function). Test: P(5,2)=20, C(5,2)=10, null for k>n, null for negative inputs.

**pythagorean.test.ts** — import `calculatePythagorean`. Test: a=3,b=4 → c=5, find missing leg from c=5,a=3→b=4, null for negative inputs.

**quadratic.test.ts** — import `calculateQuadratic`. Test: a=1,b=-5,c=6 → roots x=3,x=2 (discriminant>0), a=1,b=-2,c=1 → one real root x=1 (discriminant=0), a=1,b=0,c=1 → complex roots (discriminant<0, result not null but roots marked complex), null for a=0.

**random-number.test.ts** — import `calculateRandomNumber` or `generateRandom`. Test: min=1,max=10 → result in range [1,10], null for min>max, min=max=5 → result is 5 (edge case). Use `toBeGreaterThanOrEqual` and `toBeLessThanOrEqual`.

**ratio.test.ts** — import ratio simplification function. Test: ratio 6:4 simplifies to 3:2, ratio 1:1 stays 1:1, null for zero denominator.

**root.test.ts** — import `calculateRoot`. Test: nth root where n=2 of 9=3, cube root of 27=3, null for even root of negative number.

**rounding.test.ts** — import rounding function. Test: round 2.567 to 2 decimals=2.57, floor 2.9=2, ceil 2.1=3, truncate -2.9=-2. Use it.each for modes ["round","floor","ceil","truncate"].

**scientific-notation.test.ts** — import convert function. Test: 1234 → "1.234e+3", parse "1.234e+3" → 1234, null for empty string input.

**slope.test.ts** — import `calculateSlope`. Test: slope of (0,0)-(2,4)=2, y-intercept of line through (0,3),(1,5) is 3, null for vertical line (x1=x2).

**surface-area.test.ts** — import `calculateSurfaceArea`. Test: null for negative dimensions, sphere surface area from r=1≈12.57, cube surface area from side=2=24. Use it.each for shape modes.

**volume.test.ts** — import `calculateVolume`. Test: null for negative dimensions, cube volume side=3=27, cylinder volume r=1,h=1≈3.14. Use it.each for shape modes.

**z-score.test.ts** — import `calculateZScore`. Test: (x=75,μ=70,σ=5) → z=1.0, (x=60,μ=70,σ=5) → z=-2.0, null for σ=0.
  </action>
  <verify>Run `npm run test:run` — all 26 math test files pass. No `any` types in test files.</verify>
  <done>26 math test files created and passing under src/__tests__/lib/converters/math/</done>
</task>

</tasks>

<verification>
1. `ls src/__tests__/lib/converters/math/` shows 26 test files
2. `npm run test:run` exits 0, total test count is 66 + new math tests
3. `npm run type-check` exits 0
4. No test file contains `any` type or `console.log`
</verification>

<success_criteria>
- 26 math test files created covering trivial and simple converters
- All tests pass with npm run test:run
- Each converter has at minimum: null input rejection test + one correct calculation test
</success_criteria>

<output>
After completion, create `.planning/phases/41-full-converter-test-coverage/41-02-SUMMARY.md`
</output>
