---
phase: 41-full-converter-test-coverage
plan: "03"
type: execute
wave: 2
depends_on:
  - "41-01"
files_modified:
  - src/__tests__/lib/converters/math/big-number.test.ts
  - src/__tests__/lib/converters/math/confidence-interval.test.ts
  - src/__tests__/lib/converters/math/distance.test.ts
  - src/__tests__/lib/converters/math/long-division.test.ts
  - src/__tests__/lib/converters/math/matrix.test.ts
  - src/__tests__/lib/converters/math/number-sequence.test.ts
  - src/__tests__/lib/converters/math/p-value.test.ts
  - src/__tests__/lib/converters/math/prime-factorization.test.ts
  - src/__tests__/lib/converters/math/probability.test.ts
  - src/__tests__/lib/converters/math/sample-size.test.ts
  - src/__tests__/lib/converters/math/standard-deviation.test.ts
  - src/__tests__/lib/converters/math/statistics.test.ts
  - src/__tests__/lib/converters/math/triangle.test.ts
autonomous: true
requirements:
  - R1.6
must_haves:
  truths:
    - "13 medium-to-complex math test files exist under src/__tests__/lib/converters/math/"
    - "npm run test:run exits 0 including matrix, statistics, and triangle tests"
    - "matrix.test.ts covers add, multiply, determinant, and inverse modes"
  artifacts:
    - path: "src/__tests__/lib/converters/math/matrix.test.ts"
      provides: "Matrix calculator tests"
      min_lines: 40
    - path: "src/__tests__/lib/converters/math/statistics.test.ts"
      provides: "Statistics suite tests"
      min_lines: 30
    - path: "src/__tests__/lib/converters/math/triangle.test.ts"
      provides: "Triangle law of sines/cosines tests"
      min_lines: 20
  key_links:
    - from: "src/__tests__/lib/converters/math/matrix.test.ts"
      to: "src/lib/converters/math/matrix.ts"
      via: "@/ alias import"
      pattern: "import.*@/lib/converters/math/matrix"
---

<objective>
Write unit tests for 13 medium-to-complex math converters including matrix operations, statistics, and triangle calculations. These require more test cases than trivial converters to achieve 75% branch coverage.

Purpose: Completes math category coverage alongside plan 41-02 (parallel Wave 2).
Output: 13 test files in src/__tests__/lib/converters/math/
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-full-converter-test-coverage/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medium math converters (big-number through probability)</name>
  <files>
    src/__tests__/lib/converters/math/big-number.test.ts
    src/__tests__/lib/converters/math/confidence-interval.test.ts
    src/__tests__/lib/converters/math/distance.test.ts
    src/__tests__/lib/converters/math/long-division.test.ts
    src/__tests__/lib/converters/math/matrix.test.ts
    src/__tests__/lib/converters/math/number-sequence.test.ts
    src/__tests__/lib/converters/math/p-value.test.ts
    src/__tests__/lib/converters/math/prime-factorization.test.ts
    src/__tests__/lib/converters/math/probability.test.ts
  </files>
  <action>
Read each source file before writing tests. Create test files targeting ≥75% line coverage per file.

**big-number.test.ts** — import arithmetic functions. Test: large integer addition (e.g., factorial-sized numbers), subtraction, multiplication. Verify result is a string or BigInt (check source). Test null/error for invalid input.

**confidence-interval.test.ts** — import `calculateConfidenceInterval`. Test: null for n=0 or negative, 95% CI for known mean/stdDev/n values, 99% CI with different z-score, verify lower < mean < upper.

**distance.test.ts** — import `calculateDistance`. Test: Euclidean distance (0,0)-(3,4)=5, Manhattan distance (0,0)-(3,4)=7, Haversine distance for two known cities. Use it.each for modes ["euclidean","manhattan","haversine"].

**long-division.test.ts** — import `calculateLongDivision`. Test: 10÷3 returns quotient=3 remainder=1 and steps array, 100÷4=25 remainder=0, null for divisor=0.

**matrix.test.ts** — This is the most complex math converter (recursive, multiple modes). Read source carefully. Test:
- Addition: 2x2 matrix [[1,2],[3,4]] + [[5,6],[7,8]] = [[6,8],[10,12]]
- Multiplication: 2x2 × 2x2 identity = original matrix
- Determinant: det([[1,2],[3,4]]) = -2
- Transpose: [[1,2],[3,4]] → [[1,3],[2,4]]
- Inverse: inv([[2,0],[0,2]]) = [[0.5,0],[0,0.5]]
- Null for dimension mismatch in addition
- Use it.each for modes if function accepts a mode parameter

**number-sequence.test.ts** — import `calculateNumberSequence`. Test: arithmetic sequence first=1,diff=2,n=5=[1,3,5,7,9], geometric sequence first=2,ratio=3,n=4=[2,6,18,54], null for n<=0.

**p-value.test.ts** — import `calculatePValue`. Test: known z-score maps to expected p-value (use toBeCloseTo), null for invalid inputs. Test one-tailed and two-tailed if modes exist.

**prime-factorization.test.ts** — import `calculatePrimeFactorization`. Test: 12=[2,2,3], 7=[7] (prime), 1=[] or special case, null for n<1.

**probability.test.ts** — import probability functions. Test: probability of single event, complement (1-p), combined independent events, null for probability outside [0,1].
  </action>
  <verify>Run `npm run test:run` — all new tests pass. `npm run type-check` exits 0.</verify>
  <done>9 medium math test files created and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Statistics and remaining math converters (sample-size through triangle)</name>
  <files>
    src/__tests__/lib/converters/math/sample-size.test.ts
    src/__tests__/lib/converters/math/standard-deviation.test.ts
    src/__tests__/lib/converters/math/statistics.test.ts
    src/__tests__/lib/converters/math/triangle.test.ts
  </files>
  <action>
Read each source file before writing tests.

**sample-size.test.ts** — import `calculateSampleSize`. Test: confidence=95%,margin=5%,population=large → known result, confidence=99% gives larger sample than 95%, null for margin=0 or invalid confidence level.

**standard-deviation.test.ts** — import `calculateStandardDeviation`. Test: population std dev of [2,4,4,4,5,5,7,9]=2.0, sample std dev of same set≈2.138 (use toBeCloseTo(2.138,2)), null for empty array or n<2 for sample.

**statistics.test.ts** — import `calculateStatistics` (descriptive statistics suite). Test: dataset=[1,2,3,4,5] → mean=3, median=3, mode=null or undefined (all unique), range=4, variance is present and numeric, std dev is present. Test mode with dataset=[1,2,2,3]=2. Test null for empty dataset. Use toBeCloseTo for floating-point fields.

**triangle.test.ts** — import `calculateTriangle`. Test: 3-4-5 right triangle → all sides valid, angle sum=180°, law of cosines: a=7,b=8,C=60° → c computed correctly (use toBeCloseTo). Test null for invalid triangle (triangle inequality violation: a+b<c). If modes exist (SAS, SSS, ASA, AAS), use it.each to cover all.
  </action>
  <verify>Run `npm run test:run` — all 13 medium/complex math tests pass. No `any` in test files.</verify>
  <done>All 13 medium-complex math test files exist and pass. Math category is fully covered (38 files: 26 from plan 41-02 + 12 here + index.ts excluded).</done>
</task>

</tasks>

<verification>
1. `ls src/__tests__/lib/converters/math/` shows 39 test files (26 from 41-02 + 13 from this plan)
2. `npm run test:run` exits 0
3. `npm run type-check` exits 0
4. matrix.test.ts has tests for at least 4 matrix operations
</verification>

<success_criteria>
- 13 medium-complex math test files created
- All tests pass
- matrix.ts, statistics.ts, triangle.ts each have multiple test cases covering key branches
</success_criteria>

<output>
After completion, create `.planning/phases/41-full-converter-test-coverage/41-03-SUMMARY.md`
</output>
