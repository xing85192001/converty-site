---
phase: 41-full-converter-test-coverage
plan: "08"
type: execute
wave: 3
depends_on:
  - "41-01"
files_modified:
  - src/__tests__/lib/converters/network/bandwidth-delay-product.test.ts
  - src/__tests__/lib/converters/network/cidr-range.test.ts
  - src/__tests__/lib/converters/network/ip-classifier.test.ts
  - src/__tests__/lib/converters/network/ip-parser.test.ts
  - src/__tests__/lib/converters/network/latency-converter.test.ts
  - src/__tests__/lib/converters/network/subnetting.test.ts
  - src/__tests__/lib/converters/network/supernetting.test.ts
  - src/__tests__/lib/converters/network/tcp-throughput.test.ts
  - src/__tests__/lib/converters/network/throughput-calculator.test.ts
  - src/__tests__/lib/converters/crypto/exchange-rate.test.ts
  - src/__tests__/lib/converters/crypto/hash.test.ts
  - src/__tests__/lib/converters/crypto/mining-profitability.test.ts
  - src/__tests__/lib/converters/crypto/wallet-validator.test.ts
  - src/__tests__/lib/converters/realestate/mortgage-swiss.test.ts
  - src/__tests__/lib/converters/realestate/property-valuation.test.ts
  - src/__tests__/lib/converters/realestate/rental-yield.test.ts
autonomous: true
requirements:
  - R1.6
must_haves:
  truths:
    - "16 test files exist across network (remaining 7), crypto (4), and realestate (3)"
    - "npm run test:run exits 0 with all new tests passing"
    - "ipaddr.js converters (cidr-range, ip-parser, ip-classifier, subnetting, supernetting) use expect().toThrow() for invalid IP inputs"
    - "wallet-validator.test.ts uses TEST_ADDRESSES exported from the source file"
  artifacts:
    - path: "src/__tests__/lib/converters/network/cidr-range.test.ts"
      provides: "CIDR range tests with ipaddr.js throw pattern"
      min_lines: 15
    - path: "src/__tests__/lib/converters/crypto/wallet-validator.test.ts"
      provides: "Wallet address validation tests"
      min_lines: 15
    - path: "src/__tests__/lib/converters/realestate/mortgage-swiss.test.ts"
      provides: "Swiss mortgage tests"
      min_lines: 15
  key_links:
    - from: "src/__tests__/lib/converters/network/*.test.ts"
      to: "src/lib/converters/network/*.ts"
      via: "@/ alias import"
      pattern: "import.*@/lib/converters/network"
    - from: "src/__tests__/lib/converters/crypto/wallet-validator.test.ts"
      to: "src/lib/converters/crypto/wallet-validator.ts"
      via: "TEST_ADDRESSES import"
      pattern: "TEST_ADDRESSES"
---

<objective>
Write unit tests for the remaining 7 network converters (bb-credit and subnet already done), 4 crypto converters, and 3 realestate converters — 14 new test files total.

Purpose: Completes three categories in one parallel Wave 3 plan. ipaddr.js-based network converters require specific throw pattern. Crypto wallet validator uses exported TEST_ADDRESSES constant.
Output: 16 test files across network (partial), crypto, and realestate categories.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-full-converter-test-coverage/41-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Network remaining converters (7 files)</name>
  <files>
    src/__tests__/lib/converters/network/bandwidth-delay-product.test.ts
    src/__tests__/lib/converters/network/cidr-range.test.ts
    src/__tests__/lib/converters/network/ip-classifier.test.ts
    src/__tests__/lib/converters/network/ip-parser.test.ts
    src/__tests__/lib/converters/network/latency-converter.test.ts
    src/__tests__/lib/converters/network/subnetting.test.ts
    src/__tests__/lib/converters/network/supernetting.test.ts
    src/__tests__/lib/converters/network/tcp-throughput.test.ts
    src/__tests__/lib/converters/network/throughput-calculator.test.ts
  </files>
  <action>
Read each source file before writing tests.

CRITICAL PATTERN: ipaddr.js throws for invalid IPs (does not return null). From STATE.md Phase 40-02: for converters using ipaddr.js (cidr-range, ip-classifier, ip-parser, subnetting, supernetting), use `expect(() => fn(badInput)).toThrow()` for invalid IP test cases — NOT `expect(fn(badInput)).toBeNull()`.

**bandwidth-delay-product.test.ts** — BDP = bandwidth × RTT. Test: null for bandwidth=0 or rtt=0, 1 Gbps × 100ms RTT → BDP = 100Mb = 12.5MB (toBeCloseTo), larger bandwidth → larger BDP.

**cidr-range.test.ts** — IP range from CIDR, uses ipaddr.js. Test:
- `expect(() => calculateCidrRange({ cidr: "not-valid" })).toThrow()` (invalid input)
- `calculateCidrRange({ cidr: "192.168.1.0/24" })` → not null, has networkAddress, broadcastAddress, firstHost, lastHost
- /24 → 254 hosts, /25 → 126 hosts
- IPv6 CIDR if supported

**ip-classifier.test.ts** — Private/public/loopback classification, uses ipaddr.js. Test:
- `expect(() => classify("not-an-ip")).toThrow()` (invalid)
- "192.168.1.1" → private
- "8.8.8.8" → public
- "127.0.0.1" → loopback
- "10.0.0.1" → private (Class A)

**ip-parser.test.ts** — Parse IPv4/IPv6, uses ipaddr.js. Test:
- `expect(() => parseIp("garbage")).toThrow()` (invalid)
- "192.168.1.1" → parsed with version=4, octets array
- "::1" → IPv6 loopback, version=6
- "2001:db8::1" → valid IPv6

**latency-converter.test.ts** — ms → μs → ns conversions. Test: 1ms = 1000μs = 1,000,000ns, 1s = 1000ms, null for negative latency.

**subnetting.test.ts** — VLSM subnetting, uses ipaddr.js. Test:
- `expect(() => calculateSubnetting({ network: "invalid" })).toThrow()`
- "192.168.1.0/24" subnets correctly
- Subnet count matches expected for given prefix length

**supernetting.test.ts** — Supernet calculation, uses ipaddr.js. Test:
- `expect(() => calculateSupernet({ networks: ["invalid"] })).toThrow()`
- Combining "192.168.0.0/24" and "192.168.1.0/24" → supernet "192.168.0.0/23"
- Non-contiguous networks → error or specific result (check source)

**tcp-throughput.test.ts** — TCP throughput formula. Test: null for rtt=0, window_size=65535 bytes rtt=100ms → throughput ≈ 5.24 Mbps (toBeCloseTo,1), larger window → higher throughput.

**throughput-calculator.test.ts** — Various throughput calculations. Test: null for invalid inputs, read source for available modes (bandwidth, protocol overhead, etc.). Test at least 2 calculation scenarios.
  </action>
  <verify>Run `npm run test:run` — 9 network tests pass. Grep for `.toThrow()` in cidr-range, ip-classifier, ip-parser, subnetting, supernetting tests — must be present.</verify>
  <done>9 network test files created and passing with correct ipaddr.js throw pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Crypto and real estate converters</name>
  <files>
    src/__tests__/lib/converters/crypto/exchange-rate.test.ts
    src/__tests__/lib/converters/crypto/hash.test.ts
    src/__tests__/lib/converters/crypto/mining-profitability.test.ts
    src/__tests__/lib/converters/crypto/wallet-validator.test.ts
    src/__tests__/lib/converters/realestate/mortgage-swiss.test.ts
    src/__tests__/lib/converters/realestate/property-valuation.test.ts
    src/__tests__/lib/converters/realestate/rental-yield.test.ts
  </files>
  <action>
Read each source file before writing tests.

**CRITICAL for mining-profitability.test.ts:** First inspect `src/lib/converters/crypto/mining-profitability.ts` to check whether it imports a build-time JSON file or accepts prices as function parameters. If it imports JSON: test RATIO INVARIANTS only (e.g., "doubling hashrate doubles profitability"), NOT exact dollar amounts. If it accepts price as parameter: test with known input values.

**CRITICAL for wallet-validator.test.ts:** The source file exports `TEST_ADDRESSES` constant. Import it directly instead of hardcoding wallet addresses.

**exchange-rate.test.ts** — Rate multiplication with static rates. Test: null for unknown currency, BTC to USD at known static rate → result is rate × amount, convert 1.0 → same as rate, USD to BTC inverse of BTC to USD (within float precision using toBeCloseTo).

**hash.test.ts** — Pure hash utilities. Test: null for empty string (if applicable), same input → same hash output (deterministic), different inputs → different hashes, read source for exported functions (may be SHA256, MD5, or custom hash utilities).

**mining-profitability.test.ts** — Read source first (REQUIRED before writing). If imports JSON: test invariants like `result with hashrate=2x > result with hashrate=x`, `result with difficulty=2x < result with difficulty=x`. If accepts prices: test with specific inputs. Never assert exact dollar/coin amounts that depend on live data.

**wallet-validator.test.ts** — Uses `wallet-address-validator` npm package. Import TEST_ADDRESSES from the source:
```typescript
import { validateWalletAddress, TEST_ADDRESSES } from "@/lib/converters/crypto/wallet-validator";
```
Test: `validateWalletAddress(TEST_ADDRESSES.BTC.valid.P2PKH, "BTC")` → `isValid: true`, invalid BTC address → `isValid: false`, ETH valid address → valid (if TEST_ADDRESSES includes ETH), unknown currency → result with isValid: false.

**mortgage-swiss.test.ts** — Swiss mortgage with amortization rules. Test: null for price=0, CHF 800,000 property at 80% LTV → mortgage CHF 640,000, Swiss affordability check (annual cost must be <33% of income), amortization schedule exists. Use toBeCloseTo for monetary values.

**property-valuation.test.ts** — Cap rate, GRM. Test: null for NOI=0 (cap rate), cap rate: NOI $60,000 / property value $600,000 = 10%, GRM: price $300,000 / annual rent $18,000 = 16.67 (toBeCloseTo,1), higher NOI → higher cap rate.

**rental-yield.test.ts** — Annual rent / property value. Test: null for property value=0, $18,000 annual rent / $300,000 value = 6% yield, gross vs net yield if both present (net yield < gross yield due to expenses).
  </action>
  <verify>Run `npm run test:run` — all 16 test files pass. wallet-validator.test.ts uses TEST_ADDRESSES import. mining-profitability.test.ts uses invariants not hardcoded dollar amounts (if source imports JSON).</verify>
  <done>16 test files across network/crypto/realestate created and passing.</done>
</task>

</tasks>

<verification>
1. `ls src/__tests__/lib/converters/network/` shows 11 test files (9 new + bb-credit + subnet from Phase 40)
2. `ls src/__tests__/lib/converters/crypto/` shows 4 test files
3. `ls src/__tests__/lib/converters/realestate/` shows 3 test files
4. `npm run test:run` exits 0
5. ipaddr.js converters use .toThrow() for invalid IPs
6. wallet-validator imports TEST_ADDRESSES
</verification>

<success_criteria>
- 16 test files across 3 categories created
- All pass with npm run test:run
- ipaddr.js throw pattern used correctly for all 5 ipaddr-based converters
- wallet-validator uses exported TEST_ADDRESSES
</success_criteria>

<output>
After completion, create `.planning/phases/41-full-converter-test-coverage/41-08-SUMMARY.md`
</output>
