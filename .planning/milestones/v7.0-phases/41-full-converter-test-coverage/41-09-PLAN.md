---
phase: 41-full-converter-test-coverage
plan: "09"
type: execute
wave: 3
depends_on:
  - "41-01"
files_modified:
  - src/__tests__/lib/converters/chemistry/dilution.test.ts
  - src/__tests__/lib/converters/chemistry/equation-parser.test.ts
  - src/__tests__/lib/converters/chemistry/formula-parser.test.ts
  - src/__tests__/lib/converters/chemistry/molarity.test.ts
  - src/__tests__/lib/converters/chemistry/periodic-table-lookup.test.ts
  - src/__tests__/lib/converters/chemistry/ph-calculator.test.ts
  - src/__tests__/lib/converters/chemistry/stoichiometry.test.ts
  - src/__tests__/lib/converters/engineering/beam-deflection.test.ts
  - src/__tests__/lib/converters/engineering/column-buckling.test.ts
  - src/__tests__/lib/converters/engineering/moment-of-inertia.test.ts
  - src/__tests__/lib/converters/engineering/pipe-flow.test.ts
  - src/__tests__/lib/converters/engineering/stress-strain.test.ts
  - src/__tests__/lib/converters/engineering/unit-converter.test.ts
  - src/__tests__/lib/converters/infrastructure/cpu-comparison.test.ts
  - src/__tests__/lib/converters/infrastructure/hyperv-consolidation.test.ts
  - src/__tests__/lib/converters/infrastructure/hypervisor-comparison.test.ts
  - src/__tests__/lib/converters/infrastructure/k8s-capacity.test.ts
  - src/__tests__/lib/converters/infrastructure/server-refresh.test.ts
  - src/__tests__/lib/converters/infrastructure/server-virtualization.test.ts
  - src/__tests__/lib/converters/infrastructure/virtualization-cost.test.ts
  - src/__tests__/lib/converters/infrastructure/vm-storage.test.ts
  - src/__tests__/lib/converters/infrastructure/vmware-licensing.test.ts
  - src/__tests__/lib/converters/infrastructure/windows-licensing.test.ts
autonomous: true
requirements:
  - R1.6
must_haves:
  truths:
    - "23 test files exist across chemistry (6 new), engineering (6), and infrastructure (10)"
    - "npm run test:run exits 0 with all new tests passing"
    - "formula-parser.test.ts and equation-parser.test.ts use result.success discriminant"
    - "cpu-comparison.test.ts and server-refresh.test.ts use real cpu-database.json (no mocking)"
    - "periodic-table-lookup.test.ts and ph-calculator.test.ts use real JSON data (no mocking)"
  artifacts:
    - path: "src/__tests__/lib/converters/chemistry/formula-parser.test.ts"
      provides: "Chemical formula recursive parser tests"
      min_lines: 20
    - path: "src/__tests__/lib/converters/infrastructure/cpu-comparison.test.ts"
      provides: "CPU comparison with real database tests"
      min_lines: 20
    - path: "src/__tests__/lib/converters/engineering/beam-deflection.test.ts"
      provides: "Structural beam deflection tests"
      min_lines: 20
  key_links:
    - from: "src/__tests__/lib/converters/chemistry/*.test.ts"
      to: "src/lib/converters/chemistry/*.ts"
      via: "@/ alias import"
      pattern: "import.*@/lib/converters/chemistry"
    - from: "src/__tests__/lib/converters/infrastructure/cpu-comparison.test.ts"
      to: "src/lib/data/cpu-database.json"
      via: "real data (no mock)"
      pattern: "getFilteredCpus"
---

<objective>
Write unit tests for chemistry (6 remaining files), engineering (6 files), and infrastructure (10 files) — 22 new test files. These are the most complex categories: recursive parsers, structural engineering formulas, and data-importing infrastructure converters.

Purpose: Completes the final parallel Wave 3 batch. These converters require 8-12 tests each due to complexity.
Output: 23 test files across 3 categories.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/41-full-converter-test-coverage/41-RESEARCH.md
@.planning/phases/40-vitest-foundation/40-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chemistry and engineering converters</name>
  <files>
    src/__tests__/lib/converters/chemistry/dilution.test.ts
    src/__tests__/lib/converters/chemistry/equation-parser.test.ts
    src/__tests__/lib/converters/chemistry/formula-parser.test.ts
    src/__tests__/lib/converters/chemistry/molarity.test.ts
    src/__tests__/lib/converters/chemistry/periodic-table-lookup.test.ts
    src/__tests__/lib/converters/chemistry/ph-calculator.test.ts
    src/__tests__/lib/converters/chemistry/stoichiometry.test.ts
    src/__tests__/lib/converters/engineering/beam-deflection.test.ts
    src/__tests__/lib/converters/engineering/column-buckling.test.ts
    src/__tests__/lib/converters/engineering/moment-of-inertia.test.ts
    src/__tests__/lib/converters/engineering/pipe-flow.test.ts
    src/__tests__/lib/converters/engineering/stress-strain.test.ts
    src/__tests__/lib/converters/engineering/unit-converter.test.ts
  </files>
  <action>
Read each source file before writing tests. Use real JSON data (periodic-table.json, acids-bases.json) — no mocking required (vite-tsconfig-paths resolves @/ aliases in tests, same as Phase 40).

**Chemistry (6 remaining files after molecular-weight.ts):**

**dilution.test.ts** — C1V1=C2V2. Test: null for V2=0 (division by zero), C1=1M V1=10mL C2=? V2=100mL → C2=0.1M (toBeCloseTo,2), solve for V1 given other 3 values.

**formula-parser.test.ts** — Recursive descent parser. From research Pattern 9:
```typescript
import { parseChemicalFormula } from "@/lib/converters/chemistry/formula-parser";
it("parses H2O", () => {
  const result = parseChemicalFormula("H2O");
  expect(result.success).toBe(true);
  if (result.success) {
    expect(result.composition.H).toBe(2);
    expect(result.composition.O).toBe(1);
  }
});
```
Test: H2O → H:2 O:1, C6H12O6 → C:6 H:12 O:6, Ca(OH)2 → Ca:1 O:2 H:2 (parentheses), unmatched "Ca(OH2" → success:false, empty string → success:false.

**equation-parser.test.ts** — Chemical equation balancing. Test: null or error for empty string, simple equation "H2 + O2 -> H2O" parses without throwing, balanced equation has equal atoms on both sides, already-balanced equation returns balanced status.

**molarity.test.ts** — n/V, M=mol/L. Test: null for volume=0, 0.5 mol in 0.5L → 1.0 M, 2 mol in 4L → 0.5 M, verify n from M×V calculation.

**periodic-table-lookup.test.ts** — Lookup wrapper for periodic-table.json (real data, no mock). Test: lookup("H") → hydrogen with atomicNumber=1, lookup("Au") → gold, lookup("Uus") or unknown symbol → null, lookup("Fe") → iron atomicMass ≈ 55.845 (toBeCloseTo,2).

**ph-calculator.test.ts** — 8 modes, imports acids-bases.json (real data). Test: null for concentration=0, strong acid HCl 0.01M → pH=2.0, strong base NaOH 0.01M → pH=12.0, pure water → pH=7.0. Use it.each for available acid/base modes if function accepts mode param.

**stoichiometry.test.ts** — Multi-step stoichiometry using formula-parser. Test: null for empty reactant, given 1 mol H2 in "2H2 + O2 → 2H2O" → yields 1 mol H2O, limiting reagent detection when reactants are imbalanced.

**Engineering (6 files):**

**beam-deflection.test.ts** — Structural beam formulas (12KB, many cases). Test: null for length=0 or E=0, simply supported beam uniform load → max deflection at midspan (δ = 5wL⁴/384EI), point load at midspan → δ = PL³/48EI. Use toBeCloseTo(value, 4) for structural precision. Use it.each for beam type modes (simply-supported, cantilever, fixed-fixed, etc.) — read source for available types.

**column-buckling.test.ts** — Euler's critical load. Test: null for length=0 or I=0, Pcr = π²EI/(KL)² for known material/cross-section, longer column → lower Pcr (more buckling risk), use toBeCloseTo.

**moment-of-inertia.test.ts** — Cross-section inertia formulas (12KB, many shapes). Test: rectangular section b=100mm h=200mm → I = bh³/12 = 66,666,667 mm⁴ (toBeCloseTo,0), circular section r=50mm → I = πr⁴/4, use it.each for shapes (rectangle, circle, hollow, I-beam, etc.).

**pipe-flow.test.ts** — Reynolds number, Darcy-Weisbach. Test: null for diameter=0 or velocity=0, Re = ρvD/μ for water at standard conditions → known Re value, laminar if Re<2300, turbulent if Re>4000, Darcy-Weisbach pressure drop formula.

**stress-strain.test.ts** — Hooke's law, stress/strain. Test: null for cross-section=0, stress = F/A (1000N / 0.01m² = 100,000 Pa), strain = δ/L, E = stress/strain (Young's modulus). Use toBeCloseTo for engineering precision.

**unit-converter.test.ts** — Unit conversion tables. Test: 1 meter = 3.28084 feet (toBeCloseTo,4), 1 kg = 2.20462 lbs (toBeCloseTo,4), 1 atm = 101325 Pa, use it.each for unit category/conversion pairs.
  </action>
  <verify>Run `npm run test:run` — 13 chemistry and engineering tests pass. formula-parser.test.ts uses result.success discriminant.</verify>
  <done>13 chemistry and engineering test files created and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Infrastructure converters (10 files)</name>
  <files>
    src/__tests__/lib/converters/infrastructure/cpu-comparison.test.ts
    src/__tests__/lib/converters/infrastructure/hyperv-consolidation.test.ts
    src/__tests__/lib/converters/infrastructure/hypervisor-comparison.test.ts
    src/__tests__/lib/converters/infrastructure/k8s-capacity.test.ts
    src/__tests__/lib/converters/infrastructure/server-refresh.test.ts
    src/__tests__/lib/converters/infrastructure/server-virtualization.test.ts
    src/__tests__/lib/converters/infrastructure/virtualization-cost.test.ts
    src/__tests__/lib/converters/infrastructure/vm-storage.test.ts
    src/__tests__/lib/converters/infrastructure/vmware-licensing.test.ts
    src/__tests__/lib/converters/infrastructure/windows-licensing.test.ts
  </files>
  <action>
Read each source file before writing tests. Infrastructure converters importing cpu-database.json need NO mocking — real JSON loads via vite-tsconfig-paths (same as molecular-weight.ts in Phase 40).

From research Code Example for cpu-comparison.ts:
```typescript
import { calculateCpuComparison, getFilteredCpus } from "@/lib/converters/infrastructure/cpu-comparison";
describe("calculateCpuComparison", () => {
  it("returns null for fewer than 2 CPU IDs", () => {
    expect(calculateCpuComparison({ cpuIds: "intel-xeon-platinum-8592plus", vendor: "all", generation: "all" })).toBeNull();
  });
  it("returns comparison for 2 valid CPU IDs", () => {
    const result = calculateCpuComparison({ cpuIds: "intel-xeon-platinum-8592plus,amd-epyc-9654", vendor: "all", generation: "all" });
    expect(result).not.toBeNull();
    expect(result!.rows).toHaveLength(2);
    expect(result!.rows[0].sizingRatioVsFirst).toBe(1.0);
  });
});
```

**cpu-comparison.test.ts** — Use the example above. Also test: getFilteredCpus("all","all") returns array.length > 0, getFilteredCpus("intel","all") returns only intel CPUs, filter by generation works.

**hyperv-consolidation.test.ts** — VM consolidation arithmetic. Test: null for hosts=0 or vmsPerHost=0, 10 physical hosts at 8 VMs each = 80 VMs total, consolidation ratio calculation, memory/CPU utilization percentage.

**hypervisor-comparison.test.ts** — Side-by-side hypervisor metrics. Test: null for empty inputs, comparison with VMware vs Hyper-V inputs returns rows for each, metrics include licensing cost per VM or similar field. Read source for exact input/output structure.

**k8s-capacity.test.ts** — Pod/node capacity planning. Test: null for nodes=0, cluster with 10 nodes × 8 CPU / 0.1 CPU per pod = 800 pods max, memory constraint applies correctly, read source for input interface.

**server-refresh.test.ts** — Imports cpu-database.json (no mock). Test: null for invalid current CPU ID, valid refresh scenario returns savings/performance fields, old CPU vs new CPU shows performance improvement ratio. Use CPU IDs from the real database (read cpu-comparison.test.ts above for example IDs).

**server-virtualization.test.ts** — VMs per host calculation. Test: null for host CPU=0, 32 vCPUs at 4:1 overcommit ratio = 128 vCPUs available, memory constraint limits VM count below CPU limit.

**virtualization-cost.test.ts** — Licensing + hardware cost. Test: null for host count=0, cost per VM calculation includes licensing, hardware cost distributes across VMs, TCO over 5 years.

**vm-storage.test.ts** — IOPS and capacity planning. Test: null for VMs=0 or IOPS per VM=0, 50 VMs × 100 IOPS each = 5000 IOPS total, capacity requirement: 50 VMs × 50GB = 2500GB total. Read source for field names.

**vmware-licensing.test.ts** — vSphere licensing math. Test: null for hosts=0, Standard license per socket calculation, Enterprise Plus calculation different from Standard, verify socket count × price = total cost.

**windows-licensing.test.ts** — Windows Server license calculation. Test: null for cores=0, Standard: 1 VM requires 1 license, Datacenter: unlimited VMs per server, OEM vs retail licensing if modes exist.
  </action>
  <verify>Run `npm run test:run` — all 23 test files in this plan pass. cpu-comparison.test.ts and server-refresh.test.ts load real JSON without mocking. `npm run type-check` exits 0.</verify>
  <done>23 test files across chemistry/engineering/infrastructure created and passing. All categories fully covered.</done>
</task>

</tasks>

<verification>
1. `ls src/__tests__/lib/converters/chemistry/` shows 7 test files (6 new + molecular-weight from Phase 40)
2. `ls src/__tests__/lib/converters/engineering/` shows 6 test files
3. `ls src/__tests__/lib/converters/infrastructure/` shows 10 test files
4. `npm run test:run` exits 0
5. `npm run type-check` exits 0
6. No vi.mock() calls in test files (real JSON data used)
</verification>

<success_criteria>
- 23 test files across 3 categories created
- All pass with npm run test:run
- No mocking of JSON data files
- formula-parser, equation-parser use success discriminant
- infrastructure tests use real cpu-database.json entries
</success_criteria>

<output>
After completion, create `.planning/phases/41-full-converter-test-coverage/41-09-SUMMARY.md`
</output>
