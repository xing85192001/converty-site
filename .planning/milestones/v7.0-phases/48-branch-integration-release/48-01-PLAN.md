---
phase: 48-branch-integration-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - R8.1
must_haves:
  truths:
    - "npm run type-check exits with code 0 (zero TypeScript errors)"
    - "npm run test:run exits with code 0 (all 2288+ tests pass)"
    - "npm run build completes with zero errors and zero MISSING_MESSAGE warnings"
    - "npm run check:fix exits with code 0 (Biome clean)"
    - "CHANGELOG.md [7.0.0] section is complete with all phases 40-48"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Complete v7.0 changelog entry ready for release"
      contains: "[7.0.0]"
  key_links:
    - from: "npm run type-check"
      to: "TypeScript compiler"
      via: "tsc --noEmit"
      pattern: "0 errors"
    - from: "npm run test:run"
      to: "Vitest runner"
      via: "vitest run"
      pattern: "Tests.*passed"
    - from: "npm run build"
      to: "next build"
      via: "static export"
      pattern: "Route.*pages"
---

<objective>
Run the final regression suite across the entire v7.0 codebase and ensure the CHANGELOG is complete before tagging.

Purpose: Confirm that all 9 implementation phases (40-47) left the codebase in a clean, buildable, fully-tested state before the tag is cut. A broken release is irreversible.

Output: Green CI-equivalent (type-check + tests + build + lint), complete CHANGELOG.md [7.0.0] entry.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression suite</name>
  <files>CHANGELOG.md</files>
  <action>
    Run the following commands in sequence. If any command fails, diagnose and fix before proceeding to the next. Do NOT proceed to plan 48-02 if any command fails.

    Step 1 — TypeScript check:
    ```
    npm run type-check
    ```
    Expected: exits 0, zero errors reported.

    Step 2 — Vitest full suite (non-interactive):
    ```
    npm run test:run
    ```
    Expected: exits 0, all tests pass (2288+ tests). If failures exist, investigate and fix the failing test or the converter function.

    Step 3 — Production build:
    ```
    npm run build
    ```
    Expected: exits 0, zero MISSING_MESSAGE warnings in build output. If MISSING_MESSAGE appears, identify the missing i18n key and add it to all 4 locale files (en, fr, de, it) under the correct namespace.

    Step 4 — Biome lint/format:
    ```
    npm run check:fix
    ```
    Expected: exits 0 after auto-fixing. If any error cannot be auto-fixed, fix it manually.

    Step 5 — Update CHANGELOG.md:
    Open CHANGELOG.md and complete the [7.0.0] section. The section currently covers phases 40-43. Add the following phases if they are not already present:

    **[Phase 44] LZ-String URL Compression (2026-02-26)**
    - `lz-string@1.5.0` added for URL state compression
    - URL sync middleware writes compressed state as `?z=` param (search params, not hash — GitHub Pages compatible)
    - Backward-compatible dual-path read: `?z=` (compressed) or legacy per-key params
    - Prototype pollution prevention: only keys matching `initialValues` accepted from parsed JSON
    - Null-safety guard before JSON.parse on decompressed string
    - Round-trip lossless compression verified (compress → decompress = original state)

    **[Phase 45] Discriminated Union Result Types (2026-02-26)**
    - `CalculationResult<T>` type: `{ ok: true; value: T } | { ok: false; error: string; code: string }`
    - Adapter pattern: `result: R | null` kept in `CalculatorState` for component backward compat; `CalculationResult<R>` unwrapped inside `setValue`/`setValues`
    - `calculationError: string | undefined` added to `CalculatorState` — undefined on success, populated on `ok: false`
    - All 91 store-based calculator components updated to render `calculationError` beneath inputs
    - All 196 test files updated to use `.ok` discriminant assertions

    **[Phase 46] i18n Namespace Restructure (2026-02-26)**
    - All 4 locale JSON files (en, fr, de, it) restructured from 8 inconsistent top-level keys to stable 4-key schema: `common`, `nav`, `converter`, `calculator`
    - Migration script committed for audit trail
    - All ~210 source files updated: `converters` → `converter`, `categories` → `nav`
    - Build produces zero `MISSING_MESSAGE` warnings after migration
    - ADR-012 documents the i18n namespace rationale

    **[Phase 47] ADRs & CI Hardening (2026-02-26)**
    - ADR-011: Vitest test strategy (node environment, v8 coverage, 75% global threshold)
    - ADR-013: LZ-String URL compression (search params vs hash rationale)
    - ADR-014: Zod validation layer architecture (string-based schemas, opt-in per store)
    - ADR-015: Discriminated union result types (adapter pattern, no breaking changes)
    - CI `static.yml` hardened: type-check gate added alongside existing test gate
    - `npm run test:run` script documented as the CI-safe alias (non-interactive)

    Also update the [7.0.0] date line from "In Progress (2026-02-26)" to "2026-02-26" once all checks pass.

    Move the [Unreleased] section above [7.0.0] and clear its content (keeping the headings Added/Changed/Fixed empty).
  </action>
  <verify>
    - `npm run type-check` exits 0
    - `npm run test:run` exits 0 with passing count ≥ 2288
    - `npm run build` exits 0 with zero MISSING_MESSAGE lines
    - `npm run check:fix` exits 0
    - `CHANGELOG.md` contains phases 40-47 entries and `[7.0.0] - 2026-02-26` header
  </verify>
  <done>All four quality gates green. CHANGELOG.md [7.0.0] section complete. Codebase ready to tag.</done>
</task>

</tasks>

<verification>
Final state check:
```
npm run type-check && npm run test:run && echo "REGRESSION PASS"
grep "7.0.0" CHANGELOG.md
```
Both commands must succeed before proceeding to plan 48-02.
</verification>

<success_criteria>
- TypeScript: 0 errors
- Tests: ≥2288 passing, 0 failing
- Build: exits 0, zero MISSING_MESSAGE
- Biome: exits 0
- CHANGELOG.md: [7.0.0] section complete with phases 40-47
</success_criteria>

<output>
After completion, create `.planning/phases/48-branch-integration-release/48-01-SUMMARY.md` following the standard summary template.
</output>
