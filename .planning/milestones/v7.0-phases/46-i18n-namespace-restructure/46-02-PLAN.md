---
phase: 46-i18n-namespace-restructure
plan: "02"
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - src/app/[locale]/**/*.tsx
  - src/app/[locale]/**/*.ts
  - src/components/**/*.tsx
  - src/components/**/*.ts
autonomous: true
requirements: [R6.2]

must_haves:
  truths:
    - "No source file contains useTranslations('converters') or useTranslations('categories') or useTranslations('subcategories')"
    - "No source file contains getTranslations('converters') or getTranslations('categories')"
    - "No source file references getTranslations with namespace 'metadata' or 'realestate.property-valuation'"
    - "All 'converter.*' namespace strings match actual keys in the restructured en.json"
    - "All 'nav.*' namespace strings match actual keys in the restructured en.json"
    - "common.validation.* and common.metadata.* references resolve correctly in next-intl"
  artifacts:
    - path: "src/app/[locale]/health/page.tsx"
      provides: "Example category page using nav namespace"
      contains: '"nav.health"'
    - path: "src/app/[locale]/health/bmi/page.tsx"
      provides: "Example calculator page using converter namespace"
      contains: '"converter.bmi"'
    - path: "src/components/layout/header.tsx"
      provides: "Header using nav namespace for categories"
      contains: '"nav"'
    - path: "src/components/converter/subcategory-nav.tsx"
      provides: "Subcategory nav using nav.subcategories"
      contains: '"nav.subcategories"'
  key_links:
    - from: "src/app/[locale]/[category]/page.tsx"
      to: "src/messages/en.json nav.[category]"
      via: "getTranslations namespace"
      pattern: "getTranslations.*nav\\."
    - from: "src/app/[locale]/[category]/[calculator]/page.tsx"
      to: "src/messages/en.json converter.[calculator-id]"
      via: "getTranslations namespace"
      pattern: "getTranslations.*converter\\."
---

<objective>
Update all source code files to use the new namespace strings matching the restructured locale JSON files from Plan 01.

Purpose: Plan 01 renamed `converters` → `converter` and `categories` → `nav` (with `subcategories` nested) in the JSON files. This plan updates all TypeScript/TSX source files that reference those old namespace strings, restoring next-intl resolution for all 4293 keys.

Output: ~210 source files updated with corrected namespace strings. Build passes. Zero MISSING_MESSAGE warnings.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-i18n-namespace-restructure/46-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update converter namespace strings (converters → converter)</name>
  <files>
    src/app/[locale]/health/page.tsx
    src/app/[locale]/health/*/page.tsx
    src/app/[locale]/math/*/page.tsx
    src/app/[locale]/finance/*/page.tsx
    src/app/[locale]/photo/*/page.tsx
    src/app/[locale]/video/*/page.tsx
    src/app/[locale]/web/*/page.tsx
    src/app/[locale]/data/*/page.tsx
    src/app/[locale]/datetime/*/page.tsx
    src/app/[locale]/physics/*/page.tsx
    src/app/[locale]/music/*/page.tsx
    src/app/[locale]/color/*/page.tsx
    src/app/[locale]/chemistry/*/page.tsx
    src/app/[locale]/engineering/*/page.tsx
    src/app/[locale]/infrastructure/*/page.tsx
    src/app/[locale]/cooking/*/page.tsx
    src/app/[locale]/automotive/*/page.tsx
    src/app/[locale]/crypto/*/page.tsx
    src/app/[locale]/realestate/*/page.tsx
    src/app/[locale]/network/*/page.tsx
    src/app/[locale]/all/page.tsx
  </files>
  <action>
    Replace all occurrences of the `converters` namespace with `converter` (singular) in source files.

    **Patterns to replace (exact string replacements):**
    - `namespace: "converters.` → `namespace: "converter.`
    - `namespace: 'converters.` → `namespace: 'converter.`
    - `getTranslations("converters.` → `getTranslations("converter.`
    - `getTranslations('converters.` → `getTranslations('converter.`
    - `getTranslations("converters")` → `getTranslations("converter")`
    - `getTranslations('converters')` → `getTranslations('converter')`
    - `useTranslations("converters")` → `useTranslations("converter")`
    - `useTranslations('converters')` → `useTranslations('converter')`
    - `useTranslations("converters.` → `useTranslations("converter.`
    - `useTranslations('converters.` → `useTranslations('converter.`

    **Files affected (~21 files):** All category `page.tsx` files that call `getTranslations("converters")` or `getTranslations("converters.X")`. Also `src/components/converter/` files using `useTranslations("converters.*")`.

    Identify all affected files first:
    ```bash
    grep -rl '"converters' src/app src/components src/lib
    grep -rl "'converters" src/app src/components src/lib
    ```

    Then apply replacements using the Read + Edit tools for each affected file. Do NOT use `sed` — use the Edit tool for precision.

    After editing each file, verify the change with a quick grep to confirm no old patterns remain:
    ```bash
    grep -n "converters" src/app/[locale]/health/page.tsx
    ```
  </action>
  <verify>
    ```bash
    node -e "
    const {execSync} = require('child_process');
    // No old 'converters' namespace string should remain in source code
    try {
      const out = execSync('grep -rn \"\\\"converters\" src/app src/components --include=\"*.tsx\" --include=\"*.ts\" 2>/dev/null').toString();
      if (out.trim()) { console.log('FAIL: old converters refs found:\\n' + out); }
      else { console.log('OK: no old converters refs in app/components'); }
    } catch { console.log('OK: no old converters refs found'); }
    "
    ```
    Also check lib directory:
    ```bash
    grep -rn '"converters' src/lib --include="*.tsx" --include="*.ts" || echo "OK: none in lib"
    ```
  </verify>
  <done>
    Zero occurrences of `"converters"` or `'converters'` as a namespace string in any .tsx/.ts file.
    All 21 affected files updated to use `"converter"` (singular).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update categories and subcategories namespace strings (categories → nav)</name>
  <files>
    src/app/[locale]/*/page.tsx
    src/app/[locale]/page.tsx
    src/components/layout/header.tsx
    src/components/converter/subcategory-nav.tsx
  </files>
  <action>
    Replace all occurrences of the `categories` namespace with `nav` in source files. Also replace `subcategories` with `nav.subcategories`.

    **Patterns to replace:**
    - `namespace: "categories.` → `namespace: "nav.`
    - `namespace: 'categories.` → `namespace: 'nav.`
    - `getTranslations("categories.` → `getTranslations("nav.`
    - `getTranslations('categories.` → `getTranslations('nav.`
    - `getTranslations("categories")` → `getTranslations("nav")`
    - `getTranslations('categories')` → `getTranslations('nav')`
    - `useTranslations("categories")` → `useTranslations("nav")`
    - `useTranslations('categories')` → `useTranslations('nav')`
    - `useTranslations("subcategories")` → `useTranslations("nav.subcategories")`
    - `useTranslations('subcategories')` → `useTranslations('nav.subcategories')`

    **Files affected (~186 files):** All 20 category `page.tsx` landing pages, `src/app/[locale]/page.tsx` (homepage), all ~170 individual calculator `page.tsx` files that call `getTranslations("categories")` for metadata, `src/components/layout/header.tsx`, and `src/components/converter/subcategory-nav.tsx`.

    Identify all affected files:
    ```bash
    grep -rl '"categories' src/app src/components src/lib
    grep -rl "'categories" src/app src/components src/lib
    grep -rl '"subcategories"' src/app src/components
    ```

    Process the files. For `page.tsx` files, the change is typically on 2 lines (one for generateMetadata, one for the page body). Use the Edit tool for each file.

    **Important:** When changing `getTranslations("categories")` → `getTranslations("nav")`, the call to `tc(\`${converter.id}.name\`)` in category listing pages will now access `nav.[converter-id].name`. But wait — `converter.id` values are calculator slugs like "bmi", "compound-interest", etc. These are NOT in the `nav` namespace. The category listing pages use `tc` (from `getTranslations("converters")`) to get converter names, and `t` (from `getTranslations("categories.health")`) for category name/description.

    **Only change the namespace that was `categories` → `nav`. Leave the separate `converters` → `converter` change from Task 1 in place.**

    In `src/components/layout/header.tsx`: change `useTranslations("categories")` → `useTranslations("nav")`

    In `src/components/converter/subcategory-nav.tsx`: change `useTranslations("subcategories")` → `useTranslations("nav.subcategories")`

    Also update the `metadata` namespace used in `src/app/[locale]/layout.tsx`:
    - `namespace: "metadata"` → `namespace: "common.metadata"`

    And update the `realestate.property-valuation` namespace in any file that uses it:
    - `namespace: "realestate.property-valuation"` → `namespace: "calculator.realestate.property-valuation"` (it was already there)
    - Check which file uses this: `grep -rn "realestate.property-valuation" src/`
  </action>
  <verify>
    ```bash
    # Check no old categories namespace strings remain
    node -e "
    const {execSync} = require('child_process');
    const patterns = ['\"categories\"', \"'categories'\", '\"subcategories\"', \"'subcategories'\"];
    let found = false;
    for (const p of patterns) {
      try {
        const cmd = 'grep -rn ' + p + ' src/app src/components --include=\"*.tsx\" --include=\"*.ts\" 2>/dev/null';
        const out = execSync(cmd).toString().trim();
        if (out) { console.log('FAIL old pattern ' + p + ':\\n' + out); found = true; }
      } catch {}
    }
    if (!found) console.log('OK: no old categories/subcategories namespace refs');
    "
    ```
  </verify>
  <done>
    Zero occurrences of `"categories"`, `'categories'`, `"subcategories"`, `'subcategories'` as namespace strings in source files.
    Zero occurrences of `"metadata"` as a standalone namespace string in source files.
    `subcategory-nav.tsx` uses `useTranslations("nav.subcategories")`.
    `header.tsx` uses `useTranslations("nav")`.
    `layout.tsx` uses `namespace: "common.metadata"`.
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. Grep for any remaining old namespace strings:
   ```bash
   grep -rn '"converters' src/app src/components --include="*.tsx" --include="*.ts" || echo "OK"
   grep -rn '"categories' src/app src/components --include="*.tsx" --include="*.ts" || echo "OK"
   grep -rn '"subcategories"' src/app src/components --include="*.tsx" --include="*.ts" || echo "OK"
   ```

2. TypeScript type check:
   ```bash
   npm run type-check
   ```

3. Linting:
   ```bash
   npm run check:fix
   ```

4. Attempt build (will reveal MISSING_MESSAGE if any namespace paths are wrong):
   ```bash
   npm run build 2>&1 | grep -i "missing_message\|error\|warn" | head -30
   ```
   Zero MISSING_MESSAGE warnings required.
</verification>

<success_criteria>
- All ~207 source files updated to use new namespace strings
- Zero old namespace strings (converters, categories, subcategories, metadata at top-level) remain in source code
- TypeScript type-check passes (0 errors)
- Biome linting passes
- Build succeeds with zero MISSING_MESSAGE warnings
</success_criteria>

<output>
After completion, create `.planning/phases/46-i18n-namespace-restructure/46-02-SUMMARY.md`
</output>
